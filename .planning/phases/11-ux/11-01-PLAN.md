---
phase: 11-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/form.ts
  - src/components/ui/SectionHeader.tsx
  - src/components/wizard/WizardTabs.tsx
  - src/pages/NamingPage.tsx
autonomous: true
requirements:
  - UX-TAB-REDESIGN
must_haves:
  truths:
    - "TabId 타입이 4개 값(analysis, identity, persona, expression)을 갖는다"
    - "WizardTabs가 4개 탭을 균등 분할(25%)로 렌더한다"
    - "SectionHeader가 subtitle prop을 선택적으로 받아 12px eyebrow 텍스트를 렌더한다"
    - "NamingPage에서 persona 탭 선택 시 PersonaTab 컴포넌트가 렌더된다"
    - "WizardTabs에서 좌/우 화살표 키보드 네비게이션이 작동한다"
    - "activeTab === 'expression' 분기가 ExpressionTab을 렌더한다 (기존 동작 보존)"
  artifacts:
    - path: "src/types/form.ts"
      provides: "TabId union type with persona"
      contains: "persona"
    - path: "src/components/ui/SectionHeader.tsx"
      provides: "SectionHeader with subtitle prop"
      contains: "subtitle"
    - path: "src/components/wizard/WizardTabs.tsx"
      provides: "4-tab navigation with a11y"
      contains: "persona"
    - path: "src/pages/NamingPage.tsx"
      provides: "PersonaTab routing"
      contains: "PersonaTab"
  key_links:
    - from: "src/components/wizard/WizardTabs.tsx"
      to: "src/types/form.ts"
      via: "TabId import"
      pattern: "import.*TabId.*form"
    - from: "src/pages/NamingPage.tsx"
      to: "src/components/wizard/PersonaTab.tsx"
      via: "PersonaTab import"
      pattern: "import.*PersonaTab"
---

<objective>
4탭 위저드의 기반 인프라를 구축한다: TabId 타입 확장, SectionHeader subtitle prop 추가, WizardTabs 4탭 전환 + 접근성 개선, NamingPage 페르소나 탭 라우팅 추가.

Purpose: 후속 Plan 02/03에서 페르소나 그룹 컴포넌트와 탭 내용 재배치를 수행하기 위한 타입/UI 기반 마련
Output: 4탭 네비게이션이 작동하는 위저드 (페르소나 탭은 빈 placeholder)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ux/11-CONTEXT.md
@.planning/phases/11-ux/11-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/form.ts (line 73, current):
```typescript
export type TabId = 'analysis' | 'identity' | 'expression';
```

From src/components/ui/SectionHeader.tsx (current full file):
```typescript
interface SectionHeaderProps {
  title: string;
}
export function SectionHeader({ title }: SectionHeaderProps) {
  return (
    <div className="flex items-center gap-3 mb-5 pb-3 border-b border-[#D0CAC2]">
      <div className="w-1 h-4 rounded-full bg-[#B48C50]" />
      <h2 className="text-[14px] font-semibold text-[#2C2825]">{title}</h2>
    </div>
  );
}
```

From src/components/wizard/WizardTabs.tsx (current full file):
```typescript
import type { TabId } from '../../types/form';
interface Tab { id: TabId; label: string; }
const TABS: Tab[] = [
  { id: 'analysis', label: '분석' },
  { id: 'identity', label: '정체성' },
  { id: 'expression', label: '표현' },
];
interface WizardTabsProps {
  activeTab: TabId;
  onTabChange: (tab: TabId) => void;
}
// Indicator: width: ${100 / 3}%
```

From src/pages/NamingPage.tsx (tab rendering, lines 126-140):
```typescript
{activeTab === 'analysis' && (<div className="animate-fadeIn"><AnalysisTab /></div>)}
{activeTab === 'identity' && (<div className="animate-fadeIn"><IdentityTab /></div>)}
{activeTab === 'expression' && (<div className="animate-fadeIn"><ExpressionTab /></div>)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TabId 타입 확장 + SectionHeader subtitle prop 추가</name>
  <files>src/types/form.ts, src/components/ui/SectionHeader.tsx</files>
  <read_first>src/types/form.ts, src/components/ui/SectionHeader.tsx</read_first>
  <action>
**src/types/form.ts:**
TabId union type에 `'persona'` 추가. D-01 순서대로:
```typescript
export type TabId = 'analysis' | 'identity' | 'persona' | 'expression';
```
다른 타입(PersonaState, IdentityState 등)은 일절 수정하지 않는다. Zustand persist 스키마에 영향 없음 (activeTab은 persist 대상 아님).

**src/components/ui/SectionHeader.tsx:**
`subtitle` optional prop과 `icon` optional prop을 추가한다. 기존 call site는 영향 없음 (props가 optional).

변경 후 전체 파일:
```typescript
import type { LucideIcon } from 'lucide-react';

interface SectionHeaderProps {
  title: string;
  subtitle?: string;
  icon?: LucideIcon;
}

export function SectionHeader({ title, subtitle, icon: Icon }: SectionHeaderProps) {
  return (
    <div className="flex items-center gap-3 mb-5 pb-3 border-b border-[#D0CAC2]">
      {Icon ? (
        <Icon size={16} className="text-[#B48C50] shrink-0" />
      ) : (
        <div className="w-1 h-4 rounded-full bg-[#B48C50]" />
      )}
      <div>
        {subtitle && (
          <span className="block text-[12px] font-semibold text-[#6B6560] leading-[1.4]">
            {subtitle}
          </span>
        )}
        <h2 className="text-[14px] font-semibold text-[#2C2825]">{title}</h2>
      </div>
    </div>
  );
}
```

핵심 디자인 결정 (per UI-SPEC):
- subtitle은 12px semibold, color #6B6560 (muted) — eyebrow 역할
- icon prop은 LucideIcon 타입, 16px, color #B48C50 (accent)
- icon이 있으면 기존 w-1 h-4 pill 대신 icon 렌더
- icon이 없으면 기존 pill 유지 (하위 호환)
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/types/form.ts contains `'persona'` in TabId union
    - src/components/ui/SectionHeader.tsx contains `subtitle?: string`
    - src/components/ui/SectionHeader.tsx contains `icon?: LucideIcon`
    - src/components/ui/SectionHeader.tsx contains `text-[12px] font-semibold text-[#6B6560]`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>TabId에 persona 값 추가됨, SectionHeader에 subtitle/icon prop 추가됨, 기존 call site 하위 호환 유지</done>
</task>

<task type="auto">
  <name>Task 2: WizardTabs 4탭 전환 + 접근성 개선</name>
  <files>src/components/wizard/WizardTabs.tsx</files>
  <read_first>src/components/wizard/WizardTabs.tsx</read_first>
  <action>
WizardTabs.tsx를 4탭 구조로 업데이트한다 (per D-01, D-02).

**TABS 배열 변경:**
```typescript
const TABS: Tab[] = [
  { id: 'analysis', label: '분석' },
  { id: 'identity', label: '정체성' },
  { id: 'persona', label: '페르소나' },
  { id: 'expression', label: '표현' },
];
```

**슬라이딩 인디케이터 변경:**
`width: ${100 / 3}%` → `width: ${100 / TABS.length}%` (동적 계산으로 변경하여 하드코딩 제거)

**접근성 개선 (per UI-SPEC Interaction section):**

1. 부모 div에 `role="tablist"` 추가
2. 각 탭 버튼에 `aria-label` 추가 (접근성 이름):
   - analysis: `분석 탭 — 시장 환경`
   - identity: `정체성 탭 — 브랜드 내부`
   - persona: `페르소나 탭 — 브랜드 성격`
   - expression: `표현 탭 — 네이밍 출력`
3. 각 탭 버튼에 `tabIndex={isActive ? 0 : -1}` 추가
4. 포커스 링 추가: `focus-visible:outline-2 focus-visible:outline-[#B48C50] focus-visible:outline-offset-[-2px]`
5. 키보드 핸들러 추가: tablist div에 `onKeyDown` 핸들러
   - ArrowRight → 다음 탭 (마지막이면 첫 번째로 wrap)
   - ArrowLeft → 이전 탭 (첫 번째이면 마지막으로 wrap)
   - Home → 첫 번째 탭 (analysis)
   - End → 마지막 탭 (expression)
   - 탭 변경 시 해당 버튼에 `.focus()` 호출

Tab 인터페이스에 `ariaLabel` 필드 추가:
```typescript
interface Tab {
  id: TabId;
  label: string;
  ariaLabel: string;
}

const TABS: Tab[] = [
  { id: 'analysis', label: '분석', ariaLabel: '분석 탭 — 시장 환경' },
  { id: 'identity', label: '정체성', ariaLabel: '정체성 탭 — 브랜드 내부' },
  { id: 'persona', label: '페르소나', ariaLabel: '페르소나 탭 — 브랜드 성격' },
  { id: 'expression', label: '표현', ariaLabel: '표현 탭 — 네이밍 출력' },
];
```

키보드 핸들러 구현:
```typescript
const handleKeyDown = (e: React.KeyboardEvent) => {
  const currentIndex = TABS.findIndex((t) => t.id === activeTab);
  let nextIndex = currentIndex;

  switch (e.key) {
    case 'ArrowRight':
      nextIndex = (currentIndex + 1) % TABS.length;
      break;
    case 'ArrowLeft':
      nextIndex = (currentIndex - 1 + TABS.length) % TABS.length;
      break;
    case 'Home':
      nextIndex = 0;
      break;
    case 'End':
      nextIndex = TABS.length - 1;
      break;
    default:
      return;
  }

  e.preventDefault();
  onTabChange(TABS[nextIndex].id);
  // focus the new tab button
  const buttons = (e.currentTarget as HTMLElement).querySelectorAll<HTMLButtonElement>('[role="tab"]');
  buttons[nextIndex]?.focus();
};
```

파일은 500줄 이하 유지 (현재 53줄 → 약 85줄 예상).
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - WizardTabs.tsx contains `{ id: 'persona', label: '페르소나'`
    - WizardTabs.tsx contains `role="tablist"`
    - WizardTabs.tsx contains `ArrowRight`
    - WizardTabs.tsx contains `ArrowLeft`
    - WizardTabs.tsx contains `focus-visible:outline-2`
    - WizardTabs.tsx contains `TABS.length` (dynamic indicator width)
    - WizardTabs.tsx contains `tabIndex={isActive ? 0 : -1}`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>WizardTabs가 4개 탭(분석/정체성/페르소나/표현)을 25% 균등 분할로 렌더하고, 슬라이딩 인디케이터가 동적 너비를 사용하며, 좌/우/Home/End 키보드 네비게이션과 focus-visible 링이 동작한다</done>
</task>

<task type="auto">
  <name>Task 3: NamingPage 페르소나 탭 라우팅 추가</name>
  <files>src/pages/NamingPage.tsx</files>
  <read_first>src/pages/NamingPage.tsx</read_first>
  <action>
NamingPage.tsx에 PersonaTab 라우팅을 추가한다.

**1. Import 추가:**
기존 import 블록(line 9-11)에 추가:
```typescript
import { PersonaTab } from '../components/wizard/PersonaTab';
```

NOTE: PersonaTab.tsx는 아직 존재하지 않으므로 (Plan 02에서 생성), 이 시점에서는 **placeholder 컴포넌트를 NamingPage 내부에 임시로 정의**한다:
```typescript
// TODO: Plan 02에서 실제 PersonaTab으로 대체
function PersonaTabPlaceholder() {
  return (
    <div className="text-center py-20 text-[#A09890] text-[14px]">
      <p className="font-semibold mb-2">페르소나 탭</p>
      <p>Plan 02에서 구현 예정</p>
    </div>
  );
}
```

**2. 탭 렌더링 분기 추가:**
기존 identity 분기(line 131-135)와 expression 분기(line 136-140) 사이에 추가:
```tsx
{activeTab === 'persona' && (
  <div className="animate-fadeIn">
    <PersonaTabPlaceholder />
  </div>
)}
```

**3. ExpressionTab 기존 동작 보존 (per D-01: expression 탭은 변경 없음):**
기존 `{activeTab === 'expression' && (<div className="animate-fadeIn"><ExpressionTab /></div>)}` 분기가 그대로 유지되는지 반드시 확인한다. persona 분기를 expression 분기 앞에 삽입할 때 expression 분기를 실수로 삭제하거나 덮어쓰지 않도록 주의.

**4. Plan 02 완료 후:** placeholder를 실제 `import { PersonaTab }` 으로 교체하고 `PersonaTabPlaceholder` 함수를 삭제한다. 이 교체 작업은 Plan 02 Task에서 수행.

파일은 500줄 이하 유지 (현재 190줄 → 약 205줄).
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - NamingPage.tsx contains `activeTab === 'persona'`
    - NamingPage.tsx contains `PersonaTabPlaceholder`
    - NamingPage.tsx contains `animate-fadeIn` for persona tab section
    - NamingPage.tsx contains `activeTab === 'expression'` with `ExpressionTab` (existing behavior preserved)
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>NamingPage에서 4번째 탭(persona) 선택 시 placeholder가 렌더되고, expression 탭은 기존 ExpressionTab을 그대로 렌더하며, dev server에서 4탭 전환이 모두 작동한다</done>
</task>

</tasks>

<verification>
```bash
cd C:/BRAND && npx tsc --noEmit
cd C:/BRAND && npx vite build --mode development 2>&1 | tail -5
```
- TypeScript 컴파일 에러 없음
- Vite 빌드 성공
- 4개 탭 모두 클릭 시 컨텐츠 전환 확인 (dev server에서)
</verification>

<success_criteria>
- TabId가 4개 값을 갖는다
- SectionHeader에 subtitle/icon optional props가 추가되어 있다
- WizardTabs가 4탭을 25% 균등 분할로 렌더한다
- WizardTabs에 role="tablist", 키보드 네비게이션, focus-visible 링이 있다
- NamingPage에서 persona 탭이 placeholder를 렌더한다
- activeTab === 'expression' 분기가 ExpressionTab을 렌더한다 (기존 동작 보존)
- 기존 3개 탭의 기능이 깨지지 않았다
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux/11-01-SUMMARY.md`
</output>
