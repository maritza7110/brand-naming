---
phase: 11-ux
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/wizard/PersonaTab.tsx
  - src/components/sections/persona/PersonaIdentityGroup.tsx
  - src/components/sections/persona/PersonaStrategyGroup.tsx
  - src/components/sections/persona/PersonaMarketGroup.tsx
  - src/components/sections/persona/PersonaBenefitsGroup.tsx
  - src/components/sections/persona/PersonaExperienceGroup.tsx
  - src/pages/NamingPage.tsx
autonomous: true
requirements:
  - UX-PERSONA-TAB
must_haves:
  truths:
    - "페르소나 탭에 5개 그룹 컨테이너가 순서대로 렌더된다"
    - "각 그룹은 SectionHeader(title + subtitle eyebrow + icon)로 시작한다"
    - "그룹2(전략&경쟁력)에만 '브랜드 내부 관점' 배지와 헬퍼 텍스트가 표시된다"
    - "16개 필드가 5개 그룹에 D-05 매핑대로 분배된다"
    - "KeywordWeightSlider + RecommendButton이 그룹5 아래에 렌더된다"
    - "NamingPage의 placeholder가 실제 PersonaTab으로 교체된다"
  artifacts:
    - path: "src/components/wizard/PersonaTab.tsx"
      provides: "페르소나 탭 루트 컴포넌트"
      contains: "PersonaIdentityGroup"
    - path: "src/components/sections/persona/PersonaIdentityGroup.tsx"
      provides: "그룹1: 철학/슬로건/브랜드멘트/키워드 (4 fields)"
      contains: "philosophy"
    - path: "src/components/sections/persona/PersonaStrategyGroup.tsx"
      provides: "그룹2: 핵심기술/핵심전략/비교우위 (3 fields) + 관점 배지"
      contains: "브랜드 내부 관점"
    - path: "src/components/sections/persona/PersonaMarketGroup.tsx"
      provides: "그룹3: 고객정의/고객가치/고객문화창조 (3 fields)"
      contains: "customerDefinition"
    - path: "src/components/sections/persona/PersonaBenefitsGroup.tsx"
      provides: "그룹4: 품질/가격/기능/경험/상징 혜택 (5 fields)"
      contains: "qualityLevel"
    - path: "src/components/sections/persona/PersonaExperienceGroup.tsx"
      provides: "그룹5: 멤버쉽 철학 (1 field)"
      contains: "membershipPhilosophy"
  key_links:
    - from: "src/components/wizard/PersonaTab.tsx"
      to: "src/store/useFormStore.ts"
      via: "useFormStore selector"
      pattern: "useFormStore.*persona"
    - from: "src/components/wizard/PersonaTab.tsx"
      to: "src/hooks/useRecommend.ts"
      via: "useRecommend hook"
      pattern: "useRecommend"
    - from: "src/pages/NamingPage.tsx"
      to: "src/components/wizard/PersonaTab.tsx"
      via: "import PersonaTab"
      pattern: "import.*PersonaTab"
---

<objective>
페르소나 탭의 5개 그룹 컨테이너 컴포넌트를 생성하고, PersonaTab 루트에서 조립하며, NamingPage의 placeholder를 실제 컴포넌트로 교체한다.

Purpose: D-05의 5그룹 분리를 구현하여 16개 페르소나 필드를 의미 기반으로 그룹화
Output: 완전한 페르소나 탭 (5그룹 스크롤, 키워드 슬라이더, 추천 버튼)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ux/11-CONTEXT.md
@.planning/phases/11-ux/11-UI-SPEC.md
@.planning/phases/11-ux/11-01-SUMMARY.md

<interfaces>
<!-- From existing PersonaSection.tsx — field definitions to reuse -->
```typescript
// src/components/sections/PersonaSection.tsx (will be deleted after this plan)
const F: { key: keyof PersonaState; label: string; ph: string }[] = [
  { key: 'philosophy', label: '브랜드철학', ph: '좋은 원두, 정직한 한 잔' },
  { key: 'slogan', label: '슬로건', ph: '하루의 시작, 당신만의 한 잔' },
  { key: 'coreTechnology', label: '핵심기술', ph: '산지 직거래 로스팅' },
  { key: 'coreStrategy', label: '핵심전략', ph: '지역 밀착 커뮤니티 브랜딩' },
  { key: 'brandMent', label: '브랜드 멘트', ph: '커피 한 잔에 담긴 진심' },
  { key: 'customerDefinition', label: '고객정의', ph: '20~40대, 품질 좋은 커피를 원하는 고객' },
  { key: 'customerValue', label: '고객가치', ph: '일상 속 여유로운 커피 한 잔' },
  { key: 'customerCultureCreation', label: '고객문화창조', ph: '동네 사랑방 같은 커피 문화' },
  { key: 'competitiveAdvantage', label: '비교우위', ph: '핸드드립 전문, 아늑한 인테리어' },
  { key: 'qualityLevel', label: '품질수준', ph: '스페셜티 등급 원두' },
  { key: 'priceLevel', label: '가격수준', ph: '아메리카노 3,500원' },
  { key: 'functionalBenefit', label: '기능적혜택', ph: '출근길 빠른 픽업' },
  { key: 'experientialBenefit', label: '경험적혜택', ph: '아늑한 공간, 바리스타와의 대화' },
  { key: 'symbolicBenefit', label: '상징적혜택', ph: '감각적 라이프스타일' },
  { key: 'brandKeyword', label: '키워드', ph: '정직, 따뜻함, 장인정신' },
  { key: 'membershipPhilosophy', label: '멤버쉽 철학', ph: '단골에게 특별한 경험과 감사' },
];
```

<!-- Store actions used by groups -->
```typescript
// src/store/useFormStore.ts
updatePersona: (key: keyof PersonaState, value: string) => void;
```

<!-- SectionHeader updated in Plan 01 -->
```typescript
interface SectionHeaderProps {
  title: string;
  subtitle?: string;
  icon?: LucideIcon;
}
```

<!-- Established card style (from UI-SPEC) -->
Card wrapper: `rounded-2xl bg-[#E8E4DE] p-5 lg:p-7 border border-[#C5BFB7]`
Inter-group gap: `space-y-8` (32px)
Intra-field gap: `space-y-4` (16px)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5개 페르소나 그룹 컴포넌트 생성</name>
  <files>
    src/components/sections/persona/PersonaIdentityGroup.tsx,
    src/components/sections/persona/PersonaStrategyGroup.tsx,
    src/components/sections/persona/PersonaMarketGroup.tsx,
    src/components/sections/persona/PersonaBenefitsGroup.tsx,
    src/components/sections/persona/PersonaExperienceGroup.tsx
  </files>
  <read_first>
    src/components/sections/PersonaSection.tsx,
    src/components/ui/SectionHeader.tsx,
    src/components/ui/TextField.tsx,
    src/store/useFormStore.ts
  </read_first>
  <action>
`src/components/sections/persona/` 디렉토리를 생성하고 5개 그룹 컴포넌트를 작성한다.

**공통 패턴 (모든 그룹):**
- `useFormStore((s) => s.persona)` + `useFormStore((s) => s.updatePersona)` 사용
- 카드 wrapper: `<section className="rounded-2xl bg-[#E8E4DE] p-5 lg:p-7 border border-[#C5BFB7]">`
- `<SectionHeader title="..." subtitle="..." icon={...} />`
- 필드 stack: `<div className="space-y-4">`
- 각 필드는 `<TextField>` 사용 (MiniRecommendButton은 페르소나 탭에서 제거 — per UI-SPEC Interaction section)
- PersonaState 타입 import: `import type { PersonaState } from '../../../types/form';`

**그룹 1: PersonaIdentityGroup.tsx**
```typescript
import { useFormStore } from '../../../store/useFormStore';
import { SectionHeader } from '../../ui/SectionHeader';
import { TextField } from '../../ui/TextField';
import type { PersonaState } from '../../../types/form';
import { Sparkles } from 'lucide-react';

const FIELDS: { key: keyof PersonaState; label: string; ph: string }[] = [
  { key: 'philosophy', label: '브랜드철학', ph: '좋은 원두, 정직한 한 잔' },
  { key: 'slogan', label: '슬로건', ph: '하루의 시작, 당신만의 한 잔' },
  { key: 'brandMent', label: '브랜드 멘트', ph: '커피 한 잔에 담긴 진심' },
  { key: 'brandKeyword', label: '키워드', ph: '정직, 따뜻함, 장인정신' },
];

export function PersonaIdentityGroup() {
  const p = useFormStore((s) => s.persona);
  const u = useFormStore((s) => s.updatePersona);
  return (
    <section className="rounded-2xl bg-[#E8E4DE] p-5 lg:p-7 border border-[#C5BFB7]">
      <SectionHeader title="브랜드 정체성" subtitle="1 / 5 · Identity & Visual" icon={Sparkles} />
      <div className="space-y-4">
        {FIELDS.map(({ key, label, ph }) => (
          <TextField key={key} label={label} value={p[key]} onChange={(v) => u(key, v)} placeholder={ph} />
        ))}
      </div>
    </section>
  );
}
```

**그룹 2: PersonaStrategyGroup.tsx**
필드: `coreTechnology`, `coreStrategy`, `competitiveAdvantage`
- SectionHeader: title=`전략 & 경쟁력`, subtitle=`2 / 5 · Strategy & Competitiveness`, icon=`Target`
- **관점 배지 추가** (per D-08, UI-SPEC): SectionHeader와 fields 사이에:
```tsx
<div className="flex items-center gap-2 mb-3">
  <span className="inline-block px-2 py-0.5 rounded-full text-[14px] text-[#6B6560] bg-[#DDD7CF]">
    브랜드 내부 관점
  </span>
</div>
<p className="text-[14px] text-[#6B6560] mb-4">
  분석 탭이 시장을 본다면, 여기선 "우리 자신이 누구인지"를 정의합니다. 우리만의 기술·전략·우위를 내부 시각으로 적어주세요.
</p>
```
- Placeholders (per UI-SPEC Copywriting):
  - coreTechnology: `예: 산지 직거래 로스팅, 매일 아침 소량 배전`
  - coreStrategy: `예: 지역 주민과 함께 만드는 동네 사랑방 전략`
  - competitiveAdvantage: `예: 우리는 커피를 팔지 않고, 시간을 판다 — 대체 불가한 공간감`

**그룹 3: PersonaMarketGroup.tsx**
필드: `customerDefinition`, `customerValue`, `customerCultureCreation`
- SectionHeader: title=`고객 & 시장`, subtitle=`3 / 5 · Market & Customer`, icon=`Users`
- 관점 배지 없음 (그룹2만 해당)
- Placeholders: 기존 PersonaSection.tsx에서 복사:
  - customerDefinition: `20~40대, 품질 좋은 커피를 원하는 고객`
  - customerValue: `일상 속 여유로운 커피 한 잔`
  - customerCultureCreation: `동네 사랑방 같은 커피 문화`

**그룹 4: PersonaBenefitsGroup.tsx**
필드: `qualityLevel`, `priceLevel`, `functionalBenefit`, `experientialBenefit`, `symbolicBenefit`
- SectionHeader: title=`혜택 & 가치`, subtitle=`4 / 5 · Benefits & Value`, icon=`Gift`
- 단일 컬럼 스택 (2-column grid 사용 금지 — per UI-SPEC)
- Placeholders: 기존 PersonaSection.tsx에서 복사

**그룹 5: PersonaExperienceGroup.tsx**
필드: `membershipPhilosophy` (1개)
- SectionHeader: title=`고객 경험`, subtitle=`5 / 5 · Experience & Management`, icon=`HeartHandshake`
- TextArea 사용 (단일 필드이므로 여러 줄 입력이 자연스러움)
- Placeholder: `단골에게 특별한 경험과 감사`

각 파일은 ~30-50줄 이내 (500줄 제한 여유).
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/persona/PersonaIdentityGroup.tsx contains `philosophy` and `brandKeyword` and `Sparkles` and `1 / 5`
    - src/components/sections/persona/PersonaStrategyGroup.tsx contains `coreTechnology` and `Target` and `브랜드 내부 관점` and `2 / 5`
    - src/components/sections/persona/PersonaMarketGroup.tsx contains `customerDefinition` and `Users` and `3 / 5`
    - src/components/sections/persona/PersonaBenefitsGroup.tsx contains `qualityLevel` and `symbolicBenefit` and `Gift` and `4 / 5`
    - src/components/sections/persona/PersonaExperienceGroup.tsx contains `membershipPhilosophy` and `HeartHandshake` and `5 / 5`
    - All 5 files use `rounded-2xl bg-[#E8E4DE] p-5 lg:p-7 border border-[#C5BFB7]`
    - None of the 5 files contain `MiniRecommendButton` (removed per UI-SPEC)
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>5개 페르소나 그룹 컴포넌트가 D-05 매핑대로 생성됨, 각각 SectionHeader(title+subtitle+icon) 사용, 그룹2에만 관점 배지/헬퍼 텍스트 포함</done>
</task>

<task type="auto">
  <name>Task 2: PersonaTab 루트 컴포넌트 생성 + NamingPage placeholder 교체</name>
  <files>
    src/components/wizard/PersonaTab.tsx,
    src/pages/NamingPage.tsx
  </files>
  <read_first>
    src/pages/NamingPage.tsx,
    src/components/wizard/AnalysisTab.tsx,
    src/components/wizard/IdentityTab.tsx,
    src/store/useFormStore.ts
  </read_first>
  <action>
**1. PersonaTab.tsx 생성:**

AnalysisTab/IdentityTab 패턴을 따른다. 핵심 구조:

```typescript
import { useFormStore } from '../../store/useFormStore';
import { PersonaIdentityGroup } from '../sections/persona/PersonaIdentityGroup';
import { PersonaStrategyGroup } from '../sections/persona/PersonaStrategyGroup';
import { PersonaMarketGroup } from '../sections/persona/PersonaMarketGroup';
import { PersonaBenefitsGroup } from '../sections/persona/PersonaBenefitsGroup';
import { PersonaExperienceGroup } from '../sections/persona/PersonaExperienceGroup';
import { KeywordWeightSlider } from '../ui/KeywordWeightSlider';
import { RecommendButton } from '../ui/RecommendButton';
import { useRecommend } from '../../hooks/useRecommend';

const PERSONA_LABELS: Record<string, string> = {
  philosophy: '브랜드철학',
  slogan: '슬로건',
  brandMent: '브랜드멘트',
  brandKeyword: '브랜드키워드',
  coreTechnology: '핵심기술',
  coreStrategy: '핵심전략',
  competitiveAdvantage: '비교우위',
  customerDefinition: '고객정의',
  customerValue: '고객가치',
  customerCultureCreation: '고객문화창조',
  qualityLevel: '품질수준',
  priceLevel: '가격수준',
  functionalBenefit: '기능적혜택',
  experientialBenefit: '경험적혜택',
  symbolicBenefit: '상징적혜택',
  membershipPhilosophy: '멤버쉽철학',
};

export function PersonaTab() {
  const persona = useFormStore((s) => s.persona);
  const keywordWeights = useFormStore((s) => s.keywordWeights);
  const setKeywordWeight = useFormStore((s) => s.setKeywordWeight);
  const { recommend, isLoading } = useRecommend();

  // 입력된 키워드 목록 추출 (AnalysisTab/IdentityTab 패턴과 동일)
  const keywords: { label: string; weight: number }[] = [];
  Object.entries(persona).forEach(([key, value]) => {
    if (typeof value === 'string' && value.trim() !== '') {
      const label = PERSONA_LABELS[key] ?? key;
      keywords.push({ label, weight: keywordWeights[label] ?? 3 });
    }
  });

  const hasInput = Object.values(persona).some(
    (v) => typeof v === 'string' && v.trim() !== ''
  );

  return (
    <div className="space-y-5">
      {/* 탭 인트로 (per UI-SPEC Copywriting) */}
      <div className="mb-4">
        <h3 className="text-[14px] font-semibold text-[#D0CAC2] mb-2">
          브랜드 성격을 정의해주세요
        </h3>
        <p className="text-[14px] text-[#A09890]">
          아래 5개 그룹은 브랜드의 내부 관점입니다. 분석 탭이 "시장이 어떻게 보이는가"라면, 여기서는 "우리가 무엇이 되고 싶은가"를 기록합니다.
        </p>
      </div>

      {/* 5개 그룹 — space-y-8 (32px inter-group gap per UI-SPEC) */}
      <div className="space-y-8">
        <PersonaIdentityGroup />
        <PersonaStrategyGroup />
        <PersonaMarketGroup />
        <PersonaBenefitsGroup />
        <PersonaExperienceGroup />
      </div>

      {/* KeywordWeightSlider + RecommendButton (탭 하단, 그룹 밖) */}
      {keywords.length > 0 && (
        <div className="mt-8 rounded-2xl bg-[#363230] p-5 lg:p-7 border border-[#4A4440]">
          <KeywordWeightSlider keywords={keywords} onChange={setKeywordWeight} />
        </div>
      )}
      <div className="flex justify-end">
        <RecommendButton
          onClick={recommend}
          loading={isLoading}
          disabled={!hasInput}
          label="페르소나 기반 추천"
          loadingLabel="분석 중…"
          disabledLabel="한 개 이상 입력해주세요"
        />
      </div>
    </div>
  );
}
```

NOTE: RecommendButton의 label/loadingLabel/disabledLabel props — 현재 RecommendButton이 이 props를 지원하지 않을 수 있음. 그 경우 기존 패턴(props 없이)을 따르되, 기존 AnalysisTab/IdentityTab의 RecommendButton 호출 방식을 그대로 복사한다. executor가 RecommendButton.tsx를 read_first에서 확인하여 맞춤.

**2. NamingPage.tsx placeholder 교체:**

Plan 01에서 추가한 `PersonaTabPlaceholder` 함수를 삭제하고 실제 import로 교체:

- 삭제: `function PersonaTabPlaceholder() { ... }` 전체
- 추가 (import 블록): `import { PersonaTab } from '../components/wizard/PersonaTab';`
- 변경: `<PersonaTabPlaceholder />` → `<PersonaTab />`

**3. PersonaSection.tsx 삭제:**
IdentityTab에서 PersonaSection import가 Plan 03에서 제거된 후, PersonaSection.tsx 파일을 삭제한다. **단, Plan 03과의 순서 의존성을 고려하여: 이 Task에서는 삭제하지 않는다. Plan 03에서 IdentityTab 수정 후 삭제한다.**
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/PersonaTab.tsx contains `PersonaIdentityGroup` and `PersonaStrategyGroup` and `PersonaMarketGroup` and `PersonaBenefitsGroup` and `PersonaExperienceGroup`
    - src/components/wizard/PersonaTab.tsx contains `space-y-8` (32px inter-group gap)
    - src/components/wizard/PersonaTab.tsx contains `브랜드 성격을 정의해주세요` (tab intro heading)
    - src/components/wizard/PersonaTab.tsx contains `useRecommend`
    - src/components/wizard/PersonaTab.tsx contains `PERSONA_LABELS`
    - src/pages/NamingPage.tsx contains `import { PersonaTab }` (not placeholder)
    - src/pages/NamingPage.tsx does NOT contain `PersonaTabPlaceholder`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>PersonaTab이 5개 그룹을 조립하여 렌더하고, NamingPage에서 실제 PersonaTab 컴포넌트가 로드되며, dev server에서 페르소나 탭 클릭 시 5개 그룹이 보인다</done>
</task>

</tasks>

<verification>
```bash
cd C:/BRAND && npx tsc --noEmit
cd C:/BRAND && npx vite build --mode development 2>&1 | tail -5
```
- TypeScript 에러 없음
- 5개 그룹이 올바른 순서로 렌더됨
- 16개 필드가 누락 없이 분배됨
- 키워드 슬라이더와 추천 버튼이 동작함
</verification>

<success_criteria>
- 페르소나 탭에서 5개 그룹 컨테이너가 브랜드정체성→전략경쟁력→고객시장→혜택가치→고객경험 순서로 표시
- 각 그룹에 고유 lucide 아이콘과 "N / 5" eyebrow 표시
- 그룹2에만 "브랜드 내부 관점" 배지와 헬퍼 텍스트 표시
- 16개 필드가 빠짐없이 올바른 그룹에 배치
- MiniRecommendButton이 페르소나 탭 필드에 없음 (per UI-SPEC)
- NamingPage에서 persona 탭 선택 시 실제 PersonaTab 렌더
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux/11-02-SUMMARY.md`
</output>
