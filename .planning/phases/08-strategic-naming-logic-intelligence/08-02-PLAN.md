---
phase: 08-strategic-naming-logic-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/wizard/WizardTabs.tsx
  - src/components/wizard/AnalysisTab.tsx
  - src/components/wizard/IdentityTab.tsx
  - src/components/wizard/ExpressionTab.tsx
  - src/components/sections/CompetitorSection.tsx
  - src/components/sections/USPSection.tsx
  - src/components/sections/MarketTrendSection.tsx
  - src/components/sections/BrandPersonalitySection.tsx
  - src/components/layout/InputPanel.tsx
  - src/pages/NamingPage.tsx
autonomous: true
requirements:
  - "3단계 입력 위저드 UI"
  - "신규 데이터 항목 UI"
  - "탭 네비게이션"
  - "시장중심 배치"

must_haves:
  truths:
    - "상단에 [분석] [정체성] [표현] 3개 탭이 표시된다"
    - "탭 클릭 시 해당 콘텐츠로 즉시 전환된다"
    - "분석 탭에 StoreBasic + 경쟁사 + USP가 표시된다"
    - "정체성 탭에 BrandVision + Persona + 시장트렌드 + 브랜드퍼스널리티가 표시된다"
    - "표현 탭에 Product + 고급옵션(네이밍스타일+언어제약)이 표시된다"
    - "각 탭에서 AI 추천 받기 버튼을 사용할 수 있다"
  artifacts:
    - path: "src/components/wizard/WizardTabs.tsx"
      provides: "탭 네비게이션 바"
      exports: ["WizardTabs"]
    - path: "src/components/wizard/AnalysisTab.tsx"
      provides: "분석 탭 컨테이너"
      exports: ["AnalysisTab"]
    - path: "src/components/wizard/IdentityTab.tsx"
      provides: "정체성 탭 컨테이너"
      exports: ["IdentityTab"]
    - path: "src/components/wizard/ExpressionTab.tsx"
      provides: "표현 탭 컨테이너"
      exports: ["ExpressionTab"]
    - path: "src/pages/NamingPage.tsx"
      provides: "탭 위저드 통합 페이지"
      contains: "WizardTabs"
  key_links:
    - from: "src/pages/NamingPage.tsx"
      to: "src/components/wizard/WizardTabs.tsx"
      via: "import and render WizardTabs"
      pattern: "WizardTabs"
    - from: "src/components/wizard/AnalysisTab.tsx"
      to: "src/components/sections/StoreBasicSection.tsx"
      via: "import and render inside tab"
      pattern: "StoreBasicSection"
    - from: "src/components/wizard/IdentityTab.tsx"
      to: "src/components/sections/BrandPersonalitySection.tsx"
      via: "import and render ChipSelector"
      pattern: "BrandPersonalitySection"
---

<objective>
3단계 탭 위저드 UI를 구축한다: WizardTabs 네비게이션, 3개 탭 컨테이너, 4개 신규 섹션 컴포넌트, InputPanel/NamingPage 전환.

Purpose: 기존 단일 스크롤 폼을 탭 기반 위저드로 전환하여 구조화된 입력 경험을 제공한다 (per D-01, D-02).
Output: 완전한 탭 위저드 UI가 동작하는 NamingPage
</objective>

<execution_context>
@C:/Users/고상진/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-CONTEXT.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-RESEARCH.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-UI-SPEC.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 타입 (executor가 사용해야 할 계약) -->
From src/types/form.ts (Plan 01 이후):
```typescript
export type TabId = 'analysis' | 'identity' | 'expression';
export interface AnalysisState { competitors: string; usp: string; }
export interface IdentityState { marketTrend: string; brandPersonality: string[]; }
export interface ExpressionState { namingStyle: string[]; languageConstraint: string; }
```

From src/store/useFormStore.ts (Plan 01 이후):
```typescript
// 신규 액션:
updateAnalysis: (field: keyof AnalysisState, value: string) => void;
updateIdentityText: (field: 'marketTrend', value: string) => void;
updateIdentityChips: (field: 'brandPersonality', value: string[]) => void;
updateExpressionChips: (field: 'namingStyle', value: string[]) => void;
updateExpressionText: (field: 'languageConstraint', value: string) => void;
setActiveTab: (tab: TabId) => void;
setKeywordWeight: (keyword: string, weight: number) => void;
```

From src/components/ui/ChipSelector.tsx (Plan 01 이후):
```typescript
export function ChipSelector({ label, options, selected, onChange, maxSelection, hint }: ChipSelectorProps)
```

From src/components/ui/KeywordWeightSlider.tsx (Plan 01 이후):
```typescript
export function KeywordWeightSlider({ keywords, onChange }: KeywordWeightSliderProps)
```

From src/components/ui/AdvancedOptionsToggle.tsx (Plan 01 이후):
```typescript
export function AdvancedOptionsToggle({ children }: AdvancedOptionsToggleProps)
```

<!-- 기존 코드 — 참조용 -->
From src/components/sections/StoreBasicSection.tsx:
```typescript
export function StoreBasicSection()  // 기존 섹션 — 분석 탭으로 이동
// 내부에 useRecommend() + RecommendButton 포함 — 탭 컨테이너로 이동 필요
```

From src/components/ui/SectionHeader.tsx:
```typescript
export function SectionHeader({ title }: { title: string })
```

From src/hooks/useRecommend.ts:
```typescript
export function useRecommend(): { recommend: () => Promise<void>; isLoading: boolean }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WizardTabs + 4개 신규 섹션 컴포넌트</name>
  <files>src/components/wizard/WizardTabs.tsx, src/components/sections/CompetitorSection.tsx, src/components/sections/USPSection.tsx, src/components/sections/MarketTrendSection.tsx, src/components/sections/BrandPersonalitySection.tsx</files>
  <read_first>
    - .planning/phases/08-strategic-naming-logic-intelligence/08-UI-SPEC.md (WizardTabs #1, CompetitorInput #2, USPInput #3, MarketTrendInput #4, BrandPersonalityChips #5)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-RESEARCH.md (Pattern 1: 탭 위저드)
    - src/components/sections/StoreBasicSection.tsx (기존 섹션 컴포넌트 패턴 참조)
    - src/components/sections/BrandVisionSection.tsx (기존 섹션 컴포넌트 패턴 참조)
    - src/components/ui/TextArea.tsx (재사용 대상)
    - src/components/ui/TextField.tsx (재사용 대상)
    - src/components/ui/SectionHeader.tsx (재사용 대상)
    - src/store/useFormStore.ts (Plan 01 이후 확장된 스토어)
  </read_first>
  <action>
5개 파일을 생성한다.

**1. src/components/wizard/WizardTabs.tsx** (per D-01, UI-SPEC #1):
- Props: `{ activeTab: TabId; onTabChange: (tab: TabId) => void }`
- 탭 데이터: `[{ id: 'analysis', label: '분석' }, { id: 'identity', label: '정체성' }, { id: 'expression', label: '표현' }]`
- 레이아웃: `relative flex border-b border-[#4A4440]` 배경 `bg-[#2C2825]`
- 각 탭 버튼: `flex-1 h-[44px] text-[14px] font-semibold transition-colors`
  - 활성: `text-[#B48C50]`, 비활성: `text-[#A09890]`
- 슬라이딩 인디케이터: absolute bottom-0, h-[2px], bg-[#B48C50], transition-transform duration-300 ease-in-out
  - width: `${100 / 3}%`, transform: `translateX(${activeIndex * 100}%)`

**2. src/components/sections/CompetitorSection.tsx** (per D-09, UI-SPEC #2):
- useFormStore에서 analysis.competitors 읽기, updateAnalysis('competitors', value) 호출
- SectionHeader title="경쟁사 분석" 사용
- TextArea 컴포넌트 재사용: label="경쟁사 분석", rows=3
- placeholder="주요 경쟁 브랜드명과 그 특징을 적어주세요 (예: '스타벅스 - 프리미엄 이미지, 이디야 - 가성비')"
- 기존 섹션과 동일한 rounded-2xl bg-[#E8E4DE] p-5 lg:p-7 border border-[#C5BFB7] 스타일

**3. src/components/sections/USPSection.tsx** (per D-09, UI-SPEC #3):
- useFormStore에서 analysis.usp 읽기, updateAnalysis('usp', value) 호출
- SectionHeader title="USP (차별화 요소)"
- TextArea: rows=2
- placeholder="다른 브랜드와 구별되는 핵심 차별점을 적어주세요 (예: '로컬 원두 직접 로스팅, 반려동물 동반 특화')"
- 동일한 섹션 카드 스타일

**4. src/components/sections/MarketTrendSection.tsx** (per D-09, UI-SPEC #4):
- useFormStore에서 identity.marketTrend 읽기, updateIdentityText('marketTrend', value) 호출
- SectionHeader title="시장 트렌드"
- TextArea: rows=2
- placeholder="현재 업계의 주요 흐름이나 소비자 트렌드를 적어주세요 (예: '건강 지향, 비건, 제로슈거 열풍')"
- 동일한 섹션 카드 스타일

**5. src/components/sections/BrandPersonalitySection.tsx** (per D-09, UI-SPEC #5):
- useFormStore에서 identity.brandPersonality 읽기, updateIdentityChips('brandPersonality', value) 호출
- SectionHeader title="브랜드 퍼스널리티"
- ChipSelector 컴포넌트 사용:
  - options: ['진정성 있는', '혁신적인', '따뜻한', '세련된', '대담한', '친근한', '신뢰감 있는', '유쾌한', '고급스러운', '자연친화적']
  - maxSelection: 3
  - hint: "최대 3개 선택"
- 동일한 섹션 카드 스타일

주의: 기존 섹션 컴포넌트(StoreBasicSection 등)에서 RecommendButton을 제거하지 않는다. 탭 컨테이너(Task 2)에서 추천 버튼을 관리할 것이므로, 기존 섹션의 추천 버튼은 Task 2에서 처리.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/WizardTabs.tsx 존재, `export function WizardTabs` 포함
    - WizardTabs.tsx에 `'분석'`, `'정체성'`, `'표현'` 라벨 존재
    - WizardTabs.tsx에 `h-[44px]` (터치 타겟) 존재
    - WizardTabs.tsx에 `bg-[#B48C50]` (인디케이터 색상) 존재
    - WizardTabs.tsx에 `translateX` (슬라이딩 애니메이션) 존재
    - src/components/sections/CompetitorSection.tsx 존재, `export function CompetitorSection` 포함
    - CompetitorSection.tsx에 `updateAnalysis` 호출 존재
    - CompetitorSection.tsx에 placeholder에 "스타벅스" 포함
    - src/components/sections/USPSection.tsx 존재, `export function USPSection` 포함
    - USPSection.tsx에 `updateAnalysis` 호출 존재
    - src/components/sections/MarketTrendSection.tsx 존재, `export function MarketTrendSection` 포함
    - MarketTrendSection.tsx에 `updateIdentityText` 호출 존재
    - src/components/sections/BrandPersonalitySection.tsx 존재, `export function BrandPersonalitySection` 포함
    - BrandPersonalitySection.tsx에 `ChipSelector` import 존재
    - BrandPersonalitySection.tsx에 `maxSelection={3}` 또는 `maxSelection: 3` 존재
    - 모든 파일 500줄 이하
  </acceptance_criteria>
  <done>WizardTabs와 4개 신규 섹션이 생성되고, UI-SPEC 색상/스페이싱 계약을 따르며, 스토어와 연결됨</done>
</task>

<task type="auto">
  <name>Task 2: 3개 탭 컨테이너 + InputPanel/NamingPage 전환</name>
  <files>src/components/wizard/AnalysisTab.tsx, src/components/wizard/IdentityTab.tsx, src/components/wizard/ExpressionTab.tsx, src/components/sections/StoreBasicSection.tsx, src/components/layout/InputPanel.tsx, src/pages/NamingPage.tsx</files>
  <read_first>
    - src/pages/NamingPage.tsx (현재 페이지 구조 — 전환 대상)
    - src/components/layout/InputPanel.tsx (현재 레이아웃 — 위저드 통합 대상)
    - src/components/sections/StoreBasicSection.tsx (기존 — 추천 버튼 제거 대상)
    - src/components/sections/BrandVisionSection.tsx (기존 — 추천 버튼 제거 참조)
    - src/components/sections/PersonaSection.tsx (기존 — 추천 버튼 제거 참조)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-UI-SPEC.md (Layout Contract, 탭 전환 인터랙션)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-CONTEXT.md (D-01 자유 이동, D-02 배치, D-03 즉시 추천)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-RESEARCH.md (Pitfall 5: 추천 버튼 상태)
    - src/hooks/useRecommend.ts (추천 훅)
    - src/components/ui/KeywordWeightSlider.tsx (Plan 01에서 생성)
    - src/store/useFormStore.ts (Plan 01 이후)
  </read_first>
  <action>
6개 파일을 생성/수정한다.

**1. src/components/wizard/AnalysisTab.tsx** (per D-02: 분석 탭):
- StoreBasicSection, CompetitorSection, USPSection을 순서대로 렌더링
- 하단에 KeywordWeightSlider + RecommendButton 배치
- KeywordWeightSlider의 keywords: 현재 분석 탭에서 입력된 필드 라벨을 추출하는 함수 작성
  - storeBasic에서 industry.major가 있으면 '업종', 나머지 string 필드 중 비어있지 않은 필드의 FIELD_LABELS
  - analysis에서 competitors가 있으면 '경쟁사', usp가 있으면 'USP'
- useRecommend() 훅 사용, RecommendButton에 onClick={recommend}, loading={isLoading}
- hasInput: storeBasic 또는 analysis에 하나라도 입력된 값이 있으면 true
- space-y-5 레이아웃

**2. src/components/wizard/IdentityTab.tsx** (per D-02: 정체성 탭):
- BrandVisionSection, PersonaSection, MarketTrendSection, BrandPersonalitySection 순서대로
- 하단에 KeywordWeightSlider + RecommendButton
- keywords: brandVision, persona, identity 필드 중 입력된 항목의 라벨
- hasInput: 해당 섹션들에 하나라도 입력된 값이 있으면 true

**3. src/components/wizard/ExpressionTab.tsx** (per D-02: 표현 탭):
- ProductSection 렌더링
- AdvancedOptionsToggle 내부에:
  - NamingStyleChips: ChipSelector 사용, options=['합성어','추상어','은유/상징','두문자','의성어/의태어','외래어 차용','한글 순수','숫자 조합'], maxSelection=2, hint="최대 2개 선택"
  - LanguageConstraint: TextField, label="언어 제약", placeholder="예: '영문 5자 이내', '한글+영문 조합', '발음하기 쉬운'"
- NamingStyleChips는 useFormStore의 expression.namingStyle, updateExpressionChips 사용
- LanguageConstraint는 useFormStore의 expression.languageConstraint, updateExpressionText 사용
- 하단에 KeywordWeightSlider + RecommendButton
- hasInput: product 또는 expression에 하나라도 입력된 값이 있으면 true

**FIELD_LABELS 상수:** 신규 필드용 라벨을 각 탭 컨테이너에 정의하거나 공유 상수 파일에 정의:
```typescript
const ANALYSIS_LABELS: Record<string, string> = {
  competitors: '경쟁사',
  usp: 'USP',
  // storeBasic 라벨은 기존 gemini.ts의 FIELD_LABELS 참조
};
```

**4. 기존 섹션 수정 — StoreBasicSection.tsx에서 RecommendButton 제거:**
- import useRecommend, RecommendButton 제거
- 하단의 `<div className="mt-6 flex justify-end"><RecommendButton .../>` 제거
- hasInput 로직 제거 (더 이상 필요 없음)
- 참고: BrandVisionSection, PersonaSection도 동일하게 RecommendButton 제거 필요

**5. src/components/layout/InputPanel.tsx 수정:**
- children 아래에 `<div className="space-y-5">{children}</div>` 유지
- 히어로 영역 하단과 children 사이에 WizardTabs 슬롯 추가:
  - InputPanel props에 `tabBar?: React.ReactNode` 추가
  - 히어로 `</div>` 이후, children `<div>` 이전에 `{tabBar}` 렌더링
  - tabBar는 sticky top-0 z-10 bg-[#2C2825] 처리 (모바일 스크롤 시 고정)

**6. src/pages/NamingPage.tsx 수정:**
- 기존 `<StoreBasicSection /><BrandVisionSection /><PersonaSection />` 직접 렌더링 제거
- useFormStore에서 activeTab, setActiveTab 가져오기
- InputPanel에 tabBar prop으로 `<WizardTabs activeTab={activeTab} onTabChange={setActiveTab} />` 전달
- children으로 조건부 렌더링:
```tsx
{activeTab === 'analysis' && <AnalysisTab />}
{activeTab === 'identity' && <IdentityTab />}
{activeTab === 'expression' && <ExpressionTab />}
```
- 탭 전환 시 콘텐츠 fadeIn: 각 탭 래퍼에 `className="animate-fadeIn"` (Tailwind 커스텀 animation 또는 opacity transition)

주의사항:
- 탭 전환 시 입력값은 Zustand에 있으므로 유실 없음 (unmount 안전)
- 추천 버튼은 전체 폼에서 하나라도 입력되었으면 활성화 (per Pitfall 5)
- BrandVisionSection, PersonaSection의 RecommendButton도 제거해야 함 (StoreBasicSection과 동일)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/AnalysisTab.tsx 존재, `export function AnalysisTab` 포함
    - AnalysisTab.tsx에 StoreBasicSection, CompetitorSection, USPSection import 존재
    - AnalysisTab.tsx에 KeywordWeightSlider import 존재
    - AnalysisTab.tsx에 RecommendButton import 존재
    - src/components/wizard/IdentityTab.tsx 존재, BrandVisionSection, PersonaSection, MarketTrendSection, BrandPersonalitySection import 존재
    - src/components/wizard/ExpressionTab.tsx 존재, AdvancedOptionsToggle import 존재
    - ExpressionTab.tsx에 ChipSelector import 존재 (NamingStyleChips 역할)
    - ExpressionTab.tsx에 `maxSelection={2}` 존재
    - src/components/sections/StoreBasicSection.tsx에 RecommendButton import가 없음
    - src/pages/NamingPage.tsx에 `WizardTabs` import 존재
    - NamingPage.tsx에 `activeTab === 'analysis'` 조건부 렌더링 존재
    - NamingPage.tsx에 `AnalysisTab`, `IdentityTab`, `ExpressionTab` import 존재
    - InputPanel.tsx에 `tabBar` prop 존재
    - 모든 파일 500줄 이하
  </acceptance_criteria>
  <done>3단계 탭 위저드가 완전히 동작: 탭 전환, 섹션 배치, 각 탭별 추천 버튼과 키워드 가중치 슬라이더 포함</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 전체 통과
- `npm run dev` 후 브라우저에서 3개 탭 전환 확인
- 분석 탭: StoreBasic + 경쟁사 + USP + 키워드가중치 + 추천버튼
- 정체성 탭: BrandVision + Persona + 시장트렌드 + 퍼스널리티칩 + 키워드가중치 + 추천버튼
- 표현 탭: Product + 고급옵션(네이밍스타일+언어제약) + 키워드가중치 + 추천버튼
</verification>

<success_criteria>
- 3개 탭이 자유롭게 전환되고 입력값이 유지됨
- 4개 신규 섹션이 올바른 탭에 배치됨
- 각 탭에 KeywordWeightSlider와 RecommendButton이 하단에 표시됨
- 기존 섹션에서 RecommendButton이 제거됨
- 모든 파일 500줄 이하
</success_criteria>

<output>
After completion, create `.planning/phases/08-strategic-naming-logic-intelligence/08-02-SUMMARY.md`
</output>
