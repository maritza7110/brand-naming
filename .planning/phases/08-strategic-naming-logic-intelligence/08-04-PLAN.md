---
phase: 08-strategic-naming-logic-intelligence
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/components/recommend/RationaleExpandedCard.tsx
  - src/components/recommend/RecommendCardItem.tsx
  - src/components/recommend/EmptyState.tsx
autonomous: false
requirements:
  - "Rationale UI (네이밍 근거)"
  - "논리적 타당성 점수 표시"
  - "카드 확장 방식"

must_haves:
  truths:
    - "추천 카드를 클릭하면 확장되어 rationale 상세 정보가 표시된다"
    - "타당성 점수가 백분율(%)과 프로그레스 바로 표시된다"
    - "네이밍 기법이 칩 형태로 표시된다"
    - "의미 분석 텍스트가 표시된다"
    - "반영된 입력 항목이 칩 리스트로 표시된다"
    - "rationale 없는 기존 배치도 정상 렌더링된다"
    - "EmptyState가 위저드에 맞는 안내 문구로 업데이트된다"
  artifacts:
    - path: "src/components/recommend/RationaleExpandedCard.tsx"
      provides: "확장형 근거 카드 컴포넌트"
      exports: ["RationaleExpandedCard"]
    - path: "src/components/recommend/RecommendCardItem.tsx"
      provides: "클릭 확장 가능한 카드 (기존 수정)"
      contains: "RationaleExpandedCard"
    - path: "src/components/recommend/EmptyState.tsx"
      provides: "위저드 맞춤 빈 상태"
      contains: "각 탭의 항목을 입력하고"
  key_links:
    - from: "src/components/recommend/RecommendCardItem.tsx"
      to: "src/components/recommend/RationaleExpandedCard.tsx"
      via: "import and conditional render"
      pattern: "RationaleExpandedCard"
    - from: "src/components/recommend/RationaleExpandedCard.tsx"
      to: "src/types/form.ts"
      via: "import RationaleData type"
      pattern: "RationaleData"
---

<objective>
추천 카드에 Rationale(근거) 확장 UI를 추가하고, EmptyState를 위저드에 맞게 업데이트한다.

Purpose: 각 추천 이름에 대한 논리적 근거(타당성 점수, 네이밍 기법, 의미 분석, 반영된 입력)를 시각화하여 투명성을 높인다 (per D-07, D-08).
Output: 클릭 확장 가능한 RationaleExpandedCard, 수정된 RecommendCardItem, 업데이트된 EmptyState
</objective>

<execution_context>
@C:/Users/고상진/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-CONTEXT.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-UI-SPEC.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-01-SUMMARY.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-03-SUMMARY.md

<interfaces>
From src/types/form.ts (Plan 01 이후):
```typescript
export interface RationaleData {
  validityScore: number;      // 0~100 (%)
  namingTechnique: string;    // "합성어", "은유/상징" 등
  meaningAnalysis: string;    // 의미 분석 설명
  reflectedInputs: string[];  // 반영된 입력 항목명
}

export interface RecommendBatch {
  id: string;
  names: { brandName: string; reasoning: string; rationale?: RationaleData }[];
  basedOn: string[];
  createdAt: Date;
  industry?: IndustrySelection;
}
```

From src/components/recommend/RecommendCardItem.tsx (현재):
```typescript
interface Props { batch: RecommendBatch; }
export function RecommendCardItem({ batch }: Props)
// 카드 구조: rounded-xl bg-[#2C2825] border border-[#4A4440] p-4
// 각 name: brandName (text-[16px] font-bold text-[#B48C50]) + reasoning (text-[14px] text-[#D0CAC2])
// 하단: basedOn 칩 + 시간
```

From src/components/recommend/EmptyState.tsx (현재):
```typescript
export function EmptyState()
// 빈 상태 메시지 표시
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RationaleExpandedCard 생성 + RecommendCardItem 수정</name>
  <files>src/components/recommend/RationaleExpandedCard.tsx, src/components/recommend/RecommendCardItem.tsx</files>
  <read_first>
    - src/components/recommend/RecommendCardItem.tsx (현재 카드 구조)
    - src/components/recommend/RecommendGroup.tsx (기존 접기/펼치기 패턴 참조 — grid-template-rows)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-UI-SPEC.md (RationaleExpandedCard #10 상세 스펙)
    - src/types/form.ts (RationaleData 타입)
  </read_first>
  <action>
2개 파일을 생성/수정한다.

**1. src/components/recommend/RationaleExpandedCard.tsx** (per D-07, D-08, UI-SPEC #10):

Props:
```typescript
interface RationaleExpandedCardProps {
  rationale: RationaleData;
  isOpen: boolean;
}
```

레이아웃:
- 전체를 `grid` 기반 접기/펼치기로 구현 (기존 RecommendGroup 패턴):
  - `grid transition-all duration-200 ease-in-out`
  - isOpen: `grid-rows-[1fr]`, 닫힘: `grid-rows-[0fr]`
  - 내부: `overflow-hidden`

확장 콘텐츠 (isOpen일 때 표시):
- 상단 구분선: `border-t border-[#332F2C] mt-3 pt-3`

- **타당성 점수 영역:**
  - flex justify-between items-center
  - 좌측: `text-[20px] font-semibold text-[#B48C50]` — `{validityScore}%` 형태
  - 우측: `text-[12px] text-[#A09890]` — "타당성 점수"
  - 아래: 프로그레스 바
    - 전체: `h-[4px] bg-[#4A4440] rounded-full mt-2`
    - 채움: `h-full bg-[#B48C50] rounded-full` + width를 `style={{ width: '${score}%' }}`로 설정
    - 애니메이션: `transition-all duration-600 ease-in-out` (isOpen 시 0% -> N%)

- **네이밍 기법 영역:** (구분선: `border-t border-[#332F2C] mt-3 pt-3`)
  - 라벨: `text-[12px] font-semibold text-[#A09890] mb-1.5` — "네이밍 기법"
  - 값: 칩 형태 `px-2 py-0.5 rounded-full bg-[#4A4440] text-[12px] text-[#D0CAC2]` (basedOn 칩과 동일)

- **의미 분석 영역:** (구분선)
  - 라벨: `text-[12px] font-semibold text-[#A09890] mb-1.5` — "의미 분석"
  - 본문: `text-[14px] text-[#D0CAC2] leading-relaxed`

- **반영된 입력 영역:** (구분선)
  - 라벨: `text-[12px] font-semibold text-[#A09890] mb-1.5` — "반영된 입력"
  - 칩 리스트: flex flex-wrap gap-1.5
    - 각 칩: `px-2 py-0.5 rounded-full bg-[rgba(180,140,80,0.1)] text-[#B48C50] text-[12px]`

**2. src/components/recommend/RecommendCardItem.tsx 수정:**

- 각 name 항목을 개별 확장 가능한 카드로 변경:
  - name마다 내부 `useState<number | null>(null)` (확장된 name의 인덱스)
  - 또는 `useState<Set<number>>(new Set())` (다중 확장 지원)
  
- 각 name 영역에:
  - 클릭 핸들러: 해당 인덱스 토글
  - 우측 하단에 ChevronDown 아이콘 (14px, text-[#A09890])
    - 확장 시 rotate-180 transition 200ms
  - `cursor-pointer` 추가
  - `role="button"` + `aria-expanded={isOpen}` 접근성

- rationale이 있는 name만 확장 가능:
  - `n.rationale ? <RationaleExpandedCard rationale={n.rationale} isOpen={isExpanded} />` : null
  - rationale이 없으면 ChevronDown 아이콘도 숨김

- 기존 brandName font-bold를 font-semibold로 변경 (UI-SPEC 타이포그래피: 600 semibold)

- 기존 basedOn 칩, 시간 표시는 카드 하단에 유지 (확장 영역 아래)

- import: ChevronDown from lucide-react, RationaleExpandedCard, useState
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/recommend/RationaleExpandedCard.tsx 존재, `export function RationaleExpandedCard` 포함
    - RationaleExpandedCard.tsx에 `validityScore` 렌더링 존재 (% 표시)
    - RationaleExpandedCard.tsx에 "타당성 점수" 텍스트 존재
    - RationaleExpandedCard.tsx에 "네이밍 기법" 텍스트 존재
    - RationaleExpandedCard.tsx에 "의미 분석" 텍스트 존재
    - RationaleExpandedCard.tsx에 "반영된 입력" 텍스트 존재
    - RationaleExpandedCard.tsx에 `bg-[rgba(180,140,80,0.1)]` (반영된 입력 칩 배경) 존재
    - RationaleExpandedCard.tsx에 `grid-rows` 패턴 존재 (접기/펼치기)
    - RecommendCardItem.tsx에 `RationaleExpandedCard` import 존재
    - RecommendCardItem.tsx에 `ChevronDown` import 존재
    - RecommendCardItem.tsx에 `aria-expanded` 존재
    - RecommendCardItem.tsx에 `n.rationale` 조건 체크 존재
    - RecommendCardItem.tsx에 font-semibold 사용 (font-bold 아닌)
    - 두 파일 모두 500줄 이하
  </acceptance_criteria>
  <done>추천 카드 클릭 시 rationale 상세 정보가 확장/축소되고, rationale 없는 기존 배치도 정상 동작함</done>
</task>

<task type="auto">
  <name>Task 2: EmptyState 업데이트</name>
  <files>src/components/recommend/EmptyState.tsx</files>
  <read_first>
    - src/components/recommend/EmptyState.tsx (현재 코드)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-UI-SPEC.md (Copywriting Contract — Empty state)
  </read_first>
  <action>
EmptyState 컴포넌트의 문구를 위저드에 맞게 업데이트한다:

- heading: "추천 대기 중" (기존과 동일하면 유지)
- body: "각 탭의 항목을 입력하고 추천 받기를 눌러보세요. 단계가 진행될수록 더 정교한 이름이 나옵니다."

UI-SPEC Copywriting Contract의 문구를 정확히 사용한다. 기존 스타일(색상, 크기, 간격)은 유지.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - EmptyState.tsx에 "추천 대기 중" 텍스트 존재
    - EmptyState.tsx에 "각 탭의 항목을 입력하고 추천 받기를 눌러보세요" 텍스트 존재
    - EmptyState.tsx에 "단계가 진행될수록 더 정교한 이름이 나옵니다" 텍스트 존재
  </acceptance_criteria>
  <done>EmptyState가 위저드 컨셉에 맞는 안내 문구를 표시함</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 8 전체 기능 검증</name>
  <files>none</files>
  <action>
사용자가 전체 Phase 8 기능을 브라우저에서 직접 검증한다. `npm run dev`로 개발 서버를 실행하고 아래 검증 항목을 확인.
  </action>
  <what-built>
    3단계 탭 위저드 UI (분석/정체성/표현), 4개 신규 입력 필드, Gemini 프롬프트 고도화 (구조화 응답 + 키워드 가중치), Rationale 확장 카드 (타당성 점수 + 네이밍 기법 + 의미 분석 + 반영된 입력)
  </what-built>
  <how-to-verify>
    1. `npm run dev`로 개발 서버 실행
    2. http://localhost:5173 접속 (또는 /naming 페이지)
    3. 상단에 [분석] [정체성] [표현] 3개 탭이 보이는지 확인
    4. 각 탭 클릭 시 콘텐츠가 전환되는지 확인 (인디케이터 슬라이드)
    5. 분석 탭: 매장기본 + 경쟁사분석 + USP 입력 필드 확인
    6. 정체성 탭: 브랜드비전 + 페르소나 + 시장트렌드 + 브랜드퍼스널리티(칩 선택) 확인
    7. 표현 탭: 제품/서비스 + 고급옵션 토글(네이밍스타일 칩 + 언어제약) 확인
    8. 아무 탭에서 항목 입력 후 "AI 추천 받기" 클릭
    9. 추천 카드가 나타나면 카드 클릭 -> 확장되어 타당성 점수(%), 네이밍 기법, 의미 분석, 반영된 입력이 보이는지 확인
    10. 키워드 가중치 슬라이더가 입력된 필드에 대해 표시되는지 확인
  </how-to-verify>
  <verify>사용자가 "approved" 또는 문제점을 반환</verify>
  <done>사용자가 전체 위저드 플로우와 Rationale UI를 승인함</done>
  <resume-signal>"approved" 입력 또는 발견된 문제점 설명</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 전체 통과
- `npm run dev` 후 전체 위저드 플로우 동작 확인
- 추천 카드 클릭 시 rationale 확장/축소 동작
- 기존 배치 (rationale 없음)도 정상 렌더링
</verification>

<success_criteria>
- RationaleExpandedCard가 타당성 점수, 네이밍 기법, 의미 분석, 반영된 입력을 모두 표시
- 카드 클릭으로 확장/축소 토글 동작
- rationale 없는 기존 배치도 깨지지 않음
- EmptyState가 위저드 맞춤 문구 표시
- 사용자가 전체 플로우를 검증 완료
</success_criteria>

<output>
After completion, create `.planning/phases/08-strategic-naming-logic-intelligence/08-04-SUMMARY.md`
</output>
