---
phase: 08-strategic-naming-logic-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/form.ts
  - src/store/useFormStore.ts
  - src/components/ui/ChipSelector.tsx
  - src/components/ui/KeywordWeightSlider.tsx
  - src/components/ui/AdvancedOptionsToggle.tsx
autonomous: true
requirements:
  - "단계별 입력 프로세스 (타입 기반)"
  - "신규 데이터 항목 타입"
  - "키워드 가중치 설정"
  - "논리적 타당성 피드백 (RationaleData 타입)"

must_haves:
  truths:
    - "FormState에 analysis, identity, expression 신규 필드 그룹이 존재한다"
    - "RationaleData 타입이 정의되어 있고 RecommendBatch에서 참조한다"
    - "ChipSelector가 다중 선택 + FIFO 초과 처리를 지원한다"
    - "KeywordWeightSlider가 1~5 범위의 슬라이더를 렌더링한다"
    - "AdvancedOptionsToggle이 접기/펼치기를 수행한다"
  artifacts:
    - path: "src/types/form.ts"
      provides: "ExtendedFormState, RationaleData, TabId 타입"
      contains: "RationaleData"
    - path: "src/store/useFormStore.ts"
      provides: "신규 필드 + activeTab 상태 + 업데이트 액션"
      contains: "activeTab"
    - path: "src/components/ui/ChipSelector.tsx"
      provides: "범용 칩 선택 컴포넌트"
      exports: ["ChipSelector"]
    - path: "src/components/ui/KeywordWeightSlider.tsx"
      provides: "키워드 가중치 슬라이더"
      exports: ["KeywordWeightSlider"]
    - path: "src/components/ui/AdvancedOptionsToggle.tsx"
      provides: "고급 옵션 토글"
      exports: ["AdvancedOptionsToggle"]
  key_links:
    - from: "src/store/useFormStore.ts"
      to: "src/types/form.ts"
      via: "import ExtendedFormState types"
      pattern: "import.*form"
---

<objective>
Phase 8의 기반을 구축한다: 확장된 타입 시스템, Zustand 스토어 확장, 3개 신규 UI 프리미티브 컴포넌트.

Purpose: 이후 Plan 02~04가 의존하는 타입 계약과 상태 관리, 재사용 UI 컴포넌트를 먼저 확립한다.
Output: 확장된 form.ts 타입, 확장된 useFormStore, ChipSelector/KeywordWeightSlider/AdvancedOptionsToggle 컴포넌트
</objective>

<execution_context>
@C:/Users/고상진/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-CONTEXT.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-RESEARCH.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-UI-SPEC.md

<interfaces>
<!-- 현재 타입 시스템 — 확장 대상 -->
From src/types/form.ts:
```typescript
export interface FormState {
  storeBasic: StoreBasicState;
  brandVision: BrandVisionState;
  product: ProductState;
  persona: PersonaState;
}

export interface RecommendBatch {
  id: string;
  names: { brandName: string; reasoning: string }[];
  basedOn: string[];
  createdAt: Date;
  industry?: IndustrySelection;
}

export interface AppState extends FormState {
  batches: RecommendBatch[];
  isLoading: boolean;
  error: string | null;
  resetTimestamp: Date | null;
}
```

From src/store/useFormStore.ts:
```typescript
// persist version: 2, name: 'brand-naming-data'
// partialize: batches, resetTimestamp만 persist
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 타입 시스템 확장 (form.ts)</name>
  <files>src/types/form.ts</files>
  <read_first>
    - src/types/form.ts (현재 타입 전체 구조)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-RESEARCH.md (RationaleData 타입 정의, 파일 구조)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-CONTEXT.md (D-02 탭 배치, D-09 신규 항목)
  </read_first>
  <action>
src/types/form.ts에 다음 타입을 추가한다. 기존 타입(IndustrySelection, StoreBasicState, BrandVisionState, ProductState, PersonaState, FormState, RecommendBatch, AppState)은 모두 유지하되, 신규 타입과 확장을 추가:

1. TabId 타입 (per D-01):
```typescript
export type TabId = 'analysis' | 'identity' | 'expression';
```

2. 신규 섹션 상태 타입 (per D-09):
```typescript
/** 분석 탭 — 신규 필드 */
export interface AnalysisState {
  competitors: string;  // 경쟁사 분석 텍스트
  usp: string;          // USP/차별화 요소 텍스트
}

/** 정체성 탭 — 신규 필드 */
export interface IdentityState {
  marketTrend: string;          // 시장 트렌드 텍스트
  brandPersonality: string[];   // 브랜드 퍼스널리티 칩 선택 (최대 3개)
}

/** 표현 탭 — 신규 필드 (고급 옵션) */
export interface ExpressionState {
  namingStyle: string[];        // 네이밍 스타일 칩 선택 (최대 2개)
  languageConstraint: string;   // 언어 제약 텍스트
}
```

3. 키워드 가중치 타입 (per D-05):
```typescript
/** 키워드별 가중치 (1~5) */
export type KeywordWeights = Record<string, number>;
```

4. RationaleData 타입 (per D-07, D-08):
```typescript
export interface RationaleData {
  validityScore: number;      // 0~100 (%)
  namingTechnique: string;    // "합성어", "은유/상징" 등
  meaningAnalysis: string;    // 의미 분석 설명
  reflectedInputs: string[];  // 반영된 입력 항목명
}
```

5. RecommendBatch의 names 배열 내 항목에 optional rationale 추가:
```typescript
export interface RecommendBatch {
  id: string;
  names: { brandName: string; reasoning: string; rationale?: RationaleData }[];
  basedOn: string[];
  createdAt: Date;
  industry?: IndustrySelection;
}
```

6. FormState 확장 (기존 4개 + 신규 3개):
```typescript
export interface FormState {
  storeBasic: StoreBasicState;
  brandVision: BrandVisionState;
  product: ProductState;
  persona: PersonaState;
  analysis: AnalysisState;
  identity: IdentityState;
  expression: ExpressionState;
}
```

7. AppState 확장 (activeTab, keywordWeights 추가):
```typescript
export interface AppState extends FormState {
  batches: RecommendBatch[];
  isLoading: boolean;
  error: string | null;
  resetTimestamp: Date | null;
  activeTab: TabId;
  keywordWeights: KeywordWeights;
}
```

파일 500줄 제한 유지. 기존 인터페이스 순서 유지 후 신규 타입을 하단에 추가.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/types/form.ts에 `export type TabId = 'analysis' | 'identity' | 'expression'` 존재
    - src/types/form.ts에 `export interface AnalysisState` 존재하고 competitors, usp 필드 포함
    - src/types/form.ts에 `export interface IdentityState` 존재하고 marketTrend, brandPersonality 필드 포함
    - src/types/form.ts에 `export interface ExpressionState` 존재하고 namingStyle, languageConstraint 필드 포함
    - src/types/form.ts에 `export interface RationaleData` 존재하고 validityScore, namingTechnique, meaningAnalysis, reflectedInputs 필드 포함
    - src/types/form.ts에 `export type KeywordWeights = Record<string, number>` 존재
    - FormState에 analysis, identity, expression 필드 포함
    - AppState에 activeTab: TabId, keywordWeights: KeywordWeights 필드 포함
    - RecommendBatch.names 항목에 rationale?: RationaleData 필드 포함
    - 파일이 500줄 이하
  </acceptance_criteria>
  <done>모든 신규 타입이 export되고 tsc --noEmit 통과</done>
</task>

<task type="auto">
  <name>Task 2: Zustand 스토어 확장 (useFormStore.ts)</name>
  <files>src/store/useFormStore.ts</files>
  <read_first>
    - src/store/useFormStore.ts (현재 스토어 전체)
    - src/types/form.ts (Task 1에서 확장된 타입 — 실행 후 읽기)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-RESEARCH.md (Pitfall 1: 스토어 비대화, Pitfall 2: persist 마이그레이션)
  </read_first>
  <action>
src/store/useFormStore.ts를 확장한다. 기존 코드 구조를 유지하면서 신규 필드와 액션을 추가:

1. 신규 import 추가:
```typescript
import type {
  AppState,
  StoreBasicState,
  BrandVisionState,
  ProductState,
  PersonaState,
  RecommendBatch,
  IndustrySelection,
  AnalysisState,
  IdentityState,
  ExpressionState,
  TabId,
  KeywordWeights,
} from '../types/form';
```

2. 신규 초기값:
```typescript
const initialAnalysis: AnalysisState = {
  competitors: '',
  usp: '',
};

const initialIdentity: IdentityState = {
  marketTrend: '',
  brandPersonality: [],
};

const initialExpression: ExpressionState = {
  namingStyle: [],
  languageConstraint: '',
};
```

3. FormActions 인터페이스에 신규 액션 추가:
```typescript
updateAnalysis: (field: keyof AnalysisState, value: string) => void;
updateIdentityText: (field: 'marketTrend', value: string) => void;
updateIdentityChips: (field: 'brandPersonality', value: string[]) => void;
updateExpressionChips: (field: 'namingStyle', value: string[]) => void;
updateExpressionText: (field: 'languageConstraint', value: string) => void;
setActiveTab: (tab: TabId) => void;
setKeywordWeight: (keyword: string, weight: number) => void;
resetKeywordWeights: () => void;
```

4. create() 내부에 초기 상태 추가:
```typescript
analysis: { ...initialAnalysis },
identity: { ...initialIdentity },
expression: { ...initialExpression },
activeTab: 'analysis' as TabId,
keywordWeights: {} as KeywordWeights,
```

5. 신규 액션 구현:
- updateAnalysis: `set((state) => ({ analysis: { ...state.analysis, [field]: value } }))`
- updateIdentityText: `set((state) => ({ identity: { ...state.identity, [field]: value } }))`
- updateIdentityChips: `set((state) => ({ identity: { ...state.identity, [field]: value } }))`
- updateExpressionChips: `set((state) => ({ expression: { ...state.expression, [field]: value } }))`
- updateExpressionText: `set((state) => ({ expression: { ...state.expression, [field]: value } }))`
- setActiveTab: `set({ activeTab: tab })`
- setKeywordWeight: `set((state) => ({ keywordWeights: { ...state.keywordWeights, [keyword]: weight } }))`
- resetKeywordWeights: `set({ keywordWeights: {} })`

6. resetAll과 resetNaming에 신규 초기값 추가:
```typescript
analysis: { ...initialAnalysis },
identity: { ...initialIdentity },
expression: { ...initialExpression },
keywordWeights: {},
activeTab: 'analysis' as TabId,
```

7. persist 버전을 2 -> 3으로 올리고 마이그레이션 추가:
```typescript
version: 3,
migrate: (persisted: unknown, version: number) => {
  if (version < 2) {
    const p = persisted as { batches?: RecommendBatch[] };
    return { ...p, resetTimestamp: null };
  }
  if (version < 3) {
    // v2 -> v3: rationale 필드 없는 기존 배치 호환 (optional이므로 자동 호환)
    return persisted;
  }
  return persisted;
},
```

8. partialize는 기존 유지 (batches, resetTimestamp만 persist). 폼 입력값은 persist하지 않음.

파일 500줄 제한 주의. 현재 ~153줄이므로 확장 후에도 ~250줄 이내 예상.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - useFormStore.ts에 `initialAnalysis` 정의 존재 (competitors: '', usp: '')
    - useFormStore.ts에 `initialIdentity` 정의 존재 (marketTrend: '', brandPersonality: [])
    - useFormStore.ts에 `initialExpression` 정의 존재 (namingStyle: [], languageConstraint: '')
    - useFormStore.ts에 `activeTab: 'analysis'` 초기값 존재
    - useFormStore.ts에 `keywordWeights: {}` 초기값 존재
    - useFormStore.ts에 `updateAnalysis` 액션 존재
    - useFormStore.ts에 `setActiveTab` 액션 존재
    - useFormStore.ts에 `setKeywordWeight` 액션 존재
    - useFormStore.ts에 `version: 3` 존재
    - resetNaming 함수에 `analysis:` 초기화 포함
    - 파일이 500줄 이하
    - tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>스토어가 신규 필드/액션을 모두 포함하고, persist v3 마이그레이션이 적용됨</done>
</task>

<task type="auto">
  <name>Task 3: 신규 UI 프리미티브 3종 (ChipSelector, KeywordWeightSlider, AdvancedOptionsToggle)</name>
  <files>src/components/ui/ChipSelector.tsx, src/components/ui/KeywordWeightSlider.tsx, src/components/ui/AdvancedOptionsToggle.tsx</files>
  <read_first>
    - .planning/phases/08-strategic-naming-logic-intelligence/08-UI-SPEC.md (컴포넌트 5, 7, 9번 스펙)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-RESEARCH.md (Pattern 3: ChipSelector)
    - src/components/ui/Dropdown.tsx (기존 UI 컴포넌트 패턴 참조)
    - src/components/ui/TextField.tsx (기존 UI 컴포넌트 패턴 참조)
  </read_first>
  <action>
3개 신규 UI 컴포넌트를 생성한다. 모두 UI-SPEC의 색상/타이포그래피/스페이싱 계약을 정확히 따른다.

**1. src/components/ui/ChipSelector.tsx** (per D-10, UI-SPEC #5):
```typescript
interface ChipSelectorProps {
  label: string;
  options: string[];
  selected: string[];
  onChange: (selected: string[]) => void;
  maxSelection: number;
  hint?: string;
}
```
- 레이아웃: flex-wrap, gap-[8px]
- 칩 크기: px-3 py-1.5, text-[14px] font-semibold, rounded-lg
- 비선택 상태: bg-[#4A4440], text-[#D0CAC2], border border-[#4A4440]
- 선택 상태: bg-[rgba(180,140,80,0.15)], text-[#B48C50], border border-[#B48C50]
- 클릭 피드백: scale(0.95) -> scale(1) transition 100ms
- 최대 초과 시 FIFO: 가장 먼저 선택된 항목 해제 + 신규 추가
- 접근성: 각 칩에 role="checkbox", aria-checked={selected}, keyboard Tab+Space 지원
- 라벨: text-[14px] font-semibold text-[#5A5550] mb-2
- hint 표시: label 우측에 text-[12px] text-[#A09890]

**2. src/components/ui/KeywordWeightSlider.tsx** (per D-05, UI-SPEC #9):
```typescript
interface KeywordWeightSliderProps {
  keywords: { label: string; weight: number }[];
  onChange: (keyword: string, weight: number) => void;
}
```
- 전체 래퍼: label "키워드 가중치" (text-[14px] font-semibold text-[#5A5550] mb-2)
- 키워드 없으면 렌더링하지 않음 (keywords.length === 0이면 null 반환)
- 각 행: flex items-center gap-3, py-1.5
- 키워드 라벨: text-[14px] text-[#D0CAC2], max-w-[120px] truncate
- 슬라이더: input[type=range], min=1 max=5 step=1
  - 트랙: h-[4px], bg-[#4A4440], 채워진 부분 bg-[#B48C50]
  - thumb: w-[16px] h-[16px] rounded-full bg-[#B48C50], shadow `0 0 0 4px rgba(180,140,80,0.2)`
  - CSS로 스타일링: appearance-none + ::-webkit-slider-thumb + ::-moz-range-thumb
- 숫자 표시: text-[12px] font-semibold text-[#B48C50], w-[20px] text-right
- 기본값: 3
- 접근성: aria-label="{keyword} 가중치"
- 스크롤: max-h-[200px] overflow-y-auto (8개 초과 시)
- 슬라이더 CSS는 컴포넌트 내부에 `<style>` 또는 Tailwind @apply로 처리

**3. src/components/ui/AdvancedOptionsToggle.tsx** (per D-04, UI-SPEC #7):
```typescript
interface AdvancedOptionsToggleProps {
  children: React.ReactNode;
}
```
- 내부 useState로 isOpen 관리 (기본: false)
- 트리거 버튼: flex items-center gap-1, text-[14px] font-semibold text-[#A09890], hover:text-[#B48C50], cursor-pointer
- 아이콘: ChevronDown (16px) from lucide-react, 열림 시 rotate-180 transition 200ms
- 콘텐츠 영역: CSS grid-template-rows: 0fr/1fr 패턴으로 부드러운 슬라이드 (기존 RecommendGroup 패턴 참조)
- "고급 옵션" 텍스트 고정

각 파일 500줄 이하 (각각 ~50-100줄 예상).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/ChipSelector.tsx 존재, `export function ChipSelector` 포함
    - ChipSelector.tsx에 role="checkbox" 존재
    - ChipSelector.tsx에 maxSelection prop 사용하는 FIFO 로직 존재 (`.slice(1)` 패턴)
    - ChipSelector.tsx에 bg-[rgba(180,140,80,0.15)] (선택 상태 색상) 존재
    - src/components/ui/KeywordWeightSlider.tsx 존재, `export function KeywordWeightSlider` 포함
    - KeywordWeightSlider.tsx에 input[type="range"] 또는 type="range" 존재
    - KeywordWeightSlider.tsx에 min={1} max={5} 또는 min=1 max=5 존재
    - KeywordWeightSlider.tsx에 aria-label 존재
    - src/components/ui/AdvancedOptionsToggle.tsx 존재, `export function AdvancedOptionsToggle` 포함
    - AdvancedOptionsToggle.tsx에 ChevronDown import 존재
    - AdvancedOptionsToggle.tsx에 "고급 옵션" 텍스트 존재
    - 3개 파일 모두 500줄 이하
    - tsc --noEmit 에러 없음
  </acceptance_criteria>
  <done>3개 UI 프리미티브가 생성되고, UI-SPEC 색상/스페이싱 계약을 준수하며, 접근성 속성이 포함됨</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 전체 통과
- src/types/form.ts에 TabId, AnalysisState, IdentityState, ExpressionState, RationaleData, KeywordWeights 타입 존재
- src/store/useFormStore.ts에 신규 상태와 액션 존재, persist version 3
- 3개 신규 UI 컴포넌트 파일 존재 및 export 확인
</verification>

<success_criteria>
- 모든 신규 타입이 정의되고 tsc 통과
- Zustand 스토어가 신규 필드/액션을 지원하고 persist 마이그레이션 v3 적용
- ChipSelector, KeywordWeightSlider, AdvancedOptionsToggle이 UI-SPEC 계약대로 구현
- 모든 파일 500줄 이하
</success_criteria>

<output>
After completion, create `.planning/phases/08-strategic-naming-logic-intelligence/08-01-SUMMARY.md`
</output>
