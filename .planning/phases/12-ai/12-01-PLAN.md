---
phase: 12-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/gemini.ts
  - CLAUDE.md
autonomous: true
requirements:
  - AI-01
  - AI-02

must_haves:
  truths:
    - "AI 추천 요청 시 gemini-3.1-pro-preview 모델이 사용된다"
    - "CLAUDE.md에 기재된 모델명이 소스 코드의 MODEL_NAME 상수와 일치한다"
    - "기존 추천 기능이 모델 교체 후에도 정상 동작한다 (geminiCriteria.ts의 import 유지)"
  artifacts:
    - path: "src/services/gemini.ts"
      provides: "MODEL_NAME 상수 — AI 모델 단일 소스"
      contains: "gemini-3.1-pro-preview"
    - path: "CLAUDE.md"
      provides: "프로젝트 AI 스택 문서"
      contains: "gemini-3.1-pro-preview"
  key_links:
    - from: "src/services/geminiCriteria.ts"
      to: "src/services/gemini.ts"
      via: "import { MODEL_NAME } from './gemini'"
      pattern: "import.*MODEL_NAME.*from.*gemini"
---

<objective>
AI 모델을 gemini-3-flash-preview에서 gemini-3.1-pro-preview로 전환하고, CLAUDE.md 문서를 실제 코드와 동기화한다.

Purpose: 추천 품질을 향상시키기 위해 GA 출시된 Pro 모델로 업그레이드하고, 문서-코드 불일치를 해소한다.
Output: MODEL_NAME 상수가 변경된 gemini.ts, 모델 ID가 명시된 CLAUDE.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ai/12-RESEARCH.md

<interfaces>
<!-- geminiCriteria.ts가 gemini.ts에서 MODEL_NAME을 import하는 구조 -->
From src/services/gemini.ts:
```typescript
export const MODEL_NAME = 'gemini-3-flash-preview';  // 변경 대상
```

From src/services/geminiCriteria.ts:
```typescript
import { MODEL_NAME } from './gemini';  // 자동 반영 — 수정 불필요
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MODEL_NAME 상수를 gemini-3.1-pro-preview로 변경</name>
  <files>src/services/gemini.ts</files>
  <read_first>
    - src/services/gemini.ts (현재 MODEL_NAME 값 확인)
    - src/services/geminiCriteria.ts (MODEL_NAME import 경로 확인)
  </read_first>
  <action>
    src/services/gemini.ts 3번째 줄의 MODEL_NAME 상수 값을 변경한다:

    변경 전:
    ```typescript
    export const MODEL_NAME = 'gemini-3-flash-preview';
    ```

    변경 후:
    ```typescript
    export const MODEL_NAME = 'gemini-3.1-pro-preview';
    ```

    이것이 이 태스크의 전부다. 다른 파일은 수정하지 않는다.

    주의사항:
    - 모델명 오타 주의: 반드시 `gemini-3.1-pro-preview` (점 포함, 하이픈 포함)
    - geminiCriteria.ts는 `import { MODEL_NAME } from './gemini'`로 가져오므로 자동 반영됨 — 수정 불필요
    - SDK 버전이나 API 호출 방식은 변경하지 않는다 (AI-01 요건: 모델명만 변경)

    변경 후 검증:
    1. `grep -r "gemini-3-flash" src/` 실행하여 구 모델명 잔재가 없는지 확인
    2. `grep "MODEL_NAME" src/services/geminiCriteria.ts` 실행하여 import가 여전히 유효한지 확인
  </action>
  <verify>
    <automated>cd C:/BRAND && grep "gemini-3.1-pro-preview" src/services/gemini.ts && grep -c "gemini-3-flash" src/services/gemini.ts src/services/geminiCriteria.ts | grep ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - src/services/gemini.ts에 `export const MODEL_NAME = 'gemini-3.1-pro-preview';` 문자열이 존재한다
    - src/services/gemini.ts에 `gemini-3-flash-preview` 문자열이 존재하지 않는다
    - src/services/geminiCriteria.ts에 `gemini-3-flash-preview` 하드코딩이 없다 (기존처럼 import만 사용)
    - src/services/geminiCriteria.ts에 `import { MODEL_NAME } from './gemini'` 가 유지된다
  </acceptance_criteria>
  <done>MODEL_NAME이 'gemini-3.1-pro-preview'이고 소스 코드 전체에 구 모델명 'gemini-3-flash-preview'가 없다 (AI-01 충족)</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md AI 스택 테이블에 정확한 모델 ID 명시</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (현재 @google/genai 행의 내용 확인)
    - src/services/gemini.ts (변경된 MODEL_NAME 값 확인 — Task 1 완료 후)
  </read_first>
  <action>
    CLAUDE.md의 AI / RAG 스택 테이블에서 `@google/genai` 행의 "이유" 칼럼을 업데이트한다:

    변경 전:
    ```markdown
    | @google/genai | 1.x | Gemini 3.1 Pro API 공식 SDK | ★★★ |
    ```

    변경 후:
    ```markdown
    | @google/genai | 1.x | Gemini 3.1 Pro API 공식 SDK (모델: gemini-3.1-pro-preview) | ★★★ |
    ```

    이 변경은 CLAUDE.md에서 "Gemini 3.1 Pro API"라는 일반 표기 뒤에 정확한 모델 ID 문자열 `gemini-3.1-pro-preview`를 괄호 안에 추가하여, 코드의 MODEL_NAME 상수값과 문서가 정확히 일치하도록 만든다.

    주의사항:
    - Constraints 섹션의 "Gemini 3.1 Pro API" 표기는 이미 올바르므로 수정하지 않는다
    - AI / RAG 테이블의 @google/genai 행만 수정한다
    - 다른 테이블이나 섹션은 수정하지 않는다
  </action>
  <verify>
    <automated>cd C:/BRAND && grep "gemini-3.1-pro-preview" CLAUDE.md && grep "gemini-3.1-pro-preview" src/services/gemini.ts</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md에 `gemini-3.1-pro-preview` 문자열이 존재한다
    - CLAUDE.md의 @google/genai 행이 `| @google/genai | 1.x | Gemini 3.1 Pro API 공식 SDK (모델: gemini-3.1-pro-preview) | ★★★ |` 이다
    - src/services/gemini.ts의 MODEL_NAME 값과 CLAUDE.md의 모델 ID가 둘 다 `gemini-3.1-pro-preview`로 일치한다
  </acceptance_criteria>
  <done>CLAUDE.md에 정확한 모델 ID가 명시되어 소스 코드의 MODEL_NAME과 문서가 일치한다 (AI-02 충족)</done>
</task>

</tasks>

<verification>
Phase 12 전체 검증:
1. `grep "gemini-3.1-pro-preview" src/services/gemini.ts` — MODEL_NAME 상수에 새 모델명 확인
2. `grep -r "gemini-3-flash" src/` — 구 모델명 잔재 없음 확인 (결과 0건)
3. `grep "gemini-3.1-pro-preview" CLAUDE.md` — 문서에 정확한 모델 ID 존재 확인
4. `grep "MODEL_NAME" src/services/geminiCriteria.ts` — import 경로 정상 유지 확인
5. `npx vite build` — 빌드 성공 확인 (문자열 변경이 TypeScript 오류를 일으키지 않음)
</verification>

<success_criteria>
1. src/services/gemini.ts의 MODEL_NAME이 'gemini-3.1-pro-preview'다
2. 소스 코드 어디에도 'gemini-3-flash-preview' 문자열이 없다
3. CLAUDE.md에 'gemini-3.1-pro-preview' 모델 ID가 명시되어 있다
4. geminiCriteria.ts의 MODEL_NAME import가 정상 유지된다
5. 프로젝트가 정상 빌드된다
</success_criteria>

<output>
After completion, create `.planning/phases/12-ai/12-01-SUMMARY.md`
</output>
