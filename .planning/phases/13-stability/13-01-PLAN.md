---
phase: 13-stability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/gemini.ts
  - src/services/geminiCriteria.ts
  - src/hooks/useRecommend.ts
autonomous: true
requirements:
  - STB-01
  - STB-02
  - STB-03
  - STB-04
must_haves:
  truths:
    - "AI 추천 요청이 30초를 초과하면 자동으로 중단된다"
    - "타임아웃 또는 네트워크 오류 시 최대 2회 자동 재시도된다"
    - "재시도 중 사용자는 로딩 스피너만 보고 재시도 여부를 모른다"
    - "2회 모두 실패 시에만 에러 메시지가 표시된다"
    - "geminiCriteria.ts의 API 호출도 동일한 타임아웃이 적용된다"
  artifacts:
    - path: "src/services/gemini.ts"
      provides: "30초 타임아웃이 적용된 generateBrandNames 함수"
      contains: "AbortSignal.timeout"
    - path: "src/services/geminiCriteria.ts"
      provides: "30초 타임아웃이 적용된 generateForCriterion 함수"
      contains: "AbortSignal.timeout"
    - path: "src/hooks/useRecommend.ts"
      provides: "최대 2회 재시도 로직이 포함된 recommend 함수"
      contains: "retry"
  key_links:
    - from: "src/hooks/useRecommend.ts"
      to: "src/services/gemini.ts"
      via: "generateBrandNames() 호출 시 재시도 래핑"
      pattern: "retry.*generateBrandNames"
    - from: "src/services/gemini.ts"
      to: "@google/genai"
      via: "config.abortSignal로 타임아웃 전달"
      pattern: "abortSignal.*AbortSignal\\.timeout"
---

<objective>
AI 추천 API 호출에 30초 타임아웃과 2회 자동 재시도를 추가하여, API 장애 시 무한 로딩 문제를 해결한다.

Purpose: Gemini API가 응답하지 않을 때 사용자가 무한 로딩에 갇히는 문제를 방지. 사내 앱에서 가장 실질적인 안정성 문제 해결.
Output: 타임아웃 + 재시도가 적용된 gemini.ts, geminiCriteria.ts, useRecommend.ts

Note: STB-02 (에러 메시지 고도화), STB-03 (구조화 로깅), STB-04 (로그아웃 데이터 정리)는 사용자 결정 D-05/D-06/D-07에 의해 이 Phase에서 제외됨. 현재 구현으로 충분하다고 판단됨.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-stability/13-CONTEXT.md

<interfaces>
<!-- @google/genai SDK의 GenerateContentConfig 타입 — abortSignal 지원 확인됨 -->

From node_modules/@google/genai/dist/genai.d.ts:
```typescript
export declare interface GenerateContentConfig {
    httpOptions?: HttpOptions;
    abortSignal?: AbortSignal;  // 타임아웃 취소에 사용
    systemInstruction?: ContentUnion;
    responseMimeType?: string;
    responseSchema?: Schema;
    // ... 기타 필드
}
```

From src/services/gemini.ts (현재 API 호출 패턴):
```typescript
const response = await ai.models.generateContent({
    model: MODEL_NAME,
    contents: [{ role: 'user', parts: [{ text: prompt }] }],
    config: {
      systemInstruction: buildSystemInstruction(),
      responseMimeType: 'application/json',
      responseSchema: hasDocuments ? SCHEMA_DOC_BASED : SCHEMA_SIMPLE,
    },
  });
```

From src/hooks/useRecommend.ts (현재 에러 처리 패턴):
```typescript
try {
  const batch = await generateBrandNames(form, keywordWeights);
  addBatch(batch);
} catch (err) {
  const message = err instanceof Error ? err.message : 'AI 추천에 실패했습니다.';
  setError(message);
} finally {
  setLoading(false);
}
```

From src/store/useFormStore.ts:
```typescript
setLoading: (loading: boolean) => void;
setError: (error: string | null) => void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: gemini.ts와 geminiCriteria.ts에 30초 AbortSignal 타임아웃 추가</name>
  <files>src/services/gemini.ts, src/services/geminiCriteria.ts</files>
  <read_first>
    - src/services/gemini.ts (현재 generateBrandNames 함수 전체)
    - src/services/geminiCriteria.ts (현재 generateForCriterion 함수 전체)
  </read_first>
  <action>
두 파일의 `ai.models.generateContent()` 호출에 `AbortSignal.timeout(30_000)`을 추가한다. @google/genai SDK의 GenerateContentConfig가 `abortSignal` 필드를 네이티브 지원하므로 Promise.race 없이 깔끔하게 구현 가능.

**gemini.ts 수정 (214행 부근):**

파일 상단에 타임아웃 상수 추가:
```typescript
const API_TIMEOUT_MS = 30_000; // 30초 타임아웃 (per D-01)
```

`generateBrandNames` 함수 내 `ai.models.generateContent()` 호출의 config 객체에 abortSignal 추가:
```typescript
const response = await ai.models.generateContent({
    model: MODEL_NAME,
    contents: [{ role: 'user', parts: [{ text: prompt }] }],
    config: {
      abortSignal: AbortSignal.timeout(API_TIMEOUT_MS),
      systemInstruction: buildSystemInstruction(),
      responseMimeType: 'application/json',
      responseSchema: hasDocuments ? SCHEMA_DOC_BASED : SCHEMA_SIMPLE,
    },
  });
```

`API_TIMEOUT_MS` 상수를 export하여 다른 파일에서도 사용 가능하게:
```typescript
export const API_TIMEOUT_MS = 30_000;
```

**geminiCriteria.ts 수정 (53행 부근):**

import에 `API_TIMEOUT_MS` 추가:
```typescript
import { MODEL_NAME, API_TIMEOUT_MS } from './gemini';
```

`generateForCriterion` 함수 내 `ai.models.generateContent()` 호출의 config에 abortSignal 추가:
```typescript
const response = await ai.models.generateContent({
    model: MODEL_NAME,
    contents: [{ role: 'user', parts: [{ text: prompt }] }],
    config: {
      abortSignal: AbortSignal.timeout(API_TIMEOUT_MS),
      responseMimeType: 'application/json',
      responseSchema: { ... },
    },
  });
```
  </action>
  <verify>
    <automated>cd /c/BRAND && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - gemini.ts 에 `export const API_TIMEOUT_MS = 30_000` 가 존재
    - gemini.ts 의 generateContent config 에 `abortSignal: AbortSignal.timeout(API_TIMEOUT_MS)` 가 존재
    - geminiCriteria.ts 에 `import { MODEL_NAME, API_TIMEOUT_MS } from './gemini'` 가 존재
    - geminiCriteria.ts 의 generateContent config 에 `abortSignal: AbortSignal.timeout(API_TIMEOUT_MS)` 가 존재
    - `npx vite build` 가 에러 없이 완료
    - 두 파일 모두 500줄 이하
  </acceptance_criteria>
  <done>gemini.ts와 geminiCriteria.ts의 모든 Gemini API 호출에 30초 타임아웃이 적용됨</done>
</task>

<task type="auto">
  <name>Task 2: useRecommend.ts에 2회 자동 재시도 로직 추가</name>
  <files>src/hooks/useRecommend.ts</files>
  <read_first>
    - src/hooks/useRecommend.ts (현재 recommend 함수 전체)
    - src/services/gemini.ts (Task 1 수정 후 상태 확인)
  </read_first>
  <action>
useRecommend.ts의 `recommend` 함수에 재시도 로직을 추가한다. 재시도 대상: 타임아웃 에러(AbortError)와 네트워크 오류(TypeError). 사용자에게는 재시도 사실을 알리지 않고 로딩 스피너만 계속 표시 (per D-03).

**재시도 헬퍼 함수를 useRecommend.ts 파일 상단(hook 외부)에 추가:**

```typescript
const MAX_RETRIES = 2;
const RETRY_DELAY_MS = 1_000; // 1초 대기 후 재시도

function isRetryableError(err: unknown): boolean {
  if (err instanceof DOMException && err.name === 'TimeoutError') return true;
  if (err instanceof DOMException && err.name === 'AbortError') return true;
  if (err instanceof TypeError) return true; // 네트워크 오류
  return false;
}

async function withRetry<T>(fn: () => Promise<T>): Promise<T> {
  let lastError: unknown;
  for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
    try {
      return await fn();
    } catch (err) {
      lastError = err;
      if (attempt < MAX_RETRIES && isRetryableError(err)) {
        await new Promise((r) => setTimeout(r, RETRY_DELAY_MS));
        continue;
      }
      throw err;
    }
  }
  throw lastError;
}
```

**recommend 함수 내부의 generateBrandNames 호출을 withRetry로 래핑:**

기존:
```typescript
const batch = await generateBrandNames(form, keywordWeights);
```

변경:
```typescript
const batch = await withRetry(() => generateBrandNames(form, keywordWeights));
```

**에러 메시지 분기 추가 (catch 블록):**

기존 catch 블록을 타임아웃 에러에 대해 더 명확한 메시지를 표시하도록 수정:
```typescript
catch (err) {
  let message: string;
  if (err instanceof DOMException && (err.name === 'TimeoutError' || err.name === 'AbortError')) {
    message = 'AI 응답 시간이 초과되었습니다. 잠시 후 다시 시도해 주세요.';
  } else if (err instanceof TypeError) {
    message = '네트워크 연결을 확인해 주세요.';
  } else {
    message = err instanceof Error ? err.message : 'AI 추천에 실패했습니다.';
  }
  setError(message);
}
```

이 에러 메시지는 2회 재시도가 모두 실패한 뒤에만 표시됨 (per D-04).
  </action>
  <verify>
    <automated>cd /c/BRAND && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - useRecommend.ts에 `const MAX_RETRIES = 2` 가 존재
    - useRecommend.ts에 `function isRetryableError` 가 존재
    - useRecommend.ts에 `async function withRetry` 가 존재
    - useRecommend.ts에서 `withRetry(() => generateBrandNames(` 패턴이 존재
    - useRecommend.ts catch 블록에 `'AI 응답 시간이 초과되었습니다'` 메시지가 존재
    - useRecommend.ts catch 블록에 `'네트워크 연결을 확인해 주세요'` 메시지가 존재
    - `npx vite build` 가 에러 없이 완료
    - useRecommend.ts가 500줄 이하
  </acceptance_criteria>
  <done>
    - generateBrandNames 호출이 withRetry로 래핑되어 최대 2회 자동 재시도
    - 재시도 중 사용자는 로딩 스피너만 봄 (조용한 재시도)
    - 2회 모두 실패 후에만 타임아웃/네트워크/기타 에러에 맞는 메시지 표시
  </done>
</task>

</tasks>

<verification>
1. `npx vite build` 성공 (타입 에러 없음)
2. gemini.ts에 `AbortSignal.timeout(API_TIMEOUT_MS)` 존재
3. geminiCriteria.ts에 `AbortSignal.timeout(API_TIMEOUT_MS)` 존재
4. useRecommend.ts에 `withRetry` 래핑 존재
5. useRecommend.ts에 `MAX_RETRIES = 2` 존재
6. 모든 수정 파일이 500줄 이하
</verification>

<success_criteria>
- AI 추천 요청이 30초 타임아웃으로 보호됨 (무한 로딩 불가)
- 타임아웃/네트워크 오류 시 최대 2회 조용한 재시도 수행
- 재시도 모두 실패 시에만 사용자에게 에러 메시지 표시
- 기존 정상 동작 (API가 30초 이내 응답) 에는 영향 없음
- STB-02/03/04는 사용자 결정 D-05/D-06/D-07에 의해 의도적으로 제외됨
</success_criteria>

<output>
After completion, create `.planning/phases/13-stability/13-01-SUMMARY.md`
</output>
