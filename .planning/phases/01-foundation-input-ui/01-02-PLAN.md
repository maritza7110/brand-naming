---
phase: 01-foundation-input-ui
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/layout/AppLayout.tsx
  - src/components/layout/InputPanel.tsx
  - src/components/layout/RecommendPanel.tsx
  - src/components/recommend/EmptyState.tsx
  - src/components/sections/StoreBasicSection.tsx
  - src/components/sections/BrandVisionSection.tsx
  - src/components/sections/ProductSection.tsx
  - src/components/sections/PersonaSection.tsx
  - src/App.tsx
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-03
  - INPUT-04
  - INPUT-05
  - INPUT-06
  - OWNER-01
  - OWNER-02
  - OWNER-03
  - PERSONA-01
  - PERSONA-02
  - PERSONA-03
  - PERSONA-04
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
must_haves:
  truths:
    - "사용자가 웹앱을 열면 왼쪽 70% / 오른쪽 30% 분할 레이아웃이 표시된다"
    - "왼쪽 패널에서 기본정보 6개 항목(업종, 위치, 규모, 상품, 가격대, 타겟고객)을 입력할 수 있다"
    - "사장님 정보 3개 항목(비전, 목표, 스토리)을 입력할 수 있다"
    - "페르소나 관련 9개 항목(특장점, 핵심가치, 철학, 슬로건, 미션, 스토리, 핵심가치, 차별화, 톤앤매너)을 입력할 수 있다"
    - "왼쪽 패널이 세로 스크롤로 동작한다"
    - "오른쪽 패널에 빈 상태 안내 메시지가 표시된다"
    - "각 섹션 하단에 추천 받기 버튼이 있고 최소 1개 항목 입력 시 활성화된다"
  artifacts:
    - path: "src/components/layout/AppLayout.tsx"
      provides: "70/30 grid 레이아웃"
      contains: "grid-cols-[7fr_3fr]"
    - path: "src/components/layout/InputPanel.tsx"
      provides: "왼쪽 스크롤 패널"
      contains: "overflow-y-auto"
    - path: "src/components/layout/RecommendPanel.tsx"
      provides: "오른쪽 스티키 패널"
      contains: "sticky"
    - path: "src/components/recommend/EmptyState.tsx"
      provides: "추천 대기 빈 상태"
      contains: "Sparkles"
    - path: "src/components/sections/StoreBasicSection.tsx"
      provides: "섹션 1: 6 fields (INPUT-01~06)"
      exports: ["StoreBasicSection"]
    - path: "src/components/sections/BrandVisionSection.tsx"
      provides: "섹션 2: 3 fields (OWNER-01~03)"
      exports: ["BrandVisionSection"]
    - path: "src/components/sections/ProductSection.tsx"
      provides: "섹션 3: 2 fields (PERSONA-04 일부)"
      exports: ["ProductSection"]
    - path: "src/components/sections/PersonaSection.tsx"
      provides: "섹션 5: 7 fields (PERSONA-01~04)"
      exports: ["PersonaSection"]
    - path: "src/App.tsx"
      provides: "전체 페이지 조립"
      contains: "AppLayout"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/layout/AppLayout.tsx"
      via: "import and render"
      pattern: "import.*AppLayout"
    - from: "src/components/sections/StoreBasicSection.tsx"
      to: "src/store/useFormStore.ts"
      via: "useFormStore hook"
      pattern: "useFormStore"
    - from: "src/components/sections/PersonaSection.tsx"
      to: "src/store/useFormStore.ts"
      via: "useFormStore hook"
      pattern: "useFormStore"
    - from: "src/components/sections/*.tsx"
      to: "src/components/ui/*.tsx"
      via: "TextField, TextArea, Dropdown imports"
      pattern: "import.*from.*ui/"
    - from: "src/components/recommend/EmptyState.tsx"
      to: "lucide-react"
      via: "Sparkles icon import"
      pattern: "import.*Sparkles.*from.*lucide-react"
---

<objective>
레이아웃 셸 + 4개 입력 섹션 + 빈 상태 추천 패널 + App.tsx 통합 조립. Phase 1의 모든 사용자 대면 기능을 완성한다.

Purpose: 사용자가 브라우저에서 열었을 때 18개 입력 필드와 70/30 레이아웃이 완전히 동작하는 상태를 만든다.
Output: 완전한 입력 UI — 모든 필드 입력 가능, 추천 패널 대기 상태
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-input-ui/01-CONTEXT.md
@.planning/phases/01-foundation-input-ui/01-RESEARCH.md
@.planning/phases/01-foundation-input-ui/01-UI-SPEC.md
@.planning/phases/01-foundation-input-ui/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 핵심 인터페이스. Executor가 직접 사용할 것. -->

From src/types/form.ts:
```typescript
export interface StoreBasicState {
  category: string;        // INPUT-01
  location: string;        // INPUT-02
  scale: string;           // INPUT-03
  mainProduct: string;     // INPUT-04
  priceRange: string;      // INPUT-05
  targetCustomer: string;  // INPUT-06
}

export interface BrandVisionState {
  ceoVision: string;       // OWNER-01
  longTermGoal: string;    // OWNER-02
  personalStory: string;   // OWNER-03
}

export interface ProductState {
  uniqueStrength: string;      // PERSONA-04
  valueProposition: string;    // PERSONA-04
}

export interface PersonaState {
  philosophy: string;      // PERSONA-01
  slogan: string;          // PERSONA-02
  mission: string;         // PERSONA-03
  brandStory: string;      // PERSONA-04
  coreValues: string;      // PERSONA-04
  differentiation: string; // PERSONA-04
  toneAndManner: string;   // PERSONA-04
}
```

From src/store/useFormStore.ts:
```typescript
export const useFormStore = create<AppState & FormActions>(...);
// Actions: updateStoreBasic(field, value), updateBrandVision(field, value),
//          updateProduct(field, value), updatePersona(field, value), resetAll()
```

From src/components/ui/:
```typescript
export function TextField({ label, value, onChange, placeholder, disabled, id }: Props)
export function TextArea({ label, value, onChange, placeholder, rows, disabled, id }: Props)
export function Dropdown({ label, value, onChange, options, placeholder, disabled, id }: Props)
export function SectionHeader({ title, description }: Props)
export function RecommendButton({ onClick, disabled, loading }: Props)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 레이아웃 컴포넌트 (AppLayout, InputPanel, RecommendPanel) + EmptyState</name>
  <files>
    src/components/layout/AppLayout.tsx,
    src/components/layout/InputPanel.tsx,
    src/components/layout/RecommendPanel.tsx,
    src/components/recommend/EmptyState.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-input-ui/01-UI-SPEC.md
    src/index.css
    src/types/form.ts
  </read_first>
  <action>
    UI-SPEC의 Layout Contract를 정확히 구현.

    1. src/components/layout/AppLayout.tsx:
       - Props: `{ children: React.ReactNode }` (사용하지 않음 — 내부에서 직접 InputPanel/RecommendPanel 배치)
       - 실제로는 Props 없이, left/right children을 받는 구조:
         `{ left: React.ReactNode; right: React.ReactNode }`
       - 최외곽: `<div className="grid grid-cols-[7fr_3fr] h-screen min-w-[1024px]">`
         (LAYOUT-01: 70/30 비율, D-10: 최소 1024px)
       - left 영역: InputPanel로 감싸서 렌더
       - right 영역: RecommendPanel로 감싸서 렌더
       - named export: `export function AppLayout`

    2. src/components/layout/InputPanel.tsx:
       - Props: `{ children: React.ReactNode }`
       - 컨테이너: `<main className="overflow-y-auto h-screen scroll-smooth">`
         (LAYOUT-02: 세로 스크롤, UI-SPEC smooth scroll)
       - 내부 max-width 컨테이너: `<div className="max-w-3xl mx-auto px-8 py-12">`
         (패널 패딩 32px = p-8, 상하 48px = py-12)
       - 페이지 제목 렌더:
         - h1: `<h1 className="text-[28px] font-semibold tracking-[-0.02em] text-gray-900">브랜드 네이밍</h1>`
         - subtitle: `<p className="mt-2 text-sm text-gray-500">페르소나 항목을 채워가며 AI 브랜드명을 추천받으세요</p>`
         (UI-SPEC Copywriting Contract: Page title, Page subtitle)
       - children 렌더: `<div className="mt-12">{children}</div>` (제목 아래 섹션 시작)
       - named export: `export function InputPanel`

    3. src/components/layout/RecommendPanel.tsx:
       - Props: `{ children: React.ReactNode }`
       - 컨테이너: `<aside className="sticky top-0 h-screen overflow-y-auto border-l border-gray-200 bg-white">`
         (D-08: 스티키 고정, 내부 독립 스크롤, UI-SPEC right panel border)
       - 헤더: `<div className="sticky top-0 z-10 px-8 py-6 backdrop-blur-xl bg-white/80 border-b border-gray-200">`
         (UI-SPEC: backdrop-blur-xl bg-white/80)
         - 헤더 제목: `<h2 className="text-lg font-semibold text-gray-900">브랜드명 추천</h2>`
       - 본문: `<div className="px-8 py-6">{children}</div>`
       - named export: `export function RecommendPanel`

    4. src/components/recommend/EmptyState.tsx:
       - Props: 없음
       - Lucide `Sparkles` 아이콘 import (size 48, `text-blue-300`) — UI-SPEC Visual Focal Point
       - 중앙 정렬 레이아웃: `flex flex-col items-center justify-center text-center` + 충분한 높이 `min-h-[400px]`
       - heading: `<h3 className="mt-6 text-lg font-semibold text-gray-900">브랜드명 추천 대기 중</h3>`
         (UI-SPEC Copywriting: Empty state heading)
       - body: `<p className="mt-2 text-sm text-gray-500 max-w-[240px]">왼쪽 항목을 입력하고 "추천 받기" 버튼을 누르면 AI가 브랜드명을 추천합니다</p>`
         (UI-SPEC Copywriting: Empty state body)
       - named export: `export function EmptyState`
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
    TypeScript 컴파일 에러 없음.
  </verify>
  <acceptance_criteria>
    - src/components/layout/AppLayout.tsx에 `export function AppLayout` 포함
    - src/components/layout/AppLayout.tsx에 `grid-cols-[7fr_3fr]` 포함 (LAYOUT-01)
    - src/components/layout/AppLayout.tsx에 `min-w-[1024px]` 포함 (D-10)
    - src/components/layout/InputPanel.tsx에 `export function InputPanel` 포함
    - src/components/layout/InputPanel.tsx에 `overflow-y-auto` 포함 (LAYOUT-02)
    - src/components/layout/InputPanel.tsx에 `브랜드 네이밍` 포함 (페이지 제목)
    - src/components/layout/InputPanel.tsx에 `scroll-smooth` 포함
    - src/components/layout/RecommendPanel.tsx에 `export function RecommendPanel` 포함
    - src/components/layout/RecommendPanel.tsx에 `sticky top-0` 포함 (D-08)
    - src/components/layout/RecommendPanel.tsx에 `backdrop-blur-xl` 포함 (D-13)
    - src/components/layout/RecommendPanel.tsx에 `border-l` 포함
    - src/components/recommend/EmptyState.tsx에 `export function EmptyState` 포함
    - src/components/recommend/EmptyState.tsx에 `Sparkles` 포함 (Lucide 아이콘)
    - src/components/recommend/EmptyState.tsx에 `브랜드명 추천 대기 중` 포함
    - src/components/recommend/EmptyState.tsx에 `text-blue-300` 포함
  </acceptance_criteria>
  <done>70/30 레이아웃 셸이 완성되고, 왼쪽 패널은 세로 스크롤, 오른쪽 패널은 스티키 고정 + backdrop-blur 헤더 + EmptyState가 표시됨</done>
</task>

<task type="auto">
  <name>Task 2: 4개 섹션 컴포넌트 + App.tsx 통합 조립</name>
  <files>
    src/components/sections/StoreBasicSection.tsx,
    src/components/sections/BrandVisionSection.tsx,
    src/components/sections/ProductSection.tsx,
    src/components/sections/PersonaSection.tsx,
    src/App.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-input-ui/01-UI-SPEC.md
    .planning/phases/01-foundation-input-ui/01-RESEARCH.md
    src/types/form.ts
    src/store/useFormStore.ts
    src/components/ui/TextField.tsx
    src/components/ui/TextArea.tsx
    src/components/ui/Dropdown.tsx
    src/components/ui/SectionHeader.tsx
    src/components/ui/RecommendButton.tsx
    src/components/layout/AppLayout.tsx
    src/components/layout/InputPanel.tsx
    src/components/layout/RecommendPanel.tsx
    src/components/recommend/EmptyState.tsx
  </read_first>
  <action>
    4개 섹션 컴포넌트는 모두 동일한 패턴:
    (1) useFormStore에서 해당 섹션 상태 + update 함수 가져옴
    (2) SectionHeader로 섹션 제목 렌더
    (3) 필드들을 space-y-6 간격으로 렌더 (UI-SPEC field-to-field gap 24px)
    (4) RecommendButton을 하단에 배치 (D-15), disabled 조건은 해당 섹션 필드 중 하나라도 값이 있으면 false (D-16)

    **1. src/components/sections/StoreBasicSection.tsx** (INPUT-01 ~ INPUT-06):
       - `useFormStore`에서 `storeBasic` + `updateStoreBasic` 가져옴
       - SectionHeader: title="매장 기본/환경" (UI-SPEC Section 1 header)
       - 필드 순서:
         1. Dropdown: label="업종/카테고리", value=storeBasic.category, onChange=(v)=>updateStoreBasic('category',v), options=["음식점/레스토랑","카페/베이커리","주점/바","소매/마트","패션/의류","뷰티/미용","건강/피트니스","교육/학원","IT/테크","디자인/크리에이티브","컨설팅/전문서비스","숙박/펜션","문화/엔터테인먼트","기타"], placeholder="업종을 선택하세요"
         2. TextField: label="위치/지역", value=storeBasic.location, onChange=(v)=>updateStoreBasic('location',v), placeholder="예: 서울 마포구 연남동"
         3. Dropdown: label="매장 규모", value=storeBasic.scale, onChange=(v)=>updateStoreBasic('scale',v), options=["1인 창업","소형 (5인 미만)","중형 (5~20인)","대형 (20인 이상)","프랜차이즈"], placeholder="규모를 선택하세요"
         4. TextArea: label="주력 상품/서비스", value=storeBasic.mainProduct, onChange=(v)=>updateStoreBasic('mainProduct',v), placeholder="예: 수제 드립커피, 시그니처 디저트, 브런치 메뉴", rows=3
         5. Dropdown: label="가격대", value=storeBasic.priceRange, onChange=(v)=>updateStoreBasic('priceRange',v), options=["저가","중저가","중가","중고가","고가","프리미엄"], placeholder="가격대를 선택하세요"
         6. TextArea: label="타겟 고객", value=storeBasic.targetCustomer, onChange=(v)=>updateStoreBasic('targetCustomer',v), placeholder="예: 20-30대 직장인, 감성 카페를 즐기는 MZ세대", rows=3
       - RecommendButton: disabled = storeBasic의 모든 값이 빈 문자열일 때 true
         `const hasInput = Object.values(storeBasic).some(v => v.trim() !== '')`
         `<RecommendButton disabled={!hasInput} />`

    **2. src/components/sections/BrandVisionSection.tsx** (OWNER-01 ~ OWNER-03):
       - `useFormStore`에서 `brandVision` + `updateBrandVision` 가져옴
       - SectionHeader: title="브랜드 정체성 및 비전" (UI-SPEC Section 2 header)
       - 필드 순서:
         1. TextArea: label="CEO 비전/꿈", value=brandVision.ceoVision, onChange=(v)=>updateBrandVision('ceoVision',v), placeholder="예: 동네에서 가장 사랑받는 카페를 만들고 싶습니다", rows=4
         2. TextArea: label="5년/10년 목표", value=brandVision.longTermGoal, onChange=(v)=>updateBrandVision('longTermGoal',v), placeholder="예: 3년 내 2호점, 자체 원두 로스팅 브랜드 런칭", rows=4
         3. TextArea: label="개인 스토리/동기", value=brandVision.personalStory, onChange=(v)=>updateBrandVision('personalStory',v), placeholder="예: 10년간 바리스타로 일하며 나만의 카페를 꿈꿨습니다", rows=4
       - RecommendButton: disabled = brandVision의 모든 값이 빈 문자열일 때 true

    **3. src/components/sections/ProductSection.tsx** (PERSONA-04 일부):
       - `useFormStore`에서 `product` + `updateProduct` 가져옴
       - SectionHeader: title="제품/서비스 핵심" (UI-SPEC Section 3 header)
       - 필드 순서:
         1. TextArea: label="제품/서비스 특장점", value=product.uniqueStrength, onChange=(v)=>updateProduct('uniqueStrength',v), placeholder="예: 직접 로스팅한 싱글오리진 원두, 매일 아침 굽는 수제 빵", rows=4
         2. TextArea: label="고객에게 주는 핵심 가치", value=product.valueProposition, onChange=(v)=>updateProduct('valueProposition',v), placeholder="예: 바쁜 일상 속 여유로운 한 잔의 시간", rows=4
       - RecommendButton: disabled = product의 모든 값이 빈 문자열일 때 true

    **4. src/components/sections/PersonaSection.tsx** (PERSONA-01 ~ PERSONA-04):
       - `useFormStore`에서 `persona` + `updatePersona` 가져옴
       - SectionHeader: title="브랜드 페르소나" (UI-SPEC Section 5 header)
       - 필드 순서:
         1. TextArea: label="브랜드 철학", value=persona.philosophy, onChange=(v)=>updatePersona('philosophy',v), placeholder="예: 좋은 원두, 정직한 한 잔", rows=3
         2. TextArea: label="슬로건", value=persona.slogan, onChange=(v)=>updatePersona('slogan',v), placeholder="예: 하루의 시작, 당신만의 한 잔", rows=3
         3. TextArea: label="미션", value=persona.mission, onChange=(v)=>updatePersona('mission',v), placeholder="예: 누구나 부담 없이 좋은 커피를 즐길 수 있는 공간", rows=3
         4. TextArea: label="브랜드 스토리", value=persona.brandStory, onChange=(v)=>updatePersona('brandStory',v), placeholder="예: 강릉 할머니댁 뒷마당에서 커피 나무를 키우던 기억에서 시작했습니다", rows=4
         5. TextArea: label="핵심 가치", value=persona.coreValues, onChange=(v)=>updatePersona('coreValues',v), placeholder="예: 진정성, 품질, 따뜻한 환대", rows=3
         6. TextArea: label="차별화 포인트", value=persona.differentiation, onChange=(v)=>updatePersona('differentiation',v), placeholder="예: 산지 직거래 원두, 핸드드립 전문, 아늑한 한옥 인테리어", rows=3
         7. TextArea: label="브랜드 톤앤매너", value=persona.toneAndManner, onChange=(v)=>updatePersona('toneAndManner',v), placeholder="예: 따뜻하고 친근하지만 세련된, 정갈한 톤", rows=3
       - RecommendButton: disabled = persona의 모든 값이 빈 문자열일 때 true

    **섹션 간 구분:**
    각 섹션 컴포넌트의 최외곽을 `<section className="py-12">` 로 감싸되, 첫 번째 섹션은 `pt-0`으로 처리 (또는 App.tsx에서 space-y로 관리).
    섹션 사이에 그라데이션 구분선은 SectionHeader 내부에 이미 포함.

    **5. src/App.tsx:**
       - import: AppLayout, InputPanel, RecommendPanel, EmptyState, 4개 섹션 컴포넌트
       - 구조:
         ```tsx
         function App() {
           return (
             <AppLayout
               left={
                 <InputPanel>
                   <StoreBasicSection />
                   <BrandVisionSection />
                   <ProductSection />
                   <PersonaSection />
                 </InputPanel>
               }
               right={
                 <RecommendPanel>
                   <EmptyState />
                 </RecommendPanel>
               }
             />
           );
         }
         export default App;
         ```
       - Vite 기본 생성된 코드 완전히 교체
       - 추후 Phase 2에서 EmptyState 대신 추천 카드 리스트로 교체 예정
  </action>
  <verify>
    <automated>cd C:/BRAND && npm run build 2>&1 | tail -10</automated>
    빌드 성공 확인. 또한 `npx tsc --noEmit` 에러 없음 확인.
  </verify>
  <acceptance_criteria>
    - src/components/sections/StoreBasicSection.tsx에 `export function StoreBasicSection` 포함
    - src/components/sections/StoreBasicSection.tsx에 `useFormStore` 포함
    - src/components/sections/StoreBasicSection.tsx에 `updateStoreBasic` 포함
    - src/components/sections/StoreBasicSection.tsx에 `매장 기본/환경` 포함 (섹션 제목)
    - src/components/sections/StoreBasicSection.tsx에 `업종을 선택하세요` 포함 (INPUT-01 placeholder)
    - src/components/sections/StoreBasicSection.tsx에 `음식점/레스토랑` 포함 (INPUT-01 드롭다운 옵션)
    - src/components/sections/StoreBasicSection.tsx에 `RecommendButton` 포함 (D-15)
    - src/components/sections/BrandVisionSection.tsx에 `export function BrandVisionSection` 포함
    - src/components/sections/BrandVisionSection.tsx에 `브랜드 정체성 및 비전` 포함
    - src/components/sections/BrandVisionSection.tsx에 `updateBrandVision` 포함
    - src/components/sections/ProductSection.tsx에 `export function ProductSection` 포함
    - src/components/sections/ProductSection.tsx에 `제품/서비스 핵심` 포함
    - src/components/sections/PersonaSection.tsx에 `export function PersonaSection` 포함
    - src/components/sections/PersonaSection.tsx에 `브랜드 페르소나` 포함
    - src/components/sections/PersonaSection.tsx에 `브랜드 톤앤매너` 포함 (PERSONA-04 마지막 필드)
    - src/App.tsx에 `AppLayout` 포함
    - src/App.tsx에 `InputPanel` 포함
    - src/App.tsx에 `RecommendPanel` 포함
    - src/App.tsx에 `EmptyState` 포함
    - src/App.tsx에 `StoreBasicSection` 포함
    - src/App.tsx에 `PersonaSection` 포함
    - `npm run build` 명령이 exit code 0으로 완료
  </acceptance_criteria>
  <done>4개 섹션의 18개 입력 필드가 Zustand 스토어와 연결되어 동작하고, 각 섹션 하단에 추천 버튼이 표시되며, App.tsx가 전체를 70/30 레이아웃으로 조립하여 완전한 입력 UI를 제공</done>
</task>

</tasks>

<verification>
1. `npm run build` 성공 (zero errors)
2. `npx tsc --noEmit` 성공 (타입 에러 없음)
3. 브라우저에서 http://localhost:5173 접속 시:
   - 70/30 분할 레이아웃 표시
   - 왼쪽 패널: 4개 섹션, 18개 필드 표시
   - 오른쪽 패널: "브랜드명 추천 대기 중" 빈 상태
   - 왼쪽 패널 세로 스크롤 동작
   - 오른쪽 패널 스티키 고정
4. 각 입력 필드에 값 입력 시 Zustand 스토어에 반영
5. 각 섹션 1개 이상 입력 시 추천 받기 버튼 활성화
</verification>

<success_criteria>
- 18개 입력 필드 (dropdown 3 + text 1 + textarea 14)가 모두 동작
- 70/30 레이아웃 (grid-cols-[7fr_3fr])이 적용됨
- 왼쪽 패널 세로 스크롤 (overflow-y-auto)
- 오른쪽 패널 스티키 (sticky top-0) + backdrop-blur 헤더
- 빈 상태 (Sparkles 아이콘 + 안내 메시지) 표시
- 각 섹션에 추천 받기 버튼 존재 (D-15)
- 빈 섹션에서 추천 버튼 disabled (D-16)
- 모든 placeholder 예시문이 UI-SPEC과 일치
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-input-ui/01-02-SUMMARY.md`
</output>
