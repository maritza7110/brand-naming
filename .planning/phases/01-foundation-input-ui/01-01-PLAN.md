---
phase: 01-foundation-input-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - index.html
  - src/main.tsx
  - src/index.css
  - src/types/form.ts
  - src/store/useFormStore.ts
  - src/components/ui/TextField.tsx
  - src/components/ui/TextArea.tsx
  - src/components/ui/Dropdown.tsx
  - src/components/ui/SectionHeader.tsx
  - src/components/ui/RecommendButton.tsx
autonomous: true
requirements:
  - LAYOUT-04
must_haves:
  truths:
    - "Vite dev server가 에러 없이 실행된다"
    - "Pretendard 폰트가 로드되어 텍스트에 적용된다"
    - "Tailwind CSS v4 유틸리티 클래스가 정상 동작한다"
    - "Zustand 스토어가 18개 필드의 상태를 관리한다"
    - "5개 공용 UI 컴포넌트가 export되어 사용 가능하다"
  artifacts:
    - path: "src/types/form.ts"
      provides: "FormState 타입 정의 (18개 필드)"
      exports: ["FormState", "StoreBasicState", "BrandVisionState", "ProductState", "PersonaState"]
    - path: "src/store/useFormStore.ts"
      provides: "Zustand 폼 상태 관리"
      exports: ["useFormStore"]
    - path: "src/components/ui/TextField.tsx"
      provides: "한 줄 텍스트 입력 컴포넌트"
      exports: ["TextField"]
    - path: "src/components/ui/TextArea.tsx"
      provides: "여러 줄 텍스트 입력 컴포넌트"
      exports: ["TextArea"]
    - path: "src/components/ui/Dropdown.tsx"
      provides: "선택 드롭다운 컴포넌트"
      exports: ["Dropdown"]
    - path: "src/components/ui/SectionHeader.tsx"
      provides: "섹션 구분 헤더"
      exports: ["SectionHeader"]
    - path: "src/components/ui/RecommendButton.tsx"
      provides: "추천 받기 버튼"
      exports: ["RecommendButton"]
    - path: "src/index.css"
      provides: "Tailwind v4 + Pretendard + 커스텀 테마"
      contains: "@import \"tailwindcss\""
  key_links:
    - from: "src/store/useFormStore.ts"
      to: "src/types/form.ts"
      via: "FormState 타입 import"
      pattern: "import.*FormState.*from.*types/form"
    - from: "src/components/ui/*.tsx"
      to: "src/index.css"
      via: "Tailwind 유틸리티 클래스"
      pattern: "className="
---

<objective>
Phase 1 프로젝트 기반 구축: Vite + React 19 + TypeScript + Tailwind CSS v4 프로젝트 스캐폴딩, Pretendard 폰트 설정, 디자인 시스템 테마 CSS, 타입 정의, Zustand 스토어, 5개 공용 UI 컴포넌트 생성.

Purpose: 모든 후속 작업(레이아웃, 섹션 컴포넌트)이 의존하는 기반 인프라를 한 번에 구축한다.
Output: 실행 가능한 Vite dev server + 타입/스토어/UI 컴포넌트 라이브러리
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-input-ui/01-CONTEXT.md
@.planning/phases/01-foundation-input-ui/01-RESEARCH.md
@.planning/phases/01-foundation-input-ui/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vite + React 19 + Tailwind CSS v4 프로젝트 스캐폴딩 및 디자인 시스템 테마</name>
  <files>
    package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json,
    index.html, src/main.tsx, src/index.css
  </files>
  <read_first>
    .planning/phases/01-foundation-input-ui/01-UI-SPEC.md
    .planning/phases/01-foundation-input-ui/01-RESEARCH.md
  </read_first>
  <action>
    1. Vite 프로젝트 생성:
       ```
       npx create-vite@latest . --template react-ts
       ```
       (현재 디렉토리에 생성. 기존 .planning/ 폴더 보존 확인)

    2. 패키지 설치:
       ```
       npm install zustand lucide-react
       npm install -D tailwindcss@4 @tailwindcss/vite
       ```

    3. vite.config.ts에 Tailwind 플러그인 추가:
       ```typescript
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'
       import tailwindcss from '@tailwindcss/vite'

       export default defineConfig({
         plugins: [react(), tailwindcss()],
       })
       ```

    4. index.html에 Pretendard 폰트 CDN 추가 (head 내부):
       ```html
       <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/variable/pretendardvariable-dynamic-subset.min.css" />
       ```
       또한 title을 "브랜드 네이밍"으로 변경, lang="ko" 설정.

    5. src/index.css 작성 (Tailwind v4 + 커스텀 테마):
       ```css
       @import "tailwindcss";

       @theme {
         --font-sans: 'Pretendard Variable', Pretendard, -apple-system, BlinkMacSystemFont, system-ui, sans-serif;

         --color-primary: #2563EB;
         --color-primary-hover: #1D4ED8;
         --color-primary-light: #EFF6FF;
         --color-primary-muted: #93C5FD;
         --color-background: #FAFBFC;
         --color-surface: #FFFFFF;
         --color-border: #E5E7EB;
         --color-text-primary: #111827;
         --color-text-secondary: #6B7280;
         --color-text-muted: #9CA3AF;
         --color-destructive: #EF4444;
       }

       html {
         font-family: var(--font-sans);
         background-color: var(--color-background);
         color: var(--color-text-primary);
       }
       ```
       UI-SPEC의 Color 섹션, Typography 섹션, Spacing Scale 값을 정확히 반영.

    6. src/main.tsx를 정리 — App 컴포넌트 import + StrictMode 렌더, Vite 기본 CSS import를 './index.css'로 교체.

    7. Vite 기본 생성 파일 중 불필요 파일 삭제: src/App.css, src/assets/react.svg, public/vite.svg (있다면).
       src/App.tsx는 임시로 최소한의 "브랜드 네이밍" 텍스트만 렌더하도록 수정.
  </action>
  <verify>
    <automated>cd C:/BRAND && npm run dev -- --host 2>&1 | head -20</automated>
    Vite dev server가 에러 없이 시작되는지 확인. 또한:
    - `npm run build` 가 성공하는지 확인
    - index.html에 pretendardvariable 링크가 존재하는지 확인
    - src/index.css에 `@import "tailwindcss"` 와 `--font-sans` 가 존재하는지 확인
  </verify>
  <acceptance_criteria>
    - package.json에 "react": "^19" 또는 "~19" 포함
    - package.json에 "zustand", "lucide-react" dependencies 포함
    - package.json에 "tailwindcss", "@tailwindcss/vite" devDependencies 포함
    - vite.config.ts에 `tailwindcss()` 플러그인 호출 포함
    - index.html에 `pretendardvariable-dynamic-subset.min.css` href 포함
    - index.html에 `lang="ko"` 포함
    - src/index.css에 `@import "tailwindcss"` 포함
    - src/index.css에 `--color-primary: #2563EB` 포함
    - src/index.css에 `--font-sans: 'Pretendard Variable'` 포함
    - `npm run build` 명령이 exit code 0으로 완료
  </acceptance_criteria>
  <done>Vite dev server가 에러 없이 실행되고, Pretendard 폰트 CDN이 로드되며, Tailwind CSS v4 유틸리티가 동작하고, 디자인 시스템 컬러 토큰이 CSS 변수로 정의됨</done>
</task>

<task type="auto">
  <name>Task 2: 타입 정의 + Zustand 폼 스토어 생성</name>
  <files>src/types/form.ts, src/store/useFormStore.ts</files>
  <read_first>
    .planning/phases/01-foundation-input-ui/01-RESEARCH.md
    .planning/phases/01-foundation-input-ui/01-CONTEXT.md
  </read_first>
  <action>
    1. src/types/form.ts 생성 -- 4개 섹션별 상태 인터페이스 + 통합 FormState:

       ```typescript
       /** 섹션 1: 매장 기본/환경 (INPUT-01 ~ INPUT-06) */
       export interface StoreBasicState {
         category: string;        // INPUT-01 업종/카테고리 (dropdown)
         location: string;        // INPUT-02 위치/지역 (text)
         scale: string;           // INPUT-03 매장 규모 (dropdown)
         mainProduct: string;     // INPUT-04 주력 상품/서비스 (textarea)
         priceRange: string;      // INPUT-05 가격대 (dropdown)
         targetCustomer: string;  // INPUT-06 타겟 고객 (textarea)
       }

       /** 섹션 2: 브랜드 정체성 및 비전 (OWNER-01 ~ OWNER-03) */
       export interface BrandVisionState {
         ceoVision: string;       // OWNER-01 CEO 비전/꿈
         longTermGoal: string;    // OWNER-02 5년/10년 목표
         personalStory: string;   // OWNER-03 개인 스토리/동기
       }

       /** 섹션 3: 제품/서비스 핵심 (PERSONA-04 일부) */
       export interface ProductState {
         uniqueStrength: string;      // 제품/서비스 특장점
         valueProposition: string;    // 고객에게 주는 핵심 가치
       }

       /** 섹션 5: 브랜드 페르소나 (PERSONA-01 ~ PERSONA-04) */
       export interface PersonaState {
         philosophy: string;      // PERSONA-01 브랜드 철학
         slogan: string;          // PERSONA-02 슬로건
         mission: string;         // PERSONA-03 미션
         brandStory: string;      // PERSONA-04 브랜드 스토리
         coreValues: string;      // PERSONA-04 핵심 가치
         differentiation: string; // PERSONA-04 차별화 포인트
         toneAndManner: string;   // PERSONA-04 브랜드 톤앤매너
       }

       /** 전체 폼 상태 */
       export interface FormState {
         storeBasic: StoreBasicState;
         brandVision: BrandVisionState;
         product: ProductState;
         persona: PersonaState;
       }

       /** 추천 카드 (Phase 2 대비) */
       export interface RecommendCard {
         id: string;
         brandName: string;
         reasoning: string;
         basedOn: string[];
         createdAt: Date;
       }

       /** 전체 앱 상태 */
       export interface AppState extends FormState {
         recommendations: RecommendCard[];
       }
       ```

    2. src/store/useFormStore.ts 생성 -- Zustand create 사용:

       ```typescript
       import { create } from 'zustand';
       import type { AppState, StoreBasicState, BrandVisionState, ProductState, PersonaState } from '../types/form';

       // 초기값 (모든 필드 빈 문자열)
       const initialStoreBasic: StoreBasicState = {
         category: '', location: '', scale: '',
         mainProduct: '', priceRange: '', targetCustomer: '',
       };
       const initialBrandVision: BrandVisionState = {
         ceoVision: '', longTermGoal: '', personalStory: '',
       };
       const initialProduct: ProductState = {
         uniqueStrength: '', valueProposition: '',
       };
       const initialPersona: PersonaState = {
         philosophy: '', slogan: '', mission: '',
         brandStory: '', coreValues: '', differentiation: '', toneAndManner: '',
       };

       interface FormActions {
         updateStoreBasic: (field: keyof StoreBasicState, value: string) => void;
         updateBrandVision: (field: keyof BrandVisionState, value: string) => void;
         updateProduct: (field: keyof ProductState, value: string) => void;
         updatePersona: (field: keyof PersonaState, value: string) => void;
         resetAll: () => void;
         // Phase 2 대비
         addRecommendation: (card: import('../types/form').RecommendCard) => void;
       }

       export const useFormStore = create<AppState & FormActions>((set) => ({
         storeBasic: { ...initialStoreBasic },
         brandVision: { ...initialBrandVision },
         product: { ...initialProduct },
         persona: { ...initialPersona },
         recommendations: [],

         updateStoreBasic: (field, value) =>
           set((state) => ({ storeBasic: { ...state.storeBasic, [field]: value } })),
         updateBrandVision: (field, value) =>
           set((state) => ({ brandVision: { ...state.brandVision, [field]: value } })),
         updateProduct: (field, value) =>
           set((state) => ({ product: { ...state.product, [field]: value } })),
         updatePersona: (field, value) =>
           set((state) => ({ persona: { ...state.persona, [field]: value } })),
         resetAll: () =>
           set({
             storeBasic: { ...initialStoreBasic },
             brandVision: { ...initialBrandVision },
             product: { ...initialProduct },
             persona: { ...initialPersona },
           }),
         addRecommendation: (card) =>
           set((state) => ({ recommendations: [card, ...state.recommendations] })),
       }));
       ```

       섹션별 업데이트 함수로 분리 -- 각 섹션 컴포넌트에서 해당 함수만 import하여 사용.
       hasAnyValue 같은 유틸 헬퍼는 각 섹션 컴포넌트에서 직접 계산 (스토어 비대화 방지).
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
    TypeScript 컴파일 에러 없음 확인.
  </verify>
  <acceptance_criteria>
    - src/types/form.ts에 `export interface StoreBasicState` 포함
    - src/types/form.ts에 `export interface BrandVisionState` 포함
    - src/types/form.ts에 `export interface ProductState` 포함
    - src/types/form.ts에 `export interface PersonaState` 포함
    - src/types/form.ts에 `export interface FormState` 포함
    - src/types/form.ts에 `export interface RecommendCard` 포함
    - src/types/form.ts에 `category: string` (INPUT-01) 포함
    - src/types/form.ts에 `toneAndManner: string` (PERSONA-04 마지막 필드) 포함
    - src/store/useFormStore.ts에 `export const useFormStore = create` 포함
    - src/store/useFormStore.ts에 `updateStoreBasic` 포함
    - src/store/useFormStore.ts에 `updatePersona` 포함
    - src/store/useFormStore.ts에 `resetAll` 포함
    - src/store/useFormStore.ts에 `addRecommendation` 포함
    - `npx tsc --noEmit` 가 exit code 0으로 완료
  </acceptance_criteria>
  <done>18개 필드에 대한 타입 정의가 완료되고, Zustand 스토어가 4개 섹션별 update 함수 + resetAll + addRecommendation을 제공</done>
</task>

<task type="auto">
  <name>Task 3: 5개 공용 UI 컴포넌트 생성 (TextField, TextArea, Dropdown, SectionHeader, RecommendButton)</name>
  <files>
    src/components/ui/TextField.tsx,
    src/components/ui/TextArea.tsx,
    src/components/ui/Dropdown.tsx,
    src/components/ui/SectionHeader.tsx,
    src/components/ui/RecommendButton.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-input-ui/01-UI-SPEC.md
  </read_first>
  <action>
    UI-SPEC의 Interaction States, Typography, Spacing, CSS Techniques Contract를 정확히 구현.

    1. src/components/ui/TextField.tsx:
       - Props: `{ label: string; value: string; onChange: (value: string) => void; placeholder?: string; disabled?: boolean; id?: string }`
       - label: 14px/500weight(font-medium), mb-2 간격으로 input 위에 배치
       - `<label htmlFor={id}>` + `<input id={id}>` 연결 (접근성, UI-SPEC Accessibility)
       - input 스타일: `w-full px-4 py-2.5 rounded-lg border border-gray-200 bg-white text-[15px] leading-relaxed placeholder:text-gray-400 transition-all duration-200 hover:border-gray-300 focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 focus:outline-none disabled:opacity-50 disabled:cursor-not-allowed disabled:bg-gray-50`
       - onChange에서 `e.target.value`를 추출하여 `onChange(value)` 호출

    2. src/components/ui/TextArea.tsx:
       - Props: `{ label: string; value: string; onChange: (value: string) => void; placeholder?: string; rows?: number; disabled?: boolean; id?: string }`
       - rows 기본값 3 (D-04에 따라 3~5줄)
       - label과 textarea 스타일은 TextField와 동일한 패턴
       - textarea에 `resize-none` 추가 (깔끔한 레이아웃 유지)
       - `<label htmlFor={id}>` + `<textarea id={id}>` 연결

    3. src/components/ui/Dropdown.tsx:
       - Props: `{ label: string; value: string; onChange: (value: string) => void; options: string[]; placeholder?: string; disabled?: boolean; id?: string }`
       - `<select>` 기반 (네이티브 select — 커스텀 드롭다운은 복잡도 과도, 사내 앱에서 충분)
       - 첫 번째 option: `<option value="" disabled>{placeholder || "선택하세요"}</option>`
       - select 스타일: TextField와 동일 + `appearance-none cursor-pointer` + 우측에 chevron 아이콘 표시 (Lucide `ChevronDown` 아이콘을 absolute 포지셔닝 또는 background-image로)
       - value가 비어있을 때 `text-gray-400` (placeholder 색상)
       - `<label htmlFor={id}>` + `<select id={id}>` 연결

    4. src/components/ui/SectionHeader.tsx:
       - Props: `{ title: string; description?: string }`
       - title: `text-[22px] font-semibold tracking-[-0.01em] text-gray-900` (UI-SPEC Heading 스타일)
       - description: `text-sm text-gray-500 mt-1` (선택적)
       - 하단에 섹션 구분선: `<div className="mt-4 h-px bg-gradient-to-r from-transparent via-gray-200 to-transparent" />`
         (UI-SPEC Section divider 기법 적용)

    5. src/components/ui/RecommendButton.tsx:
       - Props: `{ onClick?: () => void; disabled?: boolean; loading?: boolean }`
       - 버튼 텍스트: "추천 받기" (UI-SPEC Copywriting: Primary CTA)
       - loading 상태: Lucide `Loader2` 아이콘 (animate-spin) + "추천 중..." 텍스트
       - 기본 스타일: `w-full py-3 px-6 rounded-lg bg-blue-600 text-white font-medium text-[15px] shadow-sm transition-all duration-200 hover:bg-blue-700 hover:shadow active:bg-blue-800 active:shadow-inner`
       - disabled 스타일: `disabled:opacity-50 disabled:cursor-not-allowed`
       - Phase 1에서 onClick은 연결하지 않을 수 있음 (Phase 2에서 API 호출 연결)
       - Lucide `Sparkles` 아이콘을 버튼 텍스트 왼쪽에 배치 (size 18)

    모든 컴포넌트:
    - export default 대신 named export 사용 (`export function ComponentName`)
    - 500줄 미만 (각 ~40-80줄 예상)
    - React.useId() 활용하여 label-input 연결 id 자동 생성 (id prop이 없을 때)
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
    TypeScript 에러 없음 확인. 또한 `npm run build` 성공 확인.
  </verify>
  <acceptance_criteria>
    - src/components/ui/TextField.tsx에 `export function TextField` 포함
    - src/components/ui/TextField.tsx에 `htmlFor` 포함 (접근성)
    - src/components/ui/TextField.tsx에 `focus:ring-2` 포함 (포커스 링)
    - src/components/ui/TextArea.tsx에 `export function TextArea` 포함
    - src/components/ui/TextArea.tsx에 `resize-none` 포함
    - src/components/ui/Dropdown.tsx에 `export function Dropdown` 포함
    - src/components/ui/Dropdown.tsx에 `<select` 포함
    - src/components/ui/Dropdown.tsx에 `선택하세요` 또는 placeholder 텍스트 포함
    - src/components/ui/SectionHeader.tsx에 `export function SectionHeader` 포함
    - src/components/ui/SectionHeader.tsx에 `from-transparent via-gray-200 to-transparent` 포함 (그라데이션 구분선)
    - src/components/ui/RecommendButton.tsx에 `export function RecommendButton` 포함
    - src/components/ui/RecommendButton.tsx에 `추천 받기` 포함
    - src/components/ui/RecommendButton.tsx에 `bg-blue-600` 포함
    - `npm run build` 명령이 exit code 0으로 완료
  </acceptance_criteria>
  <done>5개 공용 UI 컴포넌트가 UI-SPEC의 인터랙션 상태, 타이포그래피, 스페이싱, CSS 기법을 정확히 반영하여 완성됨. 모든 입력 컴포넌트에 label 연결(접근성), 포커스 링, hover 전환 효과 적용</done>
</task>

</tasks>

<verification>
1. `npm run build` 성공 (zero errors)
2. `npx tsc --noEmit` 성공 (타입 에러 없음)
3. src/types/form.ts에 18개 필드 타입 정의 존재
4. src/store/useFormStore.ts에 4개 섹션별 update 함수 존재
5. src/components/ui/ 디렉토리에 5개 .tsx 파일 존재
6. src/index.css에 Tailwind v4 import + 디자인 시스템 컬러 토큰 존재
</verification>

<success_criteria>
- Vite dev server가 에러 없이 실행됨
- TypeScript 컴파일 에러 없음
- Pretendard 폰트 CDN이 index.html에 설정됨
- Tailwind CSS v4가 @tailwindcss/vite 플러그인으로 구성됨
- 디자인 시스템 컬러 팔레트가 CSS 변수로 정의됨 (UI-SPEC Color 섹션 일치)
- 18개 입력 필드의 타입이 정의됨 (form.ts)
- Zustand 스토어가 4개 섹션별 상태 관리를 제공
- 5개 UI 컴포넌트가 UI-SPEC 인터랙션 상태를 정확히 반영
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-input-ui/01-01-SUMMARY.md`
</output>
