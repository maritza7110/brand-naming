---
phase: 14-data-test-design
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - vite.config.ts
  - package.json
  - src/utils/parseGeminiResponse.ts
  - src/utils/__tests__/parseGeminiResponse.test.ts
  - src/store/__tests__/useFormStore.test.ts
  - src/services/gemini.ts
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]

must_haves:
  truths:
    - "npm run test 명령으로 Vitest 테스트가 실행되고 통과한다"
    - "AI 응답 파싱 함수에 대해 정상/빈값/잘못된 JSON 케이스가 모두 테스트된다"
    - "세션 저장(setCurrentSessionId) 및 복원(restoreSession) 로직에 대한 테스트가 존재하고 통과한다"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vitest test 블록 설정"
      contains: "test:"
    - path: "package.json"
      provides: "test 스크립트"
      contains: "vitest run"
    - path: "src/utils/parseGeminiResponse.ts"
      provides: "AI 응답 파싱 순수 함수"
      exports: ["parseSimpleResponse", "parseDocBasedResponse"]
    - path: "src/utils/__tests__/parseGeminiResponse.test.ts"
      provides: "AI 파싱 테스트 (정상/빈값/잘못된 JSON)"
      contains: "describe"
    - path: "src/store/__tests__/useFormStore.test.ts"
      provides: "세션 저장/복원 테스트"
      contains: "setCurrentSessionId"
  key_links:
    - from: "src/services/gemini.ts"
      to: "src/utils/parseGeminiResponse.ts"
      via: "import { parseSimpleResponse, parseDocBasedResponse }"
      pattern: "import.*parseGeminiResponse"
    - from: "src/utils/__tests__/parseGeminiResponse.test.ts"
      to: "src/utils/parseGeminiResponse.ts"
      via: "import { parseSimpleResponse }"
      pattern: "from.*parseGeminiResponse"
---

<objective>
Vitest 테스트 인프라를 구축하고 AI 응답 파싱 및 세션 저장/복원 로직에 대한 테스트를 작성한다.

Purpose: 핵심 비즈니스 로직에 자동화 테스트를 추가하여 회귀 버그를 방지하고, 향후 리팩터링의 안전망을 제공한다.
Output: vitest 설정, AI 파싱 순수 함수 추출, 파싱 테스트, 세션 store 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-data-test-design/14-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/services/gemini.ts (parsing logic to extract, lines 231-273):
```typescript
interface SimpleResult {
  brandName: string; reasoning: string; validityScore?: number;
  namingTechnique?: string; meaningAnalysis?: string; reflectedInputs?: string[];
}
interface DocBasedResult {
  candidates: {
    brandName: string; reasoning: string; validityScore: number;
    namingTechnique: string; meaningAnalysis: string; reflectedInputs: string[];
    documentReference: string; trademarkRiskLevel: string;
    trademarkGrade: string; trademarkNote: string;
  }[];
  consumerChecklist: string[];
  processNote: string;
}

// Simple parsing (lines 266-273):
let simple: SimpleResult[];
try {
  simple = JSON.parse(raw);
} catch {
  const m = raw.match(/\[[\s\S]*\]/);
  if (!m) throw new Error('AI 응답을 파싱할 수 없습니다. 다시 시도해주세요.');
  simple = JSON.parse(m[0]);
}

// Doc-based parsing (lines 232-239):
let parsed: DocBasedResult;
try {
  parsed = JSON.parse(raw);
} catch {
  const m = raw.match(/\{[\s\S]*\}/);
  if (!m) throw new Error('AI 응답을 파싱할 수 없습니다. 다시 시도해주세요.');
  parsed = JSON.parse(m[0]);
}
```

From src/store/useFormStore.ts (actions to test):
```typescript
currentSessionId: null as string | null,
setCurrentSessionId: (id) => set({ currentSessionId: id }),
restoreSession: (formState, batches) => set({
  storeBasic: formState.storeBasic,
  brandVision: formState.brandVision,
  product: formState.product,
  persona: formState.persona,
  analysis: formState.analysis,
  identity: formState.identity,
  expression: formState.expression,
  batches,
  resetTimestamp: null,
  activeTab: 'analysis' as TabId,
  keywordWeights: {},
}),
resetNaming: () => set({
  // ... 초기값들 ...
  currentSessionId: null,  // Plan 01에서 추가됨
}),
```

From vite.config.ts (current):
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  optimizeDeps: {
    exclude: ['pdfjs-dist'],
  },
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest 설치 + 설정 + AI 파싱 함수 추출</name>
  <files>vite.config.ts, package.json, src/utils/parseGeminiResponse.ts, src/services/gemini.ts</files>
  <read_first>
    - vite.config.ts (현재 Vite 설정)
    - package.json (현재 scripts 및 devDependencies)
    - src/services/gemini.ts (파싱 로직 lines 170-173, 231-273)
    - tsconfig.app.json (types 필드 확인)
  </read_first>
  <action>
**Step 1: Vitest 설치 (per D-07)**
```bash
npm install -D vitest @vitest/coverage-v8
```

**Step 2: vite.config.ts에 test 블록 추가**

파일 최상단에 `/// <reference types="vitest" />` 추가 (Pitfall 5 방지).

```typescript
/// <reference types="vitest" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  optimizeDeps: {
    exclude: ['pdfjs-dist'],
  },
  test: {
    environment: 'jsdom',
    globals: true,
  },
})
```

**Step 3: package.json scripts에 test 추가**

기존 scripts 객체에 다음 두 항목 추가:
```json
"test": "vitest run",
"test:watch": "vitest"
```

기존 scripts (`dev`, `build`, `lint`, `preview`)는 그대로 유지.

**Step 4: AI 파싱 순수 함수 추출 (per D-09)**

`src/utils/parseGeminiResponse.ts` 신규 파일 생성:

```typescript
// ─── AI 응답 파싱 순수 함수 (gemini.ts에서 추출) ─────────────────────────────

export interface SimpleResult {
  brandName: string;
  reasoning: string;
  validityScore?: number;
  namingTechnique?: string;
  meaningAnalysis?: string;
  reflectedInputs?: string[];
}

export interface DocBasedResult {
  candidates: {
    brandName: string;
    reasoning: string;
    validityScore: number;
    namingTechnique: string;
    meaningAnalysis: string;
    reflectedInputs: string[];
    documentReference: string;
    trademarkRiskLevel: string;
    trademarkGrade: string;
    trademarkNote: string;
  }[];
  consumerChecklist: string[];
  processNote: string;
}

/**
 * 문서 없는 경우 Gemini 응답 파싱 (SimpleResult[])
 * - 정상 JSON 배열 → 파싱
 * - 비어있으면 에러
 * - JSON이 아니면 배열 패턴 추출 시도
 */
export function parseSimpleResponse(raw: string): SimpleResult[] {
  if (!raw || raw.trim() === '') {
    throw new Error('AI 응답이 비어 있습니다.');
  }
  try {
    return JSON.parse(raw);
  } catch {
    const m = raw.match(/\[[\s\S]*\]/);
    if (!m) throw new Error('AI 응답을 파싱할 수 없습니다. 다시 시도해주세요.');
    return JSON.parse(m[0]);
  }
}

/**
 * 문서 있는 경우 Gemini 응답 파싱 (DocBasedResult)
 * - 정상 JSON 객체 → 파싱
 * - 비어있으면 에러
 * - JSON이 아니면 객체 패턴 추출 시도
 */
export function parseDocBasedResponse(raw: string): DocBasedResult {
  if (!raw || raw.trim() === '') {
    throw new Error('AI 응답이 비어 있습니다.');
  }
  try {
    return JSON.parse(raw);
  } catch {
    const m = raw.match(/\{[\s\S]*\}/);
    if (!m) throw new Error('AI 응답을 파싱할 수 없습니다. 다시 시도해주세요.');
    return JSON.parse(m[0]);
  }
}
```

**Step 5: gemini.ts에서 추출한 함수 사용하도록 교체**

gemini.ts 상단에 import 추가:
```typescript
import { parseSimpleResponse, parseDocBasedResponse } from '../utils/parseGeminiResponse';
import type { SimpleResult, DocBasedResult } from '../utils/parseGeminiResponse';
```

gemini.ts에서 `SimpleResult`와 `DocBasedResult` 인터페이스 선언 (lines 170-183) 삭제 — 이제 utils에서 import.

gemini.ts line 231-239 (doc-based parsing) 교체:
```typescript
// 기존: try { parsed = JSON.parse(raw); } catch { ... }
const parsed = parseDocBasedResponse(raw);
```

gemini.ts line 266-273 (simple parsing) 교체:
```typescript
// 기존: try { simple = JSON.parse(raw); } catch { ... }
const simple = parseSimpleResponse(raw);
```

로컬 변수 타입 선언 (`let parsed: DocBasedResult;`, `let simple: SimpleResult[];`)도 제거 — `const`로 직접 받음.
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20 && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts first line contains `/// <reference types="vitest" />`
    - vite.config.ts contains `test:` block with `environment: 'jsdom'` and `globals: true`
    - package.json contains `"test": "vitest run"`
    - package.json contains `"test:watch": "vitest"`
    - src/utils/parseGeminiResponse.ts exists with `export function parseSimpleResponse` and `export function parseDocBasedResponse`
    - src/services/gemini.ts contains `import { parseSimpleResponse, parseDocBasedResponse } from '../utils/parseGeminiResponse'`
    - src/services/gemini.ts does NOT contain `interface SimpleResult` (moved to utils)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Vitest가 설치되어 npm run test로 실행 가능하고, AI 파싱 로직이 순수 함수로 추출되어 gemini.ts가 이를 사용한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AI 응답 파싱 테스트 작성</name>
  <files>src/utils/__tests__/parseGeminiResponse.test.ts</files>
  <read_first>
    - src/utils/parseGeminiResponse.ts (Task 1에서 생성된 파싱 함수)
  </read_first>
  <behavior>
    - parseSimpleResponse: 정상 JSON 배열 → SimpleResult[] 반환
    - parseSimpleResponse: 빈 문자열 → 'AI 응답이 비어 있습니다.' 에러
    - parseSimpleResponse: 잘못된 JSON (배열 패턴 없음) → 'AI 응답을 파싱할 수 없습니다' 에러
    - parseSimpleResponse: JSON 앞뒤에 쓰레기 텍스트가 있지만 배열 패턴 존재 → 추출 후 파싱 성공
    - parseDocBasedResponse: 정상 JSON 객체 → DocBasedResult 반환
    - parseDocBasedResponse: 빈 문자열 → 에러
    - parseDocBasedResponse: 잘못된 JSON (객체 패턴 없음) → 에러
  </behavior>
  <action>
`src/utils/__tests__/parseGeminiResponse.test.ts` 신규 파일 생성:

```typescript
import { describe, it, expect } from 'vitest';
import { parseSimpleResponse, parseDocBasedResponse } from '../parseGeminiResponse';

describe('parseSimpleResponse', () => {
  it('정상 JSON 배열을 파싱한다', () => {
    const raw = JSON.stringify([
      { brandName: '테스트', reasoning: '이유', validityScore: 85, namingTechnique: '조합', meaningAnalysis: '의미', reflectedInputs: ['업종'] },
    ]);
    const result = parseSimpleResponse(raw);
    expect(result).toHaveLength(1);
    expect(result[0].brandName).toBe('테스트');
    expect(result[0].validityScore).toBe(85);
  });

  it('빈 문자열이면 에러를 던진다', () => {
    expect(() => parseSimpleResponse('')).toThrow('AI 응답이 비어 있습니다.');
  });

  it('공백만 있는 문자열이면 에러를 던진다', () => {
    expect(() => parseSimpleResponse('   ')).toThrow('AI 응답이 비어 있습니다.');
  });

  it('배열 패턴이 없는 잘못된 JSON이면 에러를 던진다', () => {
    expect(() => parseSimpleResponse('not json at all')).toThrow('AI 응답을 파싱할 수 없습니다');
  });

  it('JSON 앞뒤에 쓰레기 텍스트가 있어도 배열 패턴을 추출한다', () => {
    const raw = 'Here is the result: [{"brandName":"추출됨","reasoning":"이유"}] end';
    const result = parseSimpleResponse(raw);
    expect(result).toHaveLength(1);
    expect(result[0].brandName).toBe('추출됨');
  });
});

describe('parseDocBasedResponse', () => {
  it('정상 JSON 객체를 파싱한다', () => {
    const raw = JSON.stringify({
      candidates: [{ brandName: '문서기반', reasoning: '이유', validityScore: 90, namingTechnique: '암시', meaningAnalysis: '분석', reflectedInputs: ['비전'], documentReference: '참고문헌', trademarkRiskLevel: '낮음', trademarkGrade: '조어상표', trademarkNote: '안전' }],
      consumerChecklist: ['체크1'],
      processNote: '프로세스',
    });
    const result = parseDocBasedResponse(raw);
    expect(result.candidates).toHaveLength(1);
    expect(result.candidates[0].brandName).toBe('문서기반');
    expect(result.consumerChecklist).toContain('체크1');
  });

  it('빈 문자열이면 에러를 던진다', () => {
    expect(() => parseDocBasedResponse('')).toThrow('AI 응답이 비어 있습니다.');
  });

  it('객체 패턴이 없는 잘못된 JSON이면 에러를 던진다', () => {
    expect(() => parseDocBasedResponse('invalid')).toThrow('AI 응답을 파싱할 수 없습니다');
  });
});
```
  </action>
  <verify>
    <automated>cd C:/BRAND && npx vitest run src/utils/__tests__/parseGeminiResponse.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/__tests__/parseGeminiResponse.test.ts exists
    - File contains `describe('parseSimpleResponse'` with at least 5 test cases
    - File contains `describe('parseDocBasedResponse'` with at least 3 test cases
    - File contains `toThrow('AI 응답이 비어 있습니다.')`
    - File contains `toThrow('AI 응답을 파싱할 수 없습니다')`
    - `npx vitest run src/utils/__tests__/parseGeminiResponse.test.ts` shows all tests passing
  </acceptance_criteria>
  <done>AI 응답 파싱 함수에 대해 정상/빈값/잘못된 JSON/쓰레기 텍스트 케이스가 모두 테스트되고 통과한다</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 세션 저장/복원 로직 테스트 작성</name>
  <files>src/store/__tests__/useFormStore.test.ts</files>
  <read_first>
    - src/store/useFormStore.ts (setCurrentSessionId, restoreSession, resetNaming — Plan 01에서 수정된 버전)
    - src/types/form.ts (FormState, RecommendBatch 타입)
  </read_first>
  <behavior>
    - setCurrentSessionId('session-123') → currentSessionId가 'session-123'
    - setCurrentSessionId(null) → currentSessionId가 null
    - restoreSession(formState, batches) → storeBasic, brandVision 등 폼 필드가 복원됨
    - restoreSession 후 batches가 전달된 값과 일치
    - resetNaming → currentSessionId가 null로 리셋 (D-03)
    - resetNaming → batches는 리셋되지 않음 (resetNaming은 batches를 초기화하지 않음)
  </behavior>
  <action>
`src/store/__tests__/useFormStore.test.ts` 신규 파일 생성:

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { useFormStore } from '../useFormStore';
import type { FormState, RecommendBatch } from '../../types/form';

// 테스트용 최소 FormState
const mockFormState: FormState = {
  storeBasic: {
    industry: { major: '음식', medium: '카페', minor: '', note: '' },
    location: '서울 강남',
    scale: '소형',
    mainProduct: '커피',
    priceRange: '중가',
    targetCustomer: '2030 직장인',
  },
  brandVision: { ceoVision: '최고의 카페', longTermGoal: '프랜차이즈', personalStory: '' },
  product: { uniqueStrength: '원두 직수입', valueProposition: '합리적 가격' },
  persona: {
    philosophy: '', slogan: '', coreTechnology: '', coreStrategy: '',
    brandMent: '', customerDefinition: '', customerValue: '',
    customerCultureCreation: '', competitiveAdvantage: '', qualityLevel: '',
    priceLevel: '', functionalBenefit: '', experientialBenefit: '',
    symbolicBenefit: '', brandKeyword: '', membershipPhilosophy: '',
  },
  analysis: { competitors: '', usp: '' },
  identity: { marketTrend: '', brandPersonality: [] },
  expression: { namingStyle: [], languageConstraint: '' },
};

const mockBatch: RecommendBatch = {
  id: 'batch-1',
  names: [{ brandName: '카페온', reasoning: '따뜻한 느낌' }],
  basedOn: ['업종', '위치'],
  createdAt: new Date('2026-04-08'),
};

describe('useFormStore - 세션 관리', () => {
  beforeEach(() => {
    // 각 테스트 전에 store를 초기 상태로 리셋 (Pitfall 3 방지)
    useFormStore.setState({
      currentSessionId: null,
      batches: [],
      storeBasic: {
        industry: { major: '', medium: '', minor: '', note: '' },
        location: '', scale: '', mainProduct: '', priceRange: '', targetCustomer: '',
      },
      brandVision: { ceoVision: '', longTermGoal: '', personalStory: '' },
      product: { uniqueStrength: '', valueProposition: '' },
    });
  });

  it('setCurrentSessionId로 세션 ID를 저장한다', () => {
    useFormStore.getState().setCurrentSessionId('session-123');
    expect(useFormStore.getState().currentSessionId).toBe('session-123');
  });

  it('setCurrentSessionId(null)로 리셋하면 null이 된다', () => {
    useFormStore.getState().setCurrentSessionId('old-session');
    useFormStore.getState().setCurrentSessionId(null);
    expect(useFormStore.getState().currentSessionId).toBeNull();
  });

  it('restoreSession이 폼 상태를 복원한다', () => {
    useFormStore.getState().restoreSession(mockFormState, [mockBatch]);
    const state = useFormStore.getState();
    expect(state.storeBasic.location).toBe('서울 강남');
    expect(state.storeBasic.mainProduct).toBe('커피');
    expect(state.brandVision.ceoVision).toBe('최고의 카페');
    expect(state.product.uniqueStrength).toBe('원두 직수입');
  });

  it('restoreSession이 batches를 복원한다', () => {
    useFormStore.getState().restoreSession(mockFormState, [mockBatch]);
    const state = useFormStore.getState();
    expect(state.batches).toHaveLength(1);
    expect(state.batches[0].names[0].brandName).toBe('카페온');
  });

  it('resetNaming이 currentSessionId를 null로 리셋한다 (D-03)', () => {
    useFormStore.getState().setCurrentSessionId('active-session');
    useFormStore.getState().resetNaming();
    expect(useFormStore.getState().currentSessionId).toBeNull();
  });

  it('resetNaming 후 resetTimestamp가 설정된다', () => {
    useFormStore.getState().resetNaming();
    expect(useFormStore.getState().resetTimestamp).toBeInstanceOf(Date);
  });
});
```
  </action>
  <verify>
    <automated>cd C:/BRAND && npx vitest run src/store/__tests__/useFormStore.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/store/__tests__/useFormStore.test.ts exists
    - File contains `describe('useFormStore - 세션 관리'`
    - File contains test for `setCurrentSessionId('session-123')`
    - File contains test for `setCurrentSessionId(null)`
    - File contains test for `restoreSession(mockFormState`
    - File contains test for `resetNaming` with `currentSessionId` assertion
    - `npx vitest run src/store/__tests__/useFormStore.test.ts` shows all tests passing
  </acceptance_criteria>
  <done>세션 저장(setCurrentSessionId), 복원(restoreSession), 초기화(resetNaming) 로직에 대한 테스트가 존재하고 모두 통과한다</done>
</task>

</tasks>

<verification>
- `npm run test` 실행 시 모든 테스트 통과
- parseGeminiResponse.test.ts: 8개 테스트 케이스 (정상/빈값/잘못된JSON/쓰레기텍스트 x 2종류)
- useFormStore.test.ts: 6개 테스트 케이스 (세션 저장/리셋/복원/배치 복원/resetNaming 리셋/타임스탬프)
- gemini.ts가 추출된 파싱 함수를 import하여 사용
</verification>

<success_criteria>
- `npm run test`로 Vitest 테스트가 실행되고 전체 통과
- AI 응답 파싱에 대해 정상/빈값/잘못된 JSON 케이스가 모두 테스트됨
- 세션 저장/복원 로직에 대한 테스트가 존재하고 통과
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/14-data-test-design/14-02-SUMMARY.md`
</output>
