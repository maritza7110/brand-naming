---
phase: 14-data-test-design
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useRecommend.ts
  - src/store/useFormStore.ts
  - src/pages/NamingPage.tsx
  - src/services/galleryService.ts
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "같은 위저드에서 추천을 여러 번 받아도 세션이 하나만 존재하고 업데이트된다"
    - "위저드 초기화 시 다음 추천은 새 세션으로 생성된다"
    - "대시보드에서 세션 복원 후 추가 추천을 받으면 기존 세션에 누적된다"
    - "리더보드 likes 쿼리에 limit이 적용되어 전체 데이터를 불러오지 않는다"
  artifacts:
    - path: "src/hooks/useRecommend.ts"
      provides: "createOrUpdate 세션 로직"
      contains: "updateSession"
    - path: "src/store/useFormStore.ts"
      provides: "resetNaming 시 currentSessionId null 리셋"
      contains: "currentSessionId: null"
    - path: "src/pages/NamingPage.tsx"
      provides: "세션 복원 시 currentSessionId 세팅"
      contains: "setCurrentSessionId"
    - path: "src/services/galleryService.ts"
      provides: "리더보드 likes 쿼리 제한"
      contains: "LEADERBOARD_LIKES_LIMIT"
  key_links:
    - from: "src/hooks/useRecommend.ts"
      to: "src/store/useFormStore.ts"
      via: "useFormStore.getState().currentSessionId 분기"
      pattern: "currentSessionId"
    - from: "src/hooks/useRecommend.ts"
      to: "src/services/sessionService.ts"
      via: "createSession / updateSession 호출"
      pattern: "sessionService\\.(create|update)Session"
---

<objective>
세션 오염 문제를 수정하고 리더보드 전송량을 제한한다.

Purpose: 추천을 받을 때마다 새 세션이 생성되는 버그를 수정하여 데이터 정합성을 확보하고, 리더보드가 불필요하게 전체 likes를 불러오는 문제를 해결한다.
Output: useRecommend.ts의 createOrUpdate 세션 로직, resetNaming의 sessionId 리셋, 대시보드 복원 시 sessionId 세팅, galleryService.ts의 likes 쿼리 limit
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/store/useFormStore.ts:
```typescript
currentSessionId: null as string | null,
setCurrentSessionId: (id) => set({ currentSessionId: id }),
restoreSession: (formState, batches) => set({ ... }),
resetNaming: () => set({ ... resetTimestamp: new Date() }),
addBatch: (batch) => set((state) => ({ batches: [batch, ...state.batches] })),
```

From src/services/sessionService.ts:
```typescript
createSession: async (userId: string, title: string, formState: FormState, batches: RecommendBatch[]): Promise<string>
updateSession: async (sessionId: string, formState: FormState, batches: RecommendBatch[]): Promise<void>
```

From src/hooks/useRecommend.ts (line 50-57, current logic):
```typescript
const batch = await withRetry(() => generateBrandNames(form, keywordWeights));
addBatch(batch);
const user = useAuthStore.getState().user;
if (user) {
  const title = batch.names.map((n) => n.brandName).join(', ');
  await sessionService.createSession(user.id, title, form, [batch]).catch(console.error);
}
```

From src/pages/NamingPage.tsx (line 68, current restore):
```typescript
useFormStore.getState().restoreSession(stored, restoredBatches);
// NOTE: setCurrentSessionId(sessionId) is MISSING here -- D-04 requires adding it
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useRecommend.ts 세션 createOrUpdate 로직 + useFormStore resetNaming 수정</name>
  <files>src/hooks/useRecommend.ts, src/store/useFormStore.ts</files>
  <read_first>
    - src/hooks/useRecommend.ts (현재 세션 저장 로직 line 50-57)
    - src/store/useFormStore.ts (currentSessionId, resetNaming, addBatch 구현)
    - src/services/sessionService.ts (createSession 반환값: Promise<string>, updateSession 시그니처)
  </read_first>
  <action>
**useRecommend.ts 수정 (line 50-57 영역 교체, per D-01/D-02):**

현재 코드:
```typescript
const batch = await withRetry(() => generateBrandNames(form, keywordWeights));
addBatch(batch);
const user = useAuthStore.getState().user;
if (user) {
  const title = batch.names.map((n) => n.brandName).join(', ');
  await sessionService.createSession(user.id, title, form, [batch]).catch(console.error);
}
```

변경 후 코드:
```typescript
const batch = await withRetry(() => generateBrandNames(form, keywordWeights));
addBatch(batch);

const user = useAuthStore.getState().user;
if (user) {
  const latestState = useFormStore.getState();
  const existingId = latestState.currentSessionId;

  if (existingId) {
    // 기존 세션 업데이트 — 누적된 전체 배치 전달 (per D-02)
    sessionService.updateSession(existingId, form, latestState.batches).catch(console.error);
  } else {
    // 최초 세션 생성
    const title = batch.names.map((n) => n.brandName).join(', ');
    sessionService.createSession(user.id, title, form, latestState.batches)
      .then((id) => useFormStore.getState().setCurrentSessionId(id))
      .catch(console.error);
  }
}
```

핵심 변경점:
1. `addBatch(batch)` 이후에 `useFormStore.getState()`를 호출하여 최신 batches를 읽음 (Pitfall 1 방지)
2. `currentSessionId`가 존재하면 `updateSession`, 없으면 `createSession` 호출
3. `createSession` 성공 시 `setCurrentSessionId(id)` 호출로 이후 추천이 같은 세션에 누적
4. `latestState.batches` 전체를 전달하여 누적 저장 (기존 `[batch]` 단건 전달에서 변경)

**useFormStore.ts 수정 (line 169-181, per D-03):**

현재 `resetNaming` 코드:
```typescript
resetNaming: () =>
  set({
    storeBasic: { ...initialStoreBasic },
    brandVision: { ...initialBrandVision },
    product: { ...initialProduct },
    persona: { ...initialPersona },
    analysis: { ...initialAnalysis },
    identity: { ...initialIdentity },
    expression: { ...initialExpression },
    keywordWeights: {},
    activeTab: 'analysis' as TabId,
    resetTimestamp: new Date(),
  }),
```

변경 후 — `currentSessionId: null` 추가:
```typescript
resetNaming: () =>
  set({
    storeBasic: { ...initialStoreBasic },
    brandVision: { ...initialBrandVision },
    product: { ...initialProduct },
    persona: { ...initialPersona },
    analysis: { ...initialAnalysis },
    identity: { ...initialIdentity },
    expression: { ...initialExpression },
    keywordWeights: {},
    activeTab: 'analysis' as TabId,
    resetTimestamp: new Date(),
    currentSessionId: null,
  }),
```
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - useRecommend.ts contains `latestState.currentSessionId` or `existingId = latestState.currentSessionId`
    - useRecommend.ts contains `sessionService.updateSession(existingId`
    - useRecommend.ts contains `latestState.batches` (NOT `[batch]`)
    - useRecommend.ts contains `setCurrentSessionId(id)`
    - useFormStore.ts resetNaming action contains `currentSessionId: null`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>추천 시 currentSessionId 분기로 create/update 구분, resetNaming 시 sessionId가 null로 리셋</done>
</task>

<task type="auto">
  <name>Task 2: NamingPage 세션 복원 시 sessionId 세팅 + galleryService 리더보드 limit</name>
  <files>src/pages/NamingPage.tsx, src/services/galleryService.ts</files>
  <read_first>
    - src/pages/NamingPage.tsx (line 60-70, 세션 복원 useEffect)
    - src/services/galleryService.ts (line 70-78, fetchLeaderboard의 likes 쿼리)
    - src/store/useFormStore.ts (setCurrentSessionId 시그니처 확인)
  </read_first>
  <action>
**NamingPage.tsx 수정 (line 68 이후, per D-04):**

현재 코드:
```typescript
useFormStore.getState().restoreSession(stored, restoredBatches);
```

변경 후 — `setCurrentSessionId` 추가:
```typescript
useFormStore.getState().restoreSession(stored, restoredBatches);
useFormStore.getState().setCurrentSessionId(sessionId);
```

`sessionId`는 같은 useEffect 내의 기존 변수 (URL 파라미터에서 추출됨). 이 한 줄 추가로 대시보드에서 복원한 세션에 추가 추천을 받으면 기존 세션에 누적된다.

**galleryService.ts 수정 (line 72, per D-05):**

현재 코드:
```typescript
let likesQuery = supabase.from('likes').select('session_id, created_at');
```

변경 후:
```typescript
const LEADERBOARD_LIKES_LIMIT = 1000;
let likesQuery = supabase
  .from('likes')
  .select('session_id, created_at')
  .limit(LEADERBOARD_LIKES_LIMIT);
```

`LEADERBOARD_LIKES_LIMIT = 1000`의 근거: 사내 100인 앱에서 likes가 1000개를 초과할 가능성은 낮고, 리더보드 TOP 5를 뽑는 데 통계적으로 충분한 샘플. 과도한 방어 코드를 피하는 Phase 13 원칙 계승 (Claude's Discretion).

상수는 `fetchLeaderboard` 함수 직전에 선언. 파일 상단의 `PAGE_SIZE` 상수와 동일한 패턴으로 모듈 레벨에 선언한다.
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - NamingPage.tsx contains `setCurrentSessionId(sessionId)` after `restoreSession` call
    - galleryService.ts contains `const LEADERBOARD_LIKES_LIMIT = 1000`
    - galleryService.ts contains `.limit(LEADERBOARD_LIKES_LIMIT)`
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>대시보드 복원 시 sessionId가 store에 세팅되고, 리더보드 likes 쿼리에 1000개 limit이 적용된다</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 타입 체크 통과
- useRecommend.ts에 `updateSession` 호출 존재
- useFormStore.ts resetNaming에 `currentSessionId: null` 존재
- NamingPage.tsx에 `setCurrentSessionId` 호출 존재
- galleryService.ts에 `.limit(LEADERBOARD_LIKES_LIMIT)` 존재
</verification>

<success_criteria>
- 추천 시 currentSessionId 유무에 따라 create/update 분기가 동작한다
- resetNaming 호출 시 currentSessionId가 null로 리셋된다
- 대시보드 세션 복원 후 currentSessionId가 세팅된다
- 리더보드 likes 쿼리가 최대 1000건으로 제한된다
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/14-data-test-design/14-01-SUMMARY.md`
</output>
