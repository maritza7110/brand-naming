---
phase: 05-card-grouping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/form.ts
  - src/store/useFormStore.ts
  - src/utils/groupBatches.ts
  - src/components/recommend/RecommendGroup.tsx
  - src/services/gemini.ts
autonomous: true
requirements: [GROUP-01, GROUP-02, RESET-01]

must_haves:
  truths:
    - "RecommendBatch 타입에 industry 옵셔널 필드가 존재한다"
    - "persist version 2로 마이그레이션되며 기존 배치 데이터가 보존된다"
    - "groupBatches 함수가 배치를 업종별 그룹으로 변환한다"
    - "RecommendGroup 컴포넌트가 CSS Grid 0fr/1fr 접기/펼치기를 제공한다"
    - "배치 생성 시 현재 선택된 업종 정보가 캡처된다"
    - "resetNaming 액션이 폼 초기화 + resetTimestamp 기록을 수행한다"
  artifacts:
    - path: "src/types/form.ts"
      provides: "RecommendBatch.industry 옵셔널 필드, AppState.resetTimestamp"
      contains: "industry?: IndustrySelection"
    - path: "src/store/useFormStore.ts"
      provides: "persist v2 마이그레이션, resetNaming 액션, resetTimestamp 상태"
      contains: "resetNaming"
    - path: "src/utils/groupBatches.ts"
      provides: "배치 -> BatchGroup[] 변환 유틸"
      exports: ["groupBatches", "BatchGroup", "getGroupKey"]
    - path: "src/components/recommend/RecommendGroup.tsx"
      provides: "그룹 헤더 + CSS Grid 접기/펼치기 래퍼"
      exports: ["RecommendGroup"]
    - path: "src/services/gemini.ts"
      provides: "배치 생성 시 industry 캡처"
      contains: "industry:"
  key_links:
    - from: "src/utils/groupBatches.ts"
      to: "src/types/form.ts"
      via: "RecommendBatch.industry import"
      pattern: "import.*RecommendBatch.*form"
    - from: "src/components/recommend/RecommendGroup.tsx"
      to: "src/components/recommend/RecommendCardItem.tsx"
      via: "RecommendCardItem 렌더링"
      pattern: "RecommendCardItem"
    - from: "src/services/gemini.ts"
      to: "src/store/useFormStore.ts"
      via: "useFormStore.getState().storeBasic.industry 캡처"
      pattern: "storeBasic\\.industry"
---

<objective>
Phase 5 Plan 01: 추천 카드 그루핑의 데이터 레이어, 유틸리티, 컴포넌트 빌딩 블록을 모두 구축한다.

Purpose: Plan 02에서 App.tsx 통합 시 필요한 모든 타입, 스토어 액션, 유틸 함수, UI 컴포넌트를 선제적으로 생성하여 의존성 없는 조립이 가능하게 한다.
Output: RecommendBatch에 industry 필드 추가된 타입, persist v2 마이그레이션, groupBatches 유틸, RecommendGroup 컴포넌트, gemini.ts industry 캡처
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-card-grouping/05-RESEARCH.md

<interfaces>
<!-- 현재 RecommendBatch 타입 (industry 필드 없음 -- 이 plan에서 추가) -->
From src/types/form.ts:
```typescript
export interface IndustrySelection {
  major: string;
  medium: string;
  minor: string;
  note: string;
}

export interface RecommendBatch {
  id: string;
  names: { brandName: string; reasoning: string }[];
  basedOn: string[];
  createdAt: Date;
}

export interface AppState extends FormState {
  batches: RecommendBatch[];
  isLoading: boolean;
  error: string | null;
}
```

From src/store/useFormStore.ts:
```typescript
interface FormActions {
  updateStoreBasic: (field: Exclude<keyof StoreBasicState, 'industry'>, value: string) => void;
  updateIndustry: (selection: IndustrySelection) => void;
  updateBrandVision: (field: keyof BrandVisionState, value: string) => void;
  updateProduct: (field: keyof ProductState, value: string) => void;
  updatePersona: (field: keyof PersonaState, value: string) => void;
  resetAll: () => void;
  addBatch: (batch: RecommendBatch) => void;
  setLoading: (loading: boolean) => void;
  setError: (error: string | null) => void;
}
// persist config: version: 1, partialize: batches only
```

From src/services/gemini.ts:
```typescript
export async function generateBrandNames(form: FormState): Promise<RecommendBatch>
// 현재 batch 반환 시 industry 필드 없음 -- 이 plan에서 추가
```

From src/components/recommend/RecommendCardItem.tsx:
```typescript
interface Props { batch: RecommendBatch; }
export function RecommendCardItem({ batch }: Props)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 타입 확장 + 스토어 v2 마이그레이션 + 그룹핑 유틸</name>
  <files>src/types/form.ts, src/store/useFormStore.ts, src/utils/groupBatches.ts</files>
  <read_first>
    - src/types/form.ts (현재 RecommendBatch, AppState 타입 확인)
    - src/store/useFormStore.ts (현재 persist config, FormActions, 초기값 확인)
    - .planning/phases/05-card-grouping/05-RESEARCH.md (Pattern 2, Pattern 4, Example 2, Example 4)
  </read_first>
  <action>
**1. src/types/form.ts 수정:**

RecommendBatch 인터페이스에 옵셔널 industry 필드 추가:
```typescript
export interface RecommendBatch {
  id: string;
  names: { brandName: string; reasoning: string }[];
  basedOn: string[];
  createdAt: Date;
  industry?: IndustrySelection;  // 배치 생성 시점의 업종 스냅샷
}
```

AppState 인터페이스에 resetTimestamp 추가:
```typescript
export interface AppState extends FormState {
  batches: RecommendBatch[];
  isLoading: boolean;
  error: string | null;
  resetTimestamp: Date | null;  // 네이밍 초기화 시점 (RESET-01)
}
```

**2. src/store/useFormStore.ts 수정:**

FormActions에 resetNaming 액션 추가:
```typescript
interface FormActions {
  // ... 기존 액션 유지
  resetNaming: () => void;  // 폼 초기화 + resetTimestamp 기록
}
```

초기 상태에 resetTimestamp 추가:
```typescript
resetTimestamp: null,
```

resetNaming 액션 구현:
```typescript
resetNaming: () =>
  set({
    storeBasic: { ...initialStoreBasic },
    brandVision: { ...initialBrandVision },
    product: { ...initialProduct },
    persona: { ...initialPersona },
    resetTimestamp: new Date(),
  }),
```

persist config version 1 -> 2:
- partialize에 resetTimestamp 추가: `(state) => ({ batches: state.batches, resetTimestamp: state.resetTimestamp })`
- migrate 함수 업데이트:
```typescript
migrate: (persisted: unknown, version: number) => {
  if (version < 2) {
    const p = persisted as { batches?: RecommendBatch[] };
    return { ...p, resetTimestamp: null };
  }
  return persisted;
},
```
- merge 함수에 resetTimestamp Date 복원 추가:
```typescript
merge: (persisted, current) => {
  const p = persisted as {
    batches?: RecommendBatch[];
    resetTimestamp?: string | null;
  } | undefined;
  const restoredBatches = (p?.batches ?? []).map((b) => ({
    ...b,
    createdAt: new Date(b.createdAt),
  }));
  const restoredTimestamp = p?.resetTimestamp
    ? new Date(p.resetTimestamp)
    : null;
  return {
    ...current,
    batches: restoredBatches,
    resetTimestamp: restoredTimestamp,
  };
},
```

**3. src/utils/groupBatches.ts 신규 생성:**

Research Example 2의 패턴을 따르되, resetTimestamp 기반 아카이브 분리도 포함:

```typescript
import type { RecommendBatch, IndustrySelection } from '../types/form';

export interface BatchGroup {
  key: string;
  label: string;
  batches: RecommendBatch[];
}

export function getGroupKey(industry?: IndustrySelection): string {
  if (!industry) return '미분류';
  return industry.minor || industry.medium || industry.major || '미분류';
}

export function groupBatches(batches: RecommendBatch[]): BatchGroup[] {
  const map = new Map<string, RecommendBatch[]>();
  const order: string[] = [];

  for (const batch of batches) {
    const key = getGroupKey(batch.industry);
    if (!map.has(key)) {
      map.set(key, []);
      order.push(key);
    }
    map.get(key)!.push(batch);
  }

  return order.map((key) => ({
    key,
    label: key,
    batches: map.get(key)!,
  }));
}

export function splitByReset(
  batches: RecommendBatch[],
  resetTimestamp: Date | null,
): { current: RecommendBatch[]; archived: RecommendBatch[] } {
  if (!resetTimestamp) return { current: batches, archived: [] };
  const current: RecommendBatch[] = [];
  const archived: RecommendBatch[] = [];
  for (const b of batches) {
    if (b.createdAt >= resetTimestamp) {
      current.push(b);
    } else {
      archived.push(b);
    }
  }
  return { current, archived };
}
```
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/types/form.ts에 `industry?: IndustrySelection` 문자열이 RecommendBatch 내에 존재
    - src/types/form.ts에 `resetTimestamp: Date | null` 문자열이 AppState 내에 존재
    - src/store/useFormStore.ts에 `version: 2` 문자열이 존재
    - src/store/useFormStore.ts에 `resetNaming` 문자열이 존재
    - src/store/useFormStore.ts에 `resetTimestamp` 문자열이 partialize 내에 존재
    - src/utils/groupBatches.ts 파일이 존재하고 `export function groupBatches` 문자열이 존재
    - src/utils/groupBatches.ts에 `export function splitByReset` 문자열이 존재
    - src/utils/groupBatches.ts에 `export function getGroupKey` 문자열이 존재
    - src/utils/groupBatches.ts에 `export interface BatchGroup` 문자열이 존재
    - tsc --noEmit이 에러 0개로 통과
  </acceptance_criteria>
  <done>RecommendBatch.industry 옵셔널 필드, AppState.resetTimestamp, persist v2 마이그레이션(기존 배치 보존), resetNaming 액션, groupBatches/splitByReset/getGroupKey 유틸 함수가 모두 존재하고 TypeScript 컴파일 성공</done>
</task>

<task type="auto">
  <name>Task 2: RecommendGroup 컴포넌트 + gemini.ts industry 캡처</name>
  <files>src/components/recommend/RecommendGroup.tsx, src/services/gemini.ts</files>
  <read_first>
    - src/components/recommend/RecommendCardItem.tsx (RecommendCardItem props, 렌더링 패턴 확인)
    - src/services/gemini.ts (현재 generateBrandNames 반환값 확인)
    - src/types/form.ts (Task 1에서 수정된 RecommendBatch 확인)
    - .planning/phases/05-card-grouping/05-RESEARCH.md (Pattern 1, Example 1 -- CSS Grid 0fr/1fr, overflow-hidden 필수)
  </read_first>
  <action>
**1. src/components/recommend/RecommendGroup.tsx 신규 생성:**

Research Example 1의 패턴을 정확히 따른다. CSS Grid `grid-rows-[0fr]` / `grid-rows-[1fr]` + `transition-[grid-template-rows]` 사용.

```tsx
import { ChevronDown } from 'lucide-react';
import type { RecommendBatch } from '../../types/form';
import { RecommendCardItem } from './RecommendCardItem';

interface RecommendGroupProps {
  label: string;
  batches: RecommendBatch[];
  isOpen: boolean;
  onToggle: () => void;
  count: number;
}

export function RecommendGroup({ label, batches, isOpen, onToggle, count }: RecommendGroupProps) {
  return (
    <div className="rounded-xl border border-[#4A4440] overflow-hidden">
      {/* 그룹 헤더 */}
      <button
        type="button"
        onClick={onToggle}
        className="w-full flex items-center justify-between px-4 py-3 bg-[#332F2C] hover:bg-[#3E3A36] transition-colors"
      >
        <div className="flex items-center gap-2">
          <ChevronDown
            size={14}
            className={`text-[#B48C50] transition-transform duration-300 ${
              isOpen ? 'rotate-0' : '-rotate-90'
            }`}
          />
          <span className="text-[12px] font-semibold text-[#E8E2DA]">{label}</span>
        </div>
        <span className="text-[10px] text-[#7A7570]">{count}건</span>
      </button>

      {/* CSS Grid 0fr/1fr 접기/펼치기 */}
      <div
        className={`grid transition-[grid-template-rows] duration-300 ease-in-out ${
          isOpen ? 'grid-rows-[1fr]' : 'grid-rows-[0fr]'
        }`}
      >
        <div className="overflow-hidden">
          <div className="p-3 space-y-2">
            {batches.map((b) => (
              <RecommendCardItem key={b.id} batch={b} />
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
```

핵심 제약:
- overflow-hidden div가 Grid 컨테이너 바로 안에 필수 (Pitfall 1)
- padding은 overflow-hidden div 안쪽의 별도 div에 배치 (Pitfall 2)
- transition-all 대신 `transition-[grid-template-rows]` 명시적 지정
- ChevronDown 아이콘: isOpen일 때 rotate-0, 접혔을 때 -rotate-90
- 색상/폰트: 기존 앱 디자인 톤 유지 (#332F2C, #B48C50, #E8E2DA, #7A7570)
- font-semibold: Phase 4에서 확립된 2-weight 시스템 준수

**2. src/services/gemini.ts 수정:**

generateBrandNames 함수에서 배치 생성 시 현재 업종 정보를 캡처한다.

현재 함수는 `form: FormState` 파라미터를 받으므로, `form.storeBasic.industry`에서 업종 정보를 가져온다.

반환 객체에 industry 필드 추가:
```typescript
// 기존 반환 부분을 수정
const industry = form.storeBasic.industry;
const hasIndustry = industry.major || industry.medium || industry.minor;

return {
  id: `batch-${now.getTime()}`,
  names: parsed.slice(0, 2).map((item) => ({
    brandName: item.brandName,
    reasoning: item.reasoning,
  })),
  basedOn: filledFields,
  createdAt: now,
  industry: hasIndustry ? { ...industry } : undefined,
};
```

업종이 하나도 선택되지 않은 경우 industry를 undefined로 설정하여 "미분류" 그룹으로 분류되게 한다. 업종이 선택된 경우 스프레드 연산자로 스냅샷을 저장한다 (참조가 아닌 복사).
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/recommend/RecommendGroup.tsx 파일이 존재
    - RecommendGroup.tsx에 `export function RecommendGroup` 문자열이 존재
    - RecommendGroup.tsx에 `grid-rows-[0fr]` 문자열이 존재
    - RecommendGroup.tsx에 `grid-rows-[1fr]` 문자열이 존재
    - RecommendGroup.tsx에 `transition-[grid-template-rows]` 문자열이 존재
    - RecommendGroup.tsx에 `overflow-hidden` 문자열이 존재
    - RecommendGroup.tsx에 `ChevronDown` 문자열이 존재
    - RecommendGroup.tsx에 `-rotate-90` 문자열이 존재
    - RecommendGroup.tsx에 `RecommendCardItem` 문자열이 존재
    - src/services/gemini.ts에 `industry:` 문자열이 반환 객체 내에 존재 (배치 생성 부분)
    - src/services/gemini.ts에 `hasIndustry` 문자열이 존재
    - tsc --noEmit이 에러 0개로 통과
    - RecommendGroup.tsx 파일이 500줄 이하
  </acceptance_criteria>
  <done>RecommendGroup 컴포넌트가 CSS Grid 0fr/1fr 접기/펼치기 + 그룹 헤더를 제공하고, gemini.ts가 배치 생성 시 업종 스냅샷을 캡처하며, TypeScript 컴파일 성공</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- TypeScript 에러 0개
2. `npx vite build` -- Vite 빌드 성공
3. 모든 파일 500줄 이하
4. src/utils/groupBatches.ts 파일 존재 + 3개 export (groupBatches, splitByReset, getGroupKey)
5. src/components/recommend/RecommendGroup.tsx 파일 존재 + CSS Grid 0fr/1fr 패턴
6. src/store/useFormStore.ts persist version: 2
7. src/services/gemini.ts 배치에 industry 필드 포함
</verification>

<success_criteria>
- RecommendBatch.industry 옵셔널 필드가 타입에 존재
- persist v2 마이그레이션이 기존 배치를 보존 (industry=undefined)
- resetNaming 액션이 폼 초기화 + resetTimestamp 기록
- groupBatches 유틸이 배치를 업종별 BatchGroup[]으로 변환
- splitByReset 유틸이 resetTimestamp 기준으로 current/archived 분리
- RecommendGroup이 CSS Grid 0fr/1fr 접기/펼치기 + 헤더 제공
- gemini.ts가 배치 생성 시 현재 업종을 스냅샷으로 캡처
- TypeScript 컴파일 + Vite 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/05-card-grouping/05-01-SUMMARY.md`
</output>
