---
phase: 06-mobile-responsive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/AppLayout.tsx
  - src/components/layout/InputPanel.tsx
  - src/components/layout/RecommendPanel.tsx
autonomous: true
requirements:
  - MOBILE-01
  - MOBILE-02

must_haves:
  truths:
    - "1024px 미만 화면에서 입력 패널이 위, 추천 패널이 아래인 세로 스택 레이아웃으로 전환된다"
    - "1024px 이상 화면에서 기존 70/30 좌우 분할 레이아웃이 그대로 유지된다"
    - "AppLayout에 min-w-[1024px] 하드코딩이 제거되어 좁은 뷰포트에서도 가로 스크롤이 발생하지 않는다"
    - "모바일에서 InputPanel이 자연 높이로 확장되어 모든 폼이 보인다"
    - "모바일에서 RecommendPanel이 일반 플로우로 입력 아래에 위치한다"
    - "모바일에서 히어로 영역이 컴팩트해지고 버튼이 제목 아래로 재배치된다"
  artifacts:
    - path: "src/components/layout/AppLayout.tsx"
      provides: "반응형 flex/grid 전환 레이아웃"
      contains: "flex flex-col lg:grid"
    - path: "src/components/layout/InputPanel.tsx"
      provides: "모바일 h-auto/overflow-visible + 히어로 반응형"
      contains: "lg:h-screen"
    - path: "src/components/layout/RecommendPanel.tsx"
      provides: "모바일 static + border-t 전환"
      contains: "static lg:sticky"
  key_links:
    - from: "src/components/layout/AppLayout.tsx"
      to: "src/components/layout/InputPanel.tsx"
      via: "left prop slot"
      pattern: "flex flex-col lg:grid lg:grid-cols-\\[7fr_3fr\\]"
    - from: "src/components/layout/AppLayout.tsx"
      to: "src/components/layout/RecommendPanel.tsx"
      via: "right prop slot"
      pattern: "flex flex-col lg:grid"
---

<objective>
레이아웃 셸 3개 컴포넌트(AppLayout, InputPanel, RecommendPanel)를 mobile-first 반응형으로 전환한다. min-w-[1024px] 하드코딩을 제거하고, 모바일에서 세로 스택 / 데스크톱에서 70/30 그리드 전환을 구현한다.

Purpose: MOBILE-01(세로 스택), MOBILE-02(min-w 제거 + lg: 브레이크포인트) 요구사항 충족. 이 레이아웃 전환이 완료되어야 내부 컴포넌트 반응형(Plan 02)이 의미를 갖는다.
Output: 3개 레이아웃 컴포넌트의 반응형 className 변환 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mobile-responsive/06-CONTEXT.md
@.planning/phases/06-mobile-responsive/06-UI-SPEC.md
@.planning/phases/06-mobile-responsive/06-RESEARCH.md

<interfaces>
<!-- 3개 레이아웃 컴포넌트의 현행 코드. Executor는 className만 교체하면 된다. -->

From src/components/layout/AppLayout.tsx (13줄):
```typescript
interface AppLayoutProps {
  left: React.ReactNode;
  right: React.ReactNode;
}
export function AppLayout({ left, right }: AppLayoutProps) {
  return (
    <div className="grid grid-cols-[7fr_3fr] min-h-screen min-w-[1024px]">
      {left}
      {right}
    </div>
  );
}
```

From src/components/layout/InputPanel.tsx (54줄):
```typescript
interface InputPanelProps {
  children: React.ReactNode;
  onSettingsClick?: () => void;
  onResetClick?: () => void;
}
// 주요 className 변경 위치:
// line 11: <main className="overflow-y-auto h-screen scroll-smooth bg-[#2C2825]">
// line 12: <div className="max-w-[640px] mx-auto px-8 pt-12 pb-20">
// line 14: <div className="relative rounded-2xl bg-[#363230] px-8 py-9 mb-10">
// line 15: <div className="relative flex items-start justify-between">
// line 17: <p className="text-[10px] font-bold ..."> (Brand Naming 라벨)
// line 20: <h1 className="text-[26px] font-bold ...">
// line 27: <div className="flex flex-col items-end gap-2"> (버튼 그룹)
// line 31: API설정 버튼 px-3 py-2
// line 40: 네이밍 초기화 버튼 px-3 py-2
```

From src/components/layout/RecommendPanel.tsx (16줄):
```typescript
interface RecommendPanelProps {
  children: React.ReactNode;
}
// 주요 className 변경 위치:
// line 7: <aside className="sticky top-0 h-screen overflow-y-auto bg-[#252220] border-l border-[#4A4440]">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppLayout + RecommendPanel 반응형 전환</name>
  <files>src/components/layout/AppLayout.tsx, src/components/layout/RecommendPanel.tsx</files>
  <read_first>
    - src/components/layout/AppLayout.tsx (현행 className 확인)
    - src/components/layout/RecommendPanel.tsx (현행 className 확인)
    - .planning/phases/06-mobile-responsive/06-UI-SPEC.md (Layout Transformation Contract 섹션)
  </read_first>
  <action>
**AppLayout.tsx** (per D-01, MOBILE-01, MOBILE-02):

line 8의 div className을 다음으로 교체:
```
"flex flex-col lg:grid lg:grid-cols-[7fr_3fr] min-h-screen"
```

변경 내역:
- `grid grid-cols-[7fr_3fr]` 제거 -> `flex flex-col` (모바일 기본: 세로 스택)
- `lg:grid lg:grid-cols-[7fr_3fr]` 추가 (데스크톱: 기존 70/30 유지)
- `min-w-[1024px]` 제거 (STATE.md blocker 해결)
- `min-h-screen` 유지

**RecommendPanel.tsx**:

line 7의 aside className을 다음으로 교체:
```
"static lg:sticky lg:top-0 h-auto lg:h-screen overflow-visible lg:overflow-y-auto bg-[#252220] border-t lg:border-t-0 lg:border-l border-[#4A4440] min-h-[300px] lg:min-h-0"
```

변경 내역:
- `sticky top-0` -> `static lg:sticky lg:top-0` (모바일에서 sticky 해제)
- `h-screen` -> `h-auto lg:h-screen` (모바일에서 자연 높이)
- `overflow-y-auto` -> `overflow-visible lg:overflow-y-auto` (모바일에서 오버플로 해제)
- `border-l` -> `border-t lg:border-t-0 lg:border-l` (모바일: 상단 구분선, 데스크톱: 좌측 구분선)
- `min-h-[300px] lg:min-h-0` 추가 (모바일 빈 상태에서 최소 높이 확보, UI-SPEC 명시)
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AppLayout.tsx contains "flex flex-col lg:grid lg:grid-cols-[7fr_3fr] min-h-screen"
    - AppLayout.tsx does NOT contain "min-w-[1024px]"
    - AppLayout.tsx does NOT contain "min-w-"
    - RecommendPanel.tsx contains "static lg:sticky"
    - RecommendPanel.tsx contains "h-auto lg:h-screen"
    - RecommendPanel.tsx contains "border-t lg:border-t-0 lg:border-l"
    - RecommendPanel.tsx contains "min-h-[300px]"
    - TypeScript compilation passes (tsc --noEmit exits 0)
  </acceptance_criteria>
  <done>AppLayout이 모바일에서 flex-col 세로 스택, 데스크톱에서 grid 70/30 전환. RecommendPanel이 모바일에서 static/h-auto, 데스크톱에서 sticky/h-screen. min-w-[1024px] 완전 제거.</done>
</task>

<task type="auto">
  <name>Task 2: InputPanel 반응형 전환 (h-screen, 패딩, 히어로 재배치)</name>
  <files>src/components/layout/InputPanel.tsx</files>
  <read_first>
    - src/components/layout/InputPanel.tsx (현행 코드 전체)
    - .planning/phases/06-mobile-responsive/06-UI-SPEC.md (InputPanel + Hero Area + Touch Target 섹션)
    - .planning/phases/06-mobile-responsive/06-CONTEXT.md (D-05, D-06 결정)
  </read_first>
  <action>
InputPanel.tsx의 className을 mobile-first로 전환한다. 변경점이 많으므로 정확한 위치별로 기술한다.

**1. main 태그 (line 11):**
현행: `"overflow-y-auto h-screen scroll-smooth bg-[#2C2825]"`
변경: `"overflow-visible lg:overflow-y-auto h-auto lg:h-screen scroll-smooth bg-[#2C2825]"`

- `overflow-y-auto` -> `overflow-visible lg:overflow-y-auto` (모바일에서 오버플로 해제)
- `h-screen` -> `h-auto lg:h-screen` (모바일에서 자연 높이)

**2. 내부 컨테이너 (line 12):**
현행: `"max-w-[640px] mx-auto px-8 pt-12 pb-20"`
변경: `"max-w-none lg:max-w-[640px] mx-auto px-5 lg:px-8 pt-6 lg:pt-12 pb-8 lg:pb-20"`

- `max-w-[640px]` -> `max-w-none lg:max-w-[640px]` (모바일 전체 폭)
- `px-8` -> `px-5 lg:px-8` (모바일 좌우 패딩 축소)
- `pt-12` -> `pt-6 lg:pt-12` (모바일 상단 패딩 축소)
- `pb-20` -> `pb-8 lg:pb-20` (모바일 하단 패딩 축소)

**3. 히어로 컨테이너 (line 14) (per D-05):**
현행: `"relative rounded-2xl bg-[#363230] px-8 py-9 mb-10"`
변경: `"relative rounded-2xl bg-[#363230] px-5 lg:px-8 py-6 lg:py-9 mb-10"`

- `px-8` -> `px-5 lg:px-8` (모바일 패딩 축소)
- `py-9` -> `py-6 lg:py-9` (모바일 패딩 축소)

**4. 히어로 내부 flex (line 15) (per D-06):**
현행: `"relative flex items-start justify-between"`
변경: `"relative flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"`

- 모바일: `flex-col gap-4` (제목과 버튼이 세로 스택)
- 데스크톱: `lg:flex-row lg:items-start lg:justify-between` (기존 좌우 배치)

**5. Brand Naming 라벨 (line 17):**
현행: `"text-[10px] font-bold tracking-[0.25em] uppercase text-[#B48C50] mb-2"`
변경: `"text-[11px] font-semibold tracking-[0.25em] uppercase text-[#B48C50] mb-2"`

- `text-[10px]` -> `text-[11px]` (UI-SPEC Typography 계약: Caption 11px로 통합)
- `font-bold` -> `font-semibold` (UI-SPEC Typography 계약: 2-weight 시스템 600)

**6. 히어로 제목 (line 20) (per D-05):**
현행: `"text-[26px] font-bold tracking-[-0.03em] text-[#F0EBE3]"`
변경: `"text-[22px] lg:text-[26px] font-semibold tracking-[-0.03em] text-[#F0EBE3]"`

- `text-[26px]` -> `text-[22px] lg:text-[26px]` (모바일 제목 축소)
- `font-bold` -> `font-semibold` (UI-SPEC Typography 계약)

**7. 히어로 설명 (line 23):**
현행: `"mt-2 text-[13px] text-[#A09890] leading-relaxed"`
변경: 유지 (변경 없음)

**8. 버튼 그룹 컨테이너 (line 27) (per D-06):**
현행: `"flex flex-col items-end gap-2"`
변경: `"flex flex-row gap-2 lg:flex-col lg:items-end lg:gap-2"`

- 모바일: `flex-row gap-2` (API설정 + 초기화 버튼이 가로 나란히)
- 데스크톱: `lg:flex-col lg:items-end lg:gap-2` (기존 세로 우측 정렬)

**9. API설정 버튼 (line 31) 터치 타겟 확대 (MOBILE-03):**
현행 py 값: `py-2`
변경: `py-3 lg:py-2`

전체 className:
`"flex items-center gap-1.5 px-3 py-3 lg:py-2 rounded-lg text-[#A09890] text-[11px] font-medium transition hover:text-[#B48C50] hover:bg-[#504A44]"`

**10. 네이밍 초기화 버튼 (line 40) 터치 타겟 확대 (MOBILE-03):**
현행 py 값: `py-2`
변경: `py-3 lg:py-2`

전체 className:
`"flex items-center gap-1.5 px-3 py-3 lg:py-2 rounded-lg text-[#A09890] text-[11px] font-medium transition hover:text-red-400 hover:bg-[#504A44]"`
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20 && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - InputPanel.tsx contains "overflow-visible lg:overflow-y-auto"
    - InputPanel.tsx contains "h-auto lg:h-screen"
    - InputPanel.tsx contains "max-w-none lg:max-w-[640px]"
    - InputPanel.tsx contains "px-5 lg:px-8"
    - InputPanel.tsx contains "pt-6 lg:pt-12"
    - InputPanel.tsx contains "pb-8 lg:pb-20"
    - InputPanel.tsx contains "px-5 lg:px-8 py-6 lg:py-9" (히어로 패딩)
    - InputPanel.tsx contains "flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between" (히어로 flex)
    - InputPanel.tsx contains "text-[22px] lg:text-[26px]" (반응형 제목)
    - InputPanel.tsx contains "font-semibold" (2-weight 시스템)
    - InputPanel.tsx does NOT contain "font-bold"
    - InputPanel.tsx contains "flex flex-row gap-2 lg:flex-col lg:items-end" (버튼 그룹)
    - InputPanel.tsx contains "py-3 lg:py-2" (API설정 버튼 터치 타겟)
    - TypeScript compilation passes (tsc --noEmit exits 0)
    - Vite build succeeds
  </acceptance_criteria>
  <done>InputPanel이 모바일에서 h-auto/overflow-visible/축소 패딩, 히어로가 flex-col로 버튼 아래 배치, 터치 타겟 44px 확보. 데스크톱에서 기존 레이아웃 완전 유지.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- TypeScript 컴파일 에러 없음
2. `npx vite build --mode development` -- 빌드 성공
3. AppLayout.tsx에 `min-w-` 문자열이 없음 (grep 확인)
4. 3개 파일 모두 `lg:` 접두사 사용 확인 (첫 반응형 도입)
</verification>

<success_criteria>
- AppLayout: min-w-[1024px] 제거됨, 모바일 flex-col / 데스크톱 grid 전환
- InputPanel: h-auto/overflow-visible/패딩 축소/히어로 재배치 완료
- RecommendPanel: static/h-auto/border-t 전환 완료
- TypeScript + Vite 빌드 모두 성공
- 데스크톱 레이아웃 회귀 없음 (lg: 접두사로 기존값 보존)
</success_criteria>

<output>
After completion, create `.planning/phases/06-mobile-responsive/06-01-SUMMARY.md`
</output>
