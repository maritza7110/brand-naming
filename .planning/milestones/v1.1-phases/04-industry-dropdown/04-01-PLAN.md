---
phase: 04-industry-dropdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/form.ts
  - src/data/industryData.ts
  - src/data/industryMigration.ts
  - src/store/useFormStore.ts
autonomous: true
requirements:
  - INDUSTRY-02
  - INDUSTRY-03

must_haves:
  truths:
    - "IndustrySelection 타입이 major/medium/minor/note 4개 string 필드를 가진다"
    - "산업분류 데이터가 대분류 ~10개, 중분류 ~75개, 소분류 ~247개 구조로 존재한다"
    - "getMajorCategories/getMediumCategories/getMinorCategories 헬퍼 함수가 계층 필터링을 수행한다"
    - "useFormStore에 updateIndustry 액션이 존재하고 IndustrySelection 전체를 교체한다"
    - "persist version이 1로 올라가고 migrate 함수가 v0 데이터를 안전하게 통과시킨다"
  artifacts:
    - path: "src/types/form.ts"
      provides: "IndustrySelection 타입, StoreBasicState.industry 필드"
      contains: "interface IndustrySelection"
    - path: "src/data/industryData.ts"
      provides: "산업분류 정적 데이터 + 헬퍼 함수"
      exports: ["INDUSTRY_DATA", "getMajorCategories", "getMediumCategories", "getMinorCategories"]
    - path: "src/data/industryMigration.ts"
      provides: "14개 기존 flat category -> IndustrySelection 매핑"
      exports: ["LEGACY_CATEGORY_MAPPING"]
    - path: "src/store/useFormStore.ts"
      provides: "updateIndustry 액션, persist version 1, migrate 함수"
      contains: "updateIndustry"
  key_links:
    - from: "src/store/useFormStore.ts"
      to: "src/types/form.ts"
      via: "import IndustrySelection"
      pattern: "import.*IndustrySelection.*from.*types/form"
    - from: "src/store/useFormStore.ts"
      to: "src/data/industryMigration.ts"
      via: "import LEGACY_CATEGORY_MAPPING"
      pattern: "import.*LEGACY_CATEGORY_MAPPING"
---

<objective>
Phase 4의 데이터 기반을 구축한다: IndustrySelection 타입 정의, 247개 산업분류 정적 데이터, 기존 카테고리 매핑 테이블, Zustand 스토어의 updateIndustry 액션 및 persist 마이그레이션.

Purpose: Plan 02(UI + AI 통합)가 이 타입/데이터/스토어 위에서 구현되므로, 계약(contract)을 먼저 확립한다.
Output: form.ts 타입 변경, industryData.ts 정적 데이터, industryMigration.ts 매핑, useFormStore.ts 액션/마이그레이션
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-industry-dropdown/04-CONTEXT.md
@.planning/phases/04-industry-dropdown/04-RESEARCH.md

<interfaces>
<!-- 현재 form.ts 타입 (수정 대상) -->
From src/types/form.ts:
```typescript
export interface StoreBasicState {
  category: string;        // INPUT-01 업종/카테고리 (dropdown) -- 이것을 industry: IndustrySelection으로 교체
  location: string;
  scale: string;
  mainProduct: string;
  priceRange: string;
  targetCustomer: string;
}

export interface AppState extends FormState {
  batches: RecommendBatch[];
  isLoading: boolean;
  error: string | null;
}
```

From src/store/useFormStore.ts:
```typescript
interface FormActions {
  updateStoreBasic: (field: keyof StoreBasicState, value: string) => void;
  // ... other actions
  addBatch: (batch: RecommendBatch) => void;
}

// persist 설정 (현재)
{
  name: 'brand-naming-data',
  partialize: (state) => ({ batches: state.batches }),
  merge: (persisted, current) => {
    const p = persisted as { batches?: RecommendBatch[] } | undefined;
    const restoredBatches = (p?.batches ?? []).map((b) => ({
      ...b,
      createdAt: new Date(b.createdAt),
    }));
    return { ...current, batches: restoredBatches };
  },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IndustrySelection 타입 정의 + StoreBasicState 변경</name>
  <files>src/types/form.ts</files>
  <read_first>
    - src/types/form.ts (현재 StoreBasicState 확인)
    - .planning/phases/04-industry-dropdown/04-RESEARCH.md (Pattern 1: IndustrySelection 타입 설계)
  </read_first>
  <action>
src/types/form.ts를 수정한다:

1. 파일 상단에 IndustrySelection 인터페이스 추가:
```typescript
/** 업종 선택 상태 (대분류 > 중분류 > 소분류 + 비고) */
export interface IndustrySelection {
  major: string;    // 대분류 (예: "음식")
  medium: string;   // 중분류 (예: "한식")
  minor: string;    // 소분류 (예: "분식")
  note: string;     // 비고 (예: "떡볶이 전문")
}
```

2. StoreBasicState에서 `category: string` 필드를 제거하고 `industry: IndustrySelection`으로 교체:
```typescript
export interface StoreBasicState {
  industry: IndustrySelection; // 업종 (계층형 선택) -- 기존 category: string 대체
  location: string;
  scale: string;
  mainProduct: string;
  priceRange: string;
  targetCustomer: string;
}
```

필드명을 category에서 industry로 변경하는 이유: IndustrySelection 객체를 담으므로 의미론적으로 명확. per D-01.

나머지 인터페이스(BrandVisionState, ProductState, PersonaState, FormState, RecommendBatch, AppState)는 변경하지 않는다.
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/types/form.ts에 `export interface IndustrySelection {` 라인이 존재한다
    - IndustrySelection에 major, medium, minor, note 4개 string 필드가 있다
    - StoreBasicState에 `industry: IndustrySelection` 필드가 있다
    - StoreBasicState에 `category: string` 필드가 없다
    - IndustrySelection이 export되어 다른 파일에서 import 가능하다
  </acceptance_criteria>
  <done>IndustrySelection 타입이 정의되고 StoreBasicState.category가 industry로 교체됨. TypeScript 컴파일 에러가 발생하지만 Task 2, 3에서 해소됨.</done>
</task>

<task type="auto">
  <name>Task 2: 산업분류 정적 데이터 + 헬퍼 함수 + 마이그레이션 매핑</name>
  <files>src/data/industryData.ts, src/data/industryMigration.ts</files>
  <read_first>
    - .planning/phases/04-industry-dropdown/04-RESEARCH.md (Pattern 2: 데이터 구조, 매핑 테이블)
    - src/components/sections/StoreBasicSection.tsx (CATEGORY_OPTIONS 14개 확인)
  </read_first>
  <action>
**src/data/ 디렉토리가 없으면 생성한다.**

**파일 1: src/data/industryData.ts** -- 산업분류 정적 데이터 + 헬퍼 함수

소상공인시장진흥공단 상권업종분류 체계를 기반으로 대분류 ~10개, 중분류 ~75개, 소분류 ~247개 데이터를 작성한다. per INDUSTRY-02.

구조:
```typescript
export interface IndustryCategory {
  major: string;
  medium: string;
  minor: string;
}

/** 전체 산업분류 데이터 (~247개 소분류) */
export const INDUSTRY_DATA: IndustryCategory[] = [
  // 대분류: 음식
  { major: '음식', medium: '한식', minor: '한식/백반/한정식' },
  { major: '음식', medium: '한식', minor: '분식' },
  // ... 247개 항목 (한 줄에 한 항목)
];
```

대분류 목록 (10개, 순서):
음식, 소매, 생활서비스, 의료, 학문/교육, 관광/여가/오락, 숙박, 부동산, 수리/개인서비스, 기타

**중요:** 파일이 500줄을 초과할 경우 데이터 배열을 `src/data/industryDataRaw.ts`로 분리하고 `industryData.ts`에서 re-export + 헬퍼 함수만 남긴다.

헬퍼 함수 3개:
```typescript
/** 대분류 목록 (유니크, 데이터 순서 유지) */
export function getMajorCategories(): string[] {
  return [...new Set(INDUSTRY_DATA.map((d) => d.major))];
}

/** 선택된 대분류의 중분류 목록 */
export function getMediumCategories(major: string): string[] {
  return [...new Set(
    INDUSTRY_DATA.filter((d) => d.major === major).map((d) => d.medium)
  )];
}

/** 선택된 대분류+중분류의 소분류 목록 */
export function getMinorCategories(major: string, medium: string): string[] {
  return INDUSTRY_DATA
    .filter((d) => d.major === major && d.medium === medium)
    .map((d) => d.minor);
}
```

**파일 2: src/data/industryMigration.ts** -- 기존 14개 flat category 매핑 (안전장치)

per D-11. 현재 form 필드는 persist 안 되므로 이 매핑은 직접 사용되지 않지만, 향후 form persist 추가 시 사용될 안전장치이다.

```typescript
import type { IndustrySelection } from '../types/form';

/** 기존 v1.0 flat 카테고리 -> IndustrySelection 매핑 (D-11) */
export const LEGACY_CATEGORY_MAPPING: Record<string, Partial<IndustrySelection>> = {
  '음식점/레스토랑': { major: '음식' },
  '카페/베이커리': { major: '음식', medium: '카페/디저트' },
  '주점/바': { major: '음식', medium: '유흥주점' },
  '소매/마트': { major: '소매' },
  '패션/의류': { major: '소매', medium: '의류' },
  '뷰티/미용': { major: '생활서비스', medium: '미용' },
  '건강/피트니스': { major: '관광/여가/오락', medium: '스포츠' },
  '교육/학원': { major: '학문/교육' },
  'IT/테크': { major: '기타' },
  '디자인/크리에이티브': { major: '기타' },
  '컨설팅/전문서비스': { major: '기타' },
  '숙박/펜션': { major: '숙박' },
  '문화/엔터테인먼트': { major: '관광/여가/오락' },
  '기타': { major: '기타' },
};
```

매핑의 medium 값은 industryData.ts의 실제 중분류명과 정확히 일치해야 한다. 데이터 작성 후 매핑 값을 교차 확인한다.
  </action>
  <verify>
    <automated>cd C:/BRAND && node -e "const d = require('./src/data/industryData.ts'); console.log('ok')" 2>&1 || npx tsx -e "import { INDUSTRY_DATA, getMajorCategories, getMediumCategories, getMinorCategories } from './src/data/industryData'; console.log('majors:', getMajorCategories().length); console.log('total:', INDUSTRY_DATA.length); console.log('medium for 음식:', getMediumCategories('음식').length);"</automated>
  </verify>
  <acceptance_criteria>
    - src/data/industryData.ts에 `export const INDUSTRY_DATA: IndustryCategory[]` 가 존재한다
    - INDUSTRY_DATA 배열 길이가 230개 이상이다 (247개 목표)
    - getMajorCategories() 반환값이 약 10개이다
    - getMediumCategories('음식') 반환값이 5개 이상이다
    - getMinorCategories 함수가 major + medium 파라미터를 받는다
    - src/data/industryMigration.ts에 `export const LEGACY_CATEGORY_MAPPING` 이 존재한다
    - LEGACY_CATEGORY_MAPPING이 14개 키를 가진다
    - 모든 소스 파일이 500줄 이하이다 (`wc -l` 확인)
  </acceptance_criteria>
  <done>산업분류 정적 데이터 ~247개가 TypeScript 파일로 존재하고, 3개 헬퍼 함수가 계층 필터링을 수행하며, 14개 기존 카테고리 매핑이 준비됨.</done>
</task>

<task type="auto">
  <name>Task 3: Zustand 스토어 updateIndustry 액션 + persist 마이그레이션</name>
  <files>src/store/useFormStore.ts</files>
  <read_first>
    - src/store/useFormStore.ts (현재 스토어 전체 구조)
    - src/types/form.ts (Task 1에서 변경한 IndustrySelection + StoreBasicState)
    - .planning/phases/04-industry-dropdown/04-RESEARCH.md (Pattern 4: updateStoreBasic 시그니처 변경, Example 3: persist migrate)
  </read_first>
  <action>
src/store/useFormStore.ts를 수정한다:

1. import에 IndustrySelection 추가:
```typescript
import type {
  AppState,
  StoreBasicState,
  BrandVisionState,
  ProductState,
  PersonaState,
  RecommendBatch,
  IndustrySelection,  // 추가
} from '../types/form';
```

2. initialStoreBasic에서 `category: ''`를 `industry: { major: '', medium: '', minor: '', note: '' }`로 교체:
```typescript
const initialStoreBasic: StoreBasicState = {
  industry: { major: '', medium: '', minor: '', note: '' },
  location: '',
  scale: '',
  mainProduct: '',
  priceRange: '',
  targetCustomer: '',
};
```

3. FormActions에 updateIndustry 액션 추가 (기존 updateStoreBasic은 유지 -- 나머지 string 필드용):
```typescript
interface FormActions {
  updateStoreBasic: (field: keyof StoreBasicState, value: string) => void;
  updateIndustry: (selection: IndustrySelection) => void;  // 추가
  // ... 나머지 동일
}
```

**주의:** updateStoreBasic의 value 타입이 `string`인데, `industry` 필드는 `IndustrySelection` 객체이므로 updateStoreBasic으로 industry를 업데이트하면 타입 에러가 발생한다. 이는 의도된 동작 -- industry는 반드시 updateIndustry를 통해서만 업데이트한다. updateStoreBasic의 field 파라미터 타입을 `Exclude<keyof StoreBasicState, 'industry'>`로 좁힌다:
```typescript
updateStoreBasic: (field: Exclude<keyof StoreBasicState, 'industry'>, value: string) => void;
```

4. 스토어 구현부에 updateIndustry 추가:
```typescript
updateIndustry: (selection) =>
  set((state) => ({
    storeBasic: { ...state.storeBasic, industry: selection },
  })),
```

5. persist 옵션에 version과 migrate 추가 (per D-10, INDUSTRY-03):
```typescript
{
  name: 'brand-naming-data',
  version: 1,  // 0 -> 1로 업그레이드
  partialize: (state) => ({ batches: state.batches }),
  migrate: (persisted: unknown, version: number) => {
    // v0: batches만 persist됨, category는 persist 안 됨
    // batches.basedOn은 string[] 라벨이므로 호환성 유지 (D-12)
    // 변환 없이 그대로 통과
    return persisted;
  },
  merge: (persisted, current) => {
    const p = persisted as { batches?: RecommendBatch[] } | undefined;
    const restoredBatches = (p?.batches ?? []).map((b) => ({
      ...b,
      createdAt: new Date(b.createdAt),
    }));
    return { ...current, batches: restoredBatches };
  },
}
```

6. resetAll에서도 industry 초기값 반영 (initialStoreBasic 스프레드로 이미 처리됨 -- 확인만).
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - useFormStore.ts에 `updateIndustry` 액션이 정의되어 있다
    - updateIndustry가 `(selection: IndustrySelection) => void` 시그니처를 가진다
    - initialStoreBasic에 `industry: { major: '', medium: '', minor: '', note: '' }` 가 있다
    - initialStoreBasic에 `category` 키가 없다
    - persist 옵션에 `version: 1` 이 설정되어 있다
    - persist 옵션에 `migrate` 함수가 존재한다
    - updateStoreBasic의 field 파라미터가 industry를 제외한다 (`Exclude<keyof StoreBasicState, 'industry'>`)
    - 파일이 500줄 이하이다
  </acceptance_criteria>
  <done>updateIndustry 액션으로 IndustrySelection 전체를 교체할 수 있고, persist version이 1로 올라가 기존 v0 localStorage 데이터가 안전하게 마이그레이션됨. TypeScript 컴파일 에러가 StoreBasicSection.tsx와 gemini.ts에 남아 있지만 Plan 02에서 해소됨.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 실행 시 form.ts, industryData.ts, industryMigration.ts, useFormStore.ts 자체에는 에러가 없어야 한다.
  (StoreBasicSection.tsx, gemini.ts에서 category 참조 에러가 남아있는 것은 Plan 02에서 해결한다.)
- `wc -l src/data/industryData.ts src/data/industryMigration.ts src/store/useFormStore.ts src/types/form.ts` 모든 파일 500줄 이하.
- industryData.ts의 INDUSTRY_DATA 길이가 230개 이상.
</verification>

<success_criteria>
1. IndustrySelection 타입이 form.ts에 export되고 major/medium/minor/note 4개 필드를 가진다
2. StoreBasicState.category가 industry: IndustrySelection으로 교체되었다
3. 산업분류 데이터가 ~247개 소분류로 정적 TypeScript 파일에 존재한다
4. getMajorCategories/getMediumCategories/getMinorCategories 헬퍼가 동작한다
5. useFormStore에 updateIndustry 액션이 있고 persist version이 1이다
6. 기존 14개 flat category -> IndustrySelection 매핑 테이블이 존재한다
</success_criteria>

<output>
After completion, create `.planning/phases/04-industry-dropdown/04-01-SUMMARY.md`
</output>
