---
phase: 04-industry-dropdown
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/components/sections/StoreBasicSection.tsx
  - src/services/gemini.ts
  - src/components/ui/SectionHeader.tsx
autonomous: false
requirements:
  - INDUSTRY-01

must_haves:
  truths:
    - "사용자가 대분류를 선택하면 해당하는 중분류 목록이 표시된다"
    - "사용자가 중분류를 선택하면 해당하는 소분류 목록이 표시된다"
    - "대분류 변경 시 중분류/소분류/비고가 초기화된다"
    - "상위 미선택 시 하위 드롭다운이 disabled 상태로 표시된다"
    - "AI 프롬프트에 업종 경로가 '음식 > 한식 > 분식 (비고: 떡볶이 전문)' 형태로 포함된다"
    - "앱이 에러 없이 빌드되고 런타임에서 정상 동작한다"
  artifacts:
    - path: "src/components/sections/StoreBasicSection.tsx"
      provides: "4단 계층형 업종 선택 UI (2x2 grid)"
      contains: "getMajorCategories"
    - path: "src/services/gemini.ts"
      provides: "업종 경로 포맷 함수, buildInputSummary 수정"
      contains: "formatIndustryPath"
    - path: "src/components/ui/SectionHeader.tsx"
      provides: "font-semibold 적용"
      contains: "font-semibold"
  key_links:
    - from: "src/components/sections/StoreBasicSection.tsx"
      to: "src/store/useFormStore.ts"
      via: "useFormStore((st) => st.storeBasic.industry) + useFormStore((st) => st.updateIndustry)"
      pattern: "useFormStore.*updateIndustry"
    - from: "src/components/sections/StoreBasicSection.tsx"
      to: "src/data/industryData.ts"
      via: "import getMajorCategories, getMediumCategories, getMinorCategories"
      pattern: "import.*getMajorCategories.*from.*industryData"
    - from: "src/services/gemini.ts"
      to: "src/types/form.ts"
      via: "IndustrySelection 타입으로 업종 경로 포맷"
      pattern: "formatIndustryPath"
---

<objective>
StoreBasicSection의 기존 14개 flat 업종 드롭다운을 4단 계층형 UI(대분류 > 중분류 > 소분류 + 비고)로 교체하고, gemini.ts의 AI 프롬프트에 업종 경로를 반영한다.

Purpose: 사용자가 247개 소분류를 3단 계층형 드롭다운으로 탐색하여 업종을 선택할 수 있게 한다 (INDUSTRY-01). AI에게 정확한 업종 경로를 전달하여 추천 품질을 높인다 (D-08).
Output: StoreBasicSection.tsx 계층형 UI, gemini.ts 업종 포맷, SectionHeader.tsx 폰트 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-industry-dropdown/04-CONTEXT.md
@.planning/phases/04-industry-dropdown/04-RESEARCH.md
@.planning/phases/04-industry-dropdown/04-UI-SPEC.md
@.planning/phases/04-industry-dropdown/04-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 타입/데이터/스토어 계약 -->

From src/types/form.ts (Plan 01에서 변경):
```typescript
export interface IndustrySelection {
  major: string;
  medium: string;
  minor: string;
  note: string;
}

export interface StoreBasicState {
  industry: IndustrySelection;
  location: string;
  scale: string;
  mainProduct: string;
  priceRange: string;
  targetCustomer: string;
}
```

From src/data/industryData.ts (Plan 01에서 생성):
```typescript
export interface IndustryCategory {
  major: string;
  medium: string;
  minor: string;
}
export const INDUSTRY_DATA: IndustryCategory[];
export function getMajorCategories(): string[];
export function getMediumCategories(major: string): string[];
export function getMinorCategories(major: string, medium: string): string[];
```

From src/store/useFormStore.ts (Plan 01에서 변경):
```typescript
updateStoreBasic: (field: Exclude<keyof StoreBasicState, 'industry'>, value: string) => void;
updateIndustry: (selection: IndustrySelection) => void;
```

From src/components/ui/Dropdown.tsx (기존, 변경 없음):
```typescript
interface DropdownProps {
  label: string;
  value: string;
  onChange: (value: string) => void;
  options: string[];
  placeholder?: string;
  disabled?: boolean;
  id?: string;
}
export function Dropdown({ label, value, onChange, options, placeholder, disabled, id }: DropdownProps): JSX.Element;
```

From src/components/ui/TextArea.tsx (기존, 변경 없음):
```typescript
interface TextAreaProps {
  label: string;
  value: string;
  onChange: (value: string) => void;
  placeholder?: string;
  rows?: number;
  disabled?: boolean;
  id?: string;
}
export function TextArea({ label, value, onChange, placeholder, rows, disabled, id }: TextAreaProps): JSX.Element;
```

From src/services/gemini.ts (수정 대상 -- 핵심 부분):
```typescript
const FIELD_LABELS: Record<string, string> = {
  category: '업종',  // 이 키를 제거하고 industry 별도 처리
  location: '위치',
  // ...
};

function buildInputSummary(form: FormState): { text: string; filledFields: string[] } {
  // Object.entries(section.data).filter(([, v]) => v.trim() !== '')
  // ^^^ category가 IndustrySelection 객체가 되면 v.trim() 에서 TypeError 발생
  // industry를 별도 처리해야 함
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StoreBasicSection 4단 계층형 드롭다운 UI 구현</name>
  <files>src/components/sections/StoreBasicSection.tsx</files>
  <read_first>
    - src/components/sections/StoreBasicSection.tsx (현재 전체 코드)
    - src/components/ui/Dropdown.tsx (DropdownProps 인터페이스 -- label, value, onChange, options, disabled)
    - src/components/ui/TextArea.tsx (TextAreaProps 인터페이스 -- label, value, onChange, placeholder, rows, disabled)
    - src/data/industryData.ts (Plan 01에서 생성한 헬퍼 함수 시그니처)
    - src/store/useFormStore.ts (Plan 01에서 추가한 updateIndustry 액션)
    - .planning/phases/04-industry-dropdown/04-UI-SPEC.md (Layout Contract, Interaction States, Cascading Reset)
  </read_first>
  <action>
src/components/sections/StoreBasicSection.tsx를 전체 재작성한다.

**제거할 것:**
- `const CATEGORY_OPTIONS = [...]` 배열 전체 삭제
- `u('category', v)` 호출 삭제

**추가할 import:**
```typescript
import { getMajorCategories, getMediumCategories, getMinorCategories } from '../../data/industryData';
```

**스토어 selector 변경:**
```typescript
const s = useFormStore((st) => st.storeBasic);
const updateField = useFormStore((st) => st.updateStoreBasic);
const industry = useFormStore((st) => st.storeBasic.industry);
const updateIndustry = useFormStore((st) => st.updateIndustry);
```

**hasInput 로직 수정** (Pitfall 1 방지 -- industry는 객체이므로 .trim() 불가):
```typescript
const hasInput =
  industry.major !== '' ||
  Object.entries(s)
    .filter(([k]) => k !== 'industry')
    .some(([, v]) => typeof v === 'string' && v.trim() !== '');
```

**계층형 옵션 계산:**
```typescript
const majorOptions = getMajorCategories();
const mediumOptions = industry.major ? getMediumCategories(industry.major) : [];
const minorOptions = industry.major && industry.medium
  ? getMinorCategories(industry.major, industry.medium) : [];
```

**Cascading reset 핸들러 (per D-06):**
```typescript
function handleMajorChange(major: string) {
  updateIndustry({ major, medium: '', minor: '', note: '' });
}
function handleMediumChange(medium: string) {
  updateIndustry({ ...industry, medium, minor: '', note: '' });
}
function handleMinorChange(minor: string) {
  updateIndustry({ ...industry, minor });
}
function handleNoteChange(note: string) {
  updateIndustry({ ...industry, note });
}
```

**JSX 레이아웃 -- 2x2 grid + 나머지 필드 (per D-04, D-05, UI-SPEC Layout Contract):**
```tsx
<section className="rounded-2xl bg-[#E8E4DE] p-7 border border-[#C5BFB7]">
  <SectionHeader title="매장 기본" />
  <div className="space-y-4">
    {/* 업종 선택 -- 2x2 grid (D-04) */}
    <div className="grid grid-cols-2 gap-4">
      <Dropdown label="대분류" value={industry.major}
        onChange={handleMajorChange} options={majorOptions} />
      <Dropdown label="중분류" value={industry.medium}
        onChange={handleMediumChange} options={mediumOptions}
        disabled={!industry.major} />
    </div>
    <div className="grid grid-cols-2 gap-4">
      <Dropdown label="소분류" value={industry.minor}
        onChange={handleMinorChange} options={minorOptions}
        disabled={!industry.medium} />
      <TextArea label="비고" value={industry.note}
        onChange={handleNoteChange}
        placeholder="예: 떡볶이 전문, 고양이카페"
        rows={1} disabled={!industry.minor} />
    </div>
    {/* 나머지 필드 -- 기존 레이아웃 유지 */}
    <div className="grid grid-cols-2 gap-4">
      <Dropdown label="규모" value={s.scale}
        onChange={(v) => updateField('scale', v)} options={SCALE_OPTIONS} />
      <Dropdown label="가격대" value={s.priceRange}
        onChange={(v) => updateField('priceRange', v)} options={PRICE_RANGE_OPTIONS} />
    </div>
    <TextArea label="주력 상품/서비스" value={s.mainProduct}
      onChange={(v) => updateField('mainProduct', v)}
      placeholder="수제 드립커피, 시그니처 디저트" rows={2} />
  </div>
  <div className="mt-6 flex justify-end">
    <RecommendButton onClick={recommend} loading={isLoading} disabled={!hasInput} />
  </div>
</section>
```

**SCALE_OPTIONS, PRICE_RANGE_OPTIONS는 기존과 동일하게 유지한다:**
```typescript
const SCALE_OPTIONS = ['1인 창업','소형 (5인 미만)','중형 (5~20인)','대형 (20인 이상)','프랜차이즈'];
const PRICE_RANGE_OPTIONS = ['저가','중저가','중가','중고가','고가','프리미엄'];
```

**D-07 준수:** 상위 미선택 시 하위 드롭다운은 `disabled` prop으로 비활성화 -- 숨기지 않고 항상 표시.
**D-03 준수:** 네이티브 `<select>` 사용 (기존 Dropdown 컴포넌트 재사용), 검색/필터 없음.
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CATEGORY_OPTIONS 배열이 StoreBasicSection.tsx에 존재하지 않는다
    - `import { getMajorCategories, getMediumCategories, getMinorCategories }` import가 있다
    - `useFormStore((st) => st.updateIndustry)` 호출이 있다
    - `<Dropdown label="대분류"` 가 있다
    - `<Dropdown label="중분류"` 가 있고 `disabled={!industry.major}` 속성이 있다
    - `<Dropdown label="소분류"` 가 있고 `disabled={!industry.medium}` 속성이 있다
    - `<TextArea label="비고"` 가 있고 `disabled={!industry.minor}` 속성이 있다
    - `grid grid-cols-2 gap-4` 클래스가 2회 이상 사용된다 (2x2 grid)
    - handleMajorChange에서 medium, minor, note가 모두 빈 문자열로 초기화된다
    - handleMediumChange에서 minor, note가 빈 문자열로 초기화된다
    - hasInput 로직에서 industry.major를 별도로 체크한다 (Object.values + trim 단일 루프가 아님)
    - 파일이 500줄 이하이다
  </acceptance_criteria>
  <done>StoreBasicSection이 4단 계층형 UI를 2x2 grid로 표시하고, cascading reset이 D-06에 따라 동작하며, disabled 상태가 D-07에 따라 표시됨.</done>
</task>

<task type="auto">
  <name>Task 2: gemini.ts buildInputSummary 수정 + formatIndustryPath 함수</name>
  <files>src/services/gemini.ts</files>
  <read_first>
    - src/services/gemini.ts (현재 전체 코드 -- buildInputSummary, FIELD_LABELS)
    - src/types/form.ts (Plan 01에서 변경한 IndustrySelection, StoreBasicState)
    - .planning/phases/04-industry-dropdown/04-RESEARCH.md (Example 2: 업종 경로 포맷, Pitfall 5: buildInputSummary 수정)
  </read_first>
  <action>
src/services/gemini.ts를 수정한다.

**1. FIELD_LABELS에서 `category: '업종'` 제거:**
```typescript
const FIELD_LABELS: Record<string, string> = {
  // category 키 삭제 -- industry는 별도 처리
  location: '위치',
  scale: '매장규모',
  mainProduct: '주력상품',
  priceRange: '가격대',
  targetCustomer: '타겟고객',
  // ... 나머지 동일
};
```

**2. formatIndustryPath 함수 추가 (per D-08):**
buildInputSummary 함수 위에 추가:
```typescript
import type { IndustrySelection } from '../types/form';
// (기존 import에 IndustrySelection 추가)

/** 업종 선택 경로를 AI 프롬프트용 문자열로 포맷 (D-08) */
function formatIndustryPath(ind: IndustrySelection): string {
  const parts = [ind.major, ind.medium, ind.minor].filter(Boolean);
  if (parts.length === 0) return '';
  let result = parts.join(' > ');
  if (ind.note) result += ` (비고: ${ind.note})`;
  return result;
}
```

**3. buildInputSummary에서 storeBasic 처리 수정 (Pitfall 1, 5 방지):**

기존 코드:
```typescript
for (const section of sections) {
  const entries = Object.entries(section.data).filter(([, v]) => v.trim() !== '');
```

이 코드에서 storeBasic.industry가 IndustrySelection 객체이므로 `v.trim()`이 TypeError를 발생시킨다.

수정: storeBasic을 별도 처리하여 industry를 먼저 포맷하고, 나머지 string 필드만 Object.entries로 순회한다.

```typescript
function buildInputSummary(form: FormState): { text: string; filledFields: string[] } {
  const filledFields: string[] = [];
  const lines: string[] = [];

  // 매장 기본 -- industry 별도 처리 (D-08, D-09)
  const industryPath = formatIndustryPath(form.storeBasic.industry);
  const basicEntries = Object.entries(form.storeBasic)
    .filter(([k, v]) => k !== 'industry' && typeof v === 'string' && v.trim() !== '');

  if (industryPath || basicEntries.length > 0) {
    lines.push('\n## 매장 기본/환경');
    if (industryPath) {
      lines.push(`- 업종: ${industryPath}`);
      filledFields.push('업종');
    }
    for (const [key, value] of basicEntries) {
      const label = FIELD_LABELS[key] ?? key;
      lines.push(`- ${label}: ${value}`);
      filledFields.push(label);
    }
  }

  // 나머지 섹션은 모든 필드가 string이므로 기존 로직 유지
  const otherSections = [
    { title: '브랜드 정체성 및 비전', data: form.brandVision },
    { title: '브랜드 페르소나', data: form.persona },
  ];

  for (const section of otherSections) {
    const entries = Object.entries(section.data).filter(([, v]) => (v as string).trim() !== '');
    if (entries.length === 0) continue;

    lines.push(`\n## ${section.title}`);
    for (const [key, value] of entries) {
      const label = FIELD_LABELS[key] ?? key;
      lines.push(`- ${label}: ${value}`);
      filledFields.push(label);
    }
  }

  return { text: lines.join('\n'), filledFields };
}
```

**sections 배열에서 storeBasic을 제거하고 별도 처리하는 것이 핵심이다.** brandVision, persona는 모든 필드가 string이므로 기존 패턴 유지.

나머지 코드(SYSTEM_PROMPT, USER_PROMPT, generateBrandNames 등)는 변경하지 않는다.
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - FIELD_LABELS에 `category` 키가 없다
    - `function formatIndustryPath(ind: IndustrySelection): string` 함수가 존재한다
    - formatIndustryPath가 `parts.join(' > ')` 패턴을 사용한다
    - formatIndustryPath가 note가 있을 때 `(비고: ${ind.note})` 를 추가한다
    - buildInputSummary에서 `form.storeBasic.industry`를 별도 처리한다 (Object.entries 루프에 포함되지 않음)
    - buildInputSummary에서 industry 경로가 있으면 `- 업종: {경로}` 형태로 lines에 추가된다
    - `Object.entries(form.storeBasic).filter(([k, v]) => k !== 'industry'` 패턴으로 industry를 건너뛴다
    - IndustrySelection이 import되어 있다
    - 파일이 500줄 이하이다
  </acceptance_criteria>
  <done>AI 프롬프트에 업종 경로가 "음식 > 한식 > 분식 (비고: 떡볶이 전문)" 형태로 반영되고, Object.entries+trim() TypeError가 방지됨.</done>
</task>

<task type="auto">
  <name>Task 3: SectionHeader font-bold -> font-semibold + 빌드 검증</name>
  <files>src/components/ui/SectionHeader.tsx</files>
  <read_first>
    - src/components/ui/SectionHeader.tsx (현재 코드 -- font-bold 확인)
    - .planning/phases/04-industry-dropdown/04-UI-SPEC.md (Typography 섹션 -- Heading: 600 SemiBold)
  </read_first>
  <action>
src/components/ui/SectionHeader.tsx에서 `font-bold`를 `font-semibold`로 변경한다 (UI-SPEC Typography 계약).

변경 전:
```typescript
<h2 className="text-[14px] font-bold text-[#2C2825]">{title}</h2>
```

변경 후:
```typescript
<h2 className="text-[14px] font-semibold text-[#2C2825]">{title}</h2>
```

이것이 이 파일의 유일한 변경이다. 14px에서 600(semibold)과 700(bold)의 시각적 차이는 미미하며, Label(12px/600)과 일관된 weight 체계를 유지한다.

변경 후 전체 빌드 검증을 수행한다.
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SectionHeader.tsx에 `font-semibold` 가 있고 `font-bold`가 없다
    - `npx tsc --noEmit` 이 에러 0개로 통과한다
    - `npx vite build` 가 성공적으로 완료된다
    - 파일이 500줄 이하이다
  </acceptance_criteria>
  <done>SectionHeader가 UI-SPEC Typography 계약에 맞게 font-semibold를 사용하고, 전체 앱이 에러 없이 빌드됨.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 계층형 드롭다운 + AI 추천 통합 검증</name>
  <files>src/components/sections/StoreBasicSection.tsx</files>
  <action>
개발 서버를 시작하고 사용자에게 4단 계층형 드롭다운의 시각적/기능적 검증을 요청한다.
`npx vite dev` 실행 후 브라우저에서 확인할 수 있도록 URL을 안내한다.

검증 대상:
- 2x2 grid 레이아웃 (대분류|중분류, 소분류|비고)
- 상위 변경 시 하위 자동 초기화 (D-06)
- 상위 미선택 시 하위 disabled + opacity-40 (D-07)
- AI 프롬프트에 업종 경로 반영 (D-08)
- SectionHeader font-semibold 적용
  </action>
  <verify>
    <automated>cd C:/BRAND && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 사용자가 "approved" 응답을 제공한다
  </acceptance_criteria>
  <done>사용자가 계층형 드롭다운 UI와 AI 추천 통합을 시각적/기능적으로 승인함.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 에러 0개
- `npx vite build` 성공
- 브라우저에서 대분류 > 중분류 > 소분류 cascading 선택이 동작
- 대분류 변경 시 하위 전체 초기화
- AI 추천 시 에러 없이 카드 생성
- 모든 수정 파일 500줄 이하
</verification>

<success_criteria>
1. 사용자가 대분류 선택 시 해당 중분류 목록이 표시된다 (INDUSTRY-01)
2. 중분류 선택 시 해당 소분류 목록이 표시된다 (INDUSTRY-01)
3. 대분류 변경 시 중/소분류/비고가 초기화된다 (D-06)
4. 상위 미선택 시 하위가 disabled 상태이다 (D-07)
5. AI 프롬프트에 업종 경로가 "대분류 > 중분류 > 소분류 (비고: xxx)" 형태로 반영된다 (D-08)
6. `npx vite build` 성공
7. 사용자가 시각적/기능적으로 승인
</success_criteria>

<output>
After completion, create `.planning/phases/04-industry-dropdown/04-02-SUMMARY.md`
</output>
