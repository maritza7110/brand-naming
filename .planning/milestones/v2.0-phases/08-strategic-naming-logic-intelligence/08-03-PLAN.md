---
phase: 08-strategic-naming-logic-intelligence
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/services/gemini.ts
  - src/hooks/useRecommend.ts
autonomous: true
requirements:
  - "Gemini 프롬프트 엔지니어링 고도화"
  - "논리적 타당성 피드백"
  - "키워드 가중치 반영"

must_haves:
  truths:
    - "Gemini API가 구조화 응답(responseSchema)으로 RationaleData를 포함한 JSON을 반환한다"
    - "프롬프트에 경쟁사/USP 데이터가 차별화 지침으로 포함된다"
    - "키워드 가중치가 자연어로 변환되어 프롬프트에 반영된다"
    - "구조화 응답 실패 시 기존 regex 파싱으로 fallback한다"
    - "useRecommend 훅이 확장된 FormState의 모든 필드를 수집하여 전달한다"
  artifacts:
    - path: "src/services/gemini.ts"
      provides: "고도화된 프롬프트 + 구조화 응답 + fallback"
      contains: "responseMimeType"
    - path: "src/hooks/useRecommend.ts"
      provides: "확장된 폼 데이터 수집 + 키워드 가중치 전달"
      contains: "keywordWeights"
  key_links:
    - from: "src/services/gemini.ts"
      to: "src/types/form.ts"
      via: "import FormState, RationaleData"
      pattern: "RationaleData"
    - from: "src/hooks/useRecommend.ts"
      to: "src/services/gemini.ts"
      via: "generateBrandNames 호출"
      pattern: "generateBrandNames"
---

<objective>
Gemini 프롬프트를 논리적 프레임워크 기반으로 고도화하고, 구조화 응답(responseSchema)을 적용하며, 키워드 가중치를 반영한다.

Purpose: AI 추천의 질을 높이고, 각 이름에 대한 논리적 근거(RationaleData)를 안정적으로 추출한다.
Output: 고도화된 gemini.ts + 확장된 useRecommend.ts
</objective>

<execution_context>
@C:/Users/고상진/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/고상진/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-CONTEXT.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-RESEARCH.md
@.planning/phases/08-strategic-naming-logic-intelligence/08-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 확장된 타입 -->
From src/types/form.ts:
```typescript
export interface AnalysisState { competitors: string; usp: string; }
export interface IdentityState { marketTrend: string; brandPersonality: string[]; }
export interface ExpressionState { namingStyle: string[]; languageConstraint: string; }
export type KeywordWeights = Record<string, number>;
export interface RationaleData {
  validityScore: number;
  namingTechnique: string;
  meaningAnalysis: string;
  reflectedInputs: string[];
}
export interface FormState {
  storeBasic: StoreBasicState;
  brandVision: BrandVisionState;
  product: ProductState;
  persona: PersonaState;
  analysis: AnalysisState;
  identity: IdentityState;
  expression: ExpressionState;
}
export interface RecommendBatch {
  id: string;
  names: { brandName: string; reasoning: string; rationale?: RationaleData }[];
  basedOn: string[];
  createdAt: Date;
  industry?: IndustrySelection;
}
```

From src/store/useFormStore.ts:
```typescript
// AppState에 keywordWeights: KeywordWeights 추가됨
// getState()로 접근 가능
```

From src/services/gemini.ts (현재):
```typescript
// SYSTEM_PROMPT: 신병철 마케팅 프레임워크 기반 지침
// USER_PROMPT: {INPUT}{DOCS}{EXCLUDE} 템플릿
// generateBrandNames(form: FormState): Promise<RecommendBatch>
// buildInputSummary(form: FormState): { text, filledFields }
// buildExcludeList(): string
// buildDocumentContext(): string
// FIELD_LABELS: Record<string, string>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gemini 프롬프트 고도화 + 구조화 응답 (gemini.ts)</name>
  <files>src/services/gemini.ts</files>
  <read_first>
    - src/services/gemini.ts (현재 전체 코드)
    - src/types/form.ts (Plan 01 이후 확장된 타입)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-RESEARCH.md (Pattern 2: 구조화 응답, Pitfall 3: 실패 fallback, Pitfall 4: 가중치 반영, 코드 예제)
    - .planning/phases/08-strategic-naming-logic-intelligence/08-CONTEXT.md (D-04, D-05, D-06, D-08)
    - node_modules/@google/genai/dist/genai.d.ts (responseSchema 타입 확인)
  </read_first>
  <action>
src/services/gemini.ts를 고도화한다. 기존 구조를 유지하면서 확장:

**1. FIELD_LABELS 확장** — 신규 필드 라벨 추가:
```typescript
competitors: '경쟁사',
usp: 'USP',
marketTrend: '시장트렌드',
brandPersonality: '브랜드퍼스널리티',
namingStyle: '네이밍스타일',
languageConstraint: '언어제약',
```

**2. buildInputSummary 확장** — FormState의 신규 필드를 반영:
기존 섹션(storeBasic, brandVision, persona) 이후에 신규 섹션 추가:
```typescript
// 분석 섹션 (신규)
const analysisEntries = Object.entries(form.analysis)
  .filter(([, v]) => typeof v === 'string' && v.trim() !== '');
if (analysisEntries.length > 0) {
  lines.push('\n## 분석 (경쟁/차별화)');
  for (const [key, value] of analysisEntries) {
    const label = FIELD_LABELS[key] ?? key;
    lines.push(`- ${label}: ${value as string}`);
    filledFields.push(label);
  }
}

// 정체성 섹션 (신규)
// marketTrend: 텍스트
if (form.identity.marketTrend.trim()) {
  lines.push('\n## 시장 트렌드');
  lines.push(`- 시장트렌드: ${form.identity.marketTrend}`);
  filledFields.push('시장트렌드');
}
// brandPersonality: 배열
if (form.identity.brandPersonality.length > 0) {
  lines.push(`- 브랜드퍼스널리티: ${form.identity.brandPersonality.join(', ')}`);
  filledFields.push('브랜드퍼스널리티');
}

// 표현 섹션 (신규, 고급 옵션)
const expressionParts: string[] = [];
if (form.expression.namingStyle.length > 0) {
  expressionParts.push(`네이밍스타일: ${form.expression.namingStyle.join(', ')}`);
  filledFields.push('네이밍스타일');
}
if (form.expression.languageConstraint.trim()) {
  expressionParts.push(`언어제약: ${form.expression.languageConstraint}`);
  filledFields.push('언어제약');
}
if (expressionParts.length > 0) {
  lines.push('\n## 표현 (네이밍 스타일)');
  expressionParts.forEach(p => lines.push(`- ${p}`));
}
```

**3. 경쟁사/USP 차별화 지침 함수 추가** (per D-06, Claude 재량):
```typescript
function buildDifferentiationContext(form: FormState): string {
  const parts: string[] = [];
  if (form.analysis.competitors.trim()) {
    parts.push(`[경쟁 환경]\n${form.analysis.competitors}`);
    parts.push('위 경쟁사와 명확히 차별화되는 이름을 만드세요. 비슷한 어감/의미는 피하세요.');
  }
  if (form.analysis.usp.trim()) {
    parts.push(`[핵심 차별점 (USP)]\n${form.analysis.usp}`);
    parts.push('이 차별점이 이름에서 직관적으로 느껴져야 합니다.');
  }
  return parts.length > 0 ? '\n\n' + parts.join('\n') : '';
}
```

**4. 키워드 가중치 -> 프롬프트 자연어 변환 함수 추가** (per D-05):
```typescript
const WEIGHT_LABELS: Record<number, string> = {
  1: '참고 수준',
  2: '보통',
  3: '중요',
  4: '매우 중요',
  5: '최우선 반영',
};

function buildWeightedKeywords(weights: Record<string, number>): string {
  const entries = Object.entries(weights).filter(([, w]) => w !== 3); // 기본값(3)은 생략
  if (entries.length === 0) return '';
  const sorted = entries.sort(([, a], [, b]) => b - a);
  const lines = sorted.map(([keyword, weight]) =>
    `- ${keyword}: ${WEIGHT_LABELS[weight]} (${weight}/5)`
  );
  return `\n\n[키워드 우선순위 — 높은 순서대로 반영하세요]\n${lines.join('\n')}`;
}
```

**5. 타당성 점수 산출 지침을 SYSTEM_PROMPT에 추가:**
기존 SYSTEM_PROMPT 끝에 추가:
```
[타당성 점수 산출 기준]
- 입력된 키워드/정보와 브랜드명의 의미적 연관성 (40%)
- 네이밍 기법의 적절성 (20%)
- 발음 용이성 및 기억성 (20%)
- 업종 특성 반영도 (20%)
각 이름에 0~100 점수를 부여하세요.
```

**6. USER_PROMPT 수정 — 신규 플레이스홀더 추가:**
```typescript
const USER_PROMPT = `아래 사업 정보를 바탕으로 브랜드명을 추천해주세요.

[입력된 정보]
{INPUT}{DIFFERENTIATION}{WEIGHTS}{DOCS}{EXCLUDE}

정확히 2개를 추천하세요.`;
```
JSON 형식 지시는 제거 (responseSchema가 강제하므로).

**7. generateBrandNames 함수 수정:**

7a. 함수 시그니처에 keywordWeights 파라미터 추가:
```typescript
export async function generateBrandNames(
  form: FormState,
  keywordWeights?: Record<string, number>
): Promise<RecommendBatch>
```

7b. 프롬프트 조립에 신규 컨텍스트 포함:
```typescript
const diffContext = buildDifferentiationContext(form);
const weightContext = keywordWeights ? buildWeightedKeywords(keywordWeights) : '';
const prompt = USER_PROMPT
  .replace('{INPUT}', inputSummary)
  .replace('{DIFFERENTIATION}', diffContext)
  .replace('{WEIGHTS}', weightContext)
  .replace('{DOCS}', docContext)
  .replace('{EXCLUDE}', excludeContext);
```

7c. Gemini API 호출을 구조화 응답으로 변경:
```typescript
const response = await ai.models.generateContent({
  model: 'gemini-3.1-flash-lite-preview',
  contents: [{ role: 'user', parts: [{ text: prompt }] }],
  config: {
    systemInstruction: SYSTEM_PROMPT,
    responseMimeType: 'application/json',
    responseSchema: {
      type: 'array',
      items: {
        type: 'object',
        properties: {
          brandName: { type: 'string', description: '추천 브랜드명 (한글)' },
          reasoning: { type: 'string', description: '추천 이유 (한 문장)' },
          validityScore: { type: 'number', description: '타당성 점수 (0~100)' },
          namingTechnique: { type: 'string', description: '사용된 네이밍 기법' },
          meaningAnalysis: { type: 'string', description: '의미 분석 설명' },
          reflectedInputs: {
            type: 'array',
            items: { type: 'string' },
            description: '반영된 입력 항목명 리스트',
          },
        },
        required: ['brandName', 'reasoning', 'validityScore', 'namingTechnique', 'meaningAnalysis', 'reflectedInputs'],
      },
    },
  },
});
```

7d. 응답 파싱을 구조화 응답 우선 + regex fallback:
```typescript
const raw = response.text ?? '';
let parsed: GeminiStructuredResult[];

try {
  // 구조화 응답: 직접 JSON 파싱
  parsed = JSON.parse(raw);
} catch {
  // fallback: 기존 regex 파싱
  const jsonMatch = raw.match(/\[[\s\S]*\]/);
  if (!jsonMatch) {
    throw new Error('AI 응답을 파싱할 수 없습니다. 다시 시도해주세요.');
  }
  parsed = JSON.parse(jsonMatch[0]);
}
```

7e. GeminiStructuredResult 인터페이스 업데이트:
```typescript
interface GeminiStructuredResult {
  brandName: string;
  reasoning: string;
  validityScore?: number;
  namingTechnique?: string;
  meaningAnalysis?: string;
  reflectedInputs?: string[];
}
```

7f. RecommendBatch 생성 시 rationale 매핑:
```typescript
names: parsed.slice(0, 2).map((item) => ({
  brandName: item.brandName,
  reasoning: item.reasoning,
  rationale: item.validityScore != null ? {
    validityScore: Math.min(100, Math.max(0, item.validityScore)),
    namingTechnique: item.namingTechnique ?? '분석 중',
    meaningAnalysis: item.meaningAnalysis ?? '',
    reflectedInputs: item.reflectedInputs ?? [],
  } : undefined,
})),
```

파일 500줄 제한 유지. 현재 ~228줄 -> 확장 후 ~400줄 예상.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - gemini.ts에 `responseMimeType: 'application/json'` 존재
    - gemini.ts에 `responseSchema` 객체 존재
    - gemini.ts에 `validityScore` 가 responseSchema properties에 존재
    - gemini.ts에 `buildDifferentiationContext` 함수 존재
    - gemini.ts에 `buildWeightedKeywords` 함수 존재
    - gemini.ts에 WEIGHT_LABELS 객체 존재 ('참고 수준', '최우선 반영' 등)
    - gemini.ts에 `타당성 점수 산출 기준` 텍스트가 SYSTEM_PROMPT에 존재
    - gemini.ts에 `keywordWeights` 파라미터가 generateBrandNames에 존재
    - gemini.ts에 regex fallback 로직 존재 (`raw.match`)
    - gemini.ts에 `Math.min(100, Math.max(0,` 범위 검증 존재
    - gemini.ts에 rationale 매핑 로직 존재
    - gemini.ts에 `systemInstruction` 설정 존재 (config 내부)
    - 파일이 500줄 이하
  </acceptance_criteria>
  <done>Gemini 프롬프트가 고도화되고, 구조화 응답으로 RationaleData를 추출하며, fallback이 동작함</done>
</task>

<task type="auto">
  <name>Task 2: useRecommend 훅 확장</name>
  <files>src/hooks/useRecommend.ts</files>
  <read_first>
    - src/hooks/useRecommend.ts (현재 코드)
    - src/services/gemini.ts (Task 1 이후 — generateBrandNames 시그니처 변경)
    - src/store/useFormStore.ts (Plan 01 이후 — keywordWeights 상태)
  </read_first>
  <action>
src/hooks/useRecommend.ts를 확장하여 신규 폼 필드와 키워드 가중치를 수집한다:

1. recommend 함수에서 getState()로 신규 필드 포함:
```typescript
const recommend = useCallback(async () => {
  const { storeBasic, brandVision, product, persona, analysis, identity, expression, keywordWeights } = useFormStore.getState();
  const form = { storeBasic, brandVision, product, persona, analysis, identity, expression };

  setLoading(true);
  setError(null);
  try {
    const batch = await generateBrandNames(form, keywordWeights);
    addBatch(batch);
  } catch (err) {
    const message = err instanceof Error ? err.message : 'AI 추천에 실패했습니다.';
    setError(message);
  } finally {
    setLoading(false);
  }
}, [setLoading, setError, addBatch]);
```

변경사항:
- form 객체에 analysis, identity, expression 추가
- generateBrandNames 호출 시 keywordWeights 전달
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - useRecommend.ts에 `analysis, identity, expression` 구조 분해 존재
    - useRecommend.ts에 `keywordWeights` 구조 분해 존재
    - useRecommend.ts에 `generateBrandNames(form, keywordWeights)` 호출 존재
    - form 객체에 analysis, identity, expression 필드 포함
    - 파일이 500줄 이하
  </acceptance_criteria>
  <done>useRecommend가 확장된 FormState 전체와 키워드 가중치를 generateBrandNames에 전달함</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 전체 통과
- gemini.ts에 구조화 응답 설정 존재
- useRecommend.ts에서 확장된 폼 데이터 + 키워드 가중치 전달
</verification>

<success_criteria>
- Gemini API 호출 시 responseSchema로 rationale 데이터를 포함한 구조화 응답을 요청
- 경쟁사/USP/키워드 가중치가 프롬프트에 자연어로 반영
- 구조화 응답 실패 시 기존 regex fallback으로 안전하게 처리
- useRecommend가 모든 신규 필드를 수집하여 전달
- 모든 파일 500줄 이하
</success_criteria>

<output>
After completion, create `.planning/phases/08-strategic-naming-logic-intelligence/08-03-SUMMARY.md`
</output>
