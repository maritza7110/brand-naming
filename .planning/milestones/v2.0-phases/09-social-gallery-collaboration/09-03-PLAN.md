---
phase: 09-social-gallery-collaboration
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/pages/GalleryPage.tsx
  - src/components/gallery/GalleryGrid.tsx
  - src/components/gallery/GalleryCard.tsx
  - src/components/gallery/GallerySortTabs.tsx
  - src/components/gallery/GallerySkeletonCard.tsx
  - src/App.tsx
autonomous: true
requirements: [SOCIAL-GALLERY, SOCIAL-PUBLISH]

must_haves:
  truths:
    - "/gallery 라우트에 접속하면 공개 세션 카드 그리드가 표시된다"
    - "최신순/인기순 탭으로 정렬을 전환할 수 있다"
    - "스크롤 하단 도달 시 다음 페이지가 자동 로드된다"
    - "로딩 중에는 스켈레톤 카드가 표시된다"
    - "공개 세션이 없으면 빈 상태 메시지가 표시된다"
  artifacts:
    - path: "src/pages/GalleryPage.tsx"
      provides: "/gallery 진입점 페이지"
      min_lines: 30
    - path: "src/components/gallery/GalleryGrid.tsx"
      provides: "반응형 그리드 + 무한 스크롤 sentinel"
      contains: "sentinelRef"
    - path: "src/components/gallery/GalleryCard.tsx"
      provides: "공개 세션 요약 카드"
      contains: "GalleryCard"
    - path: "src/components/gallery/GallerySortTabs.tsx"
      provides: "최신순/인기순 정렬 탭"
      contains: "최신순"
    - path: "src/App.tsx"
      provides: "/gallery 라우트 등록"
      contains: "/gallery"
  key_links:
    - from: "src/pages/GalleryPage.tsx"
      to: "src/store/useGalleryStore.ts"
      via: "useGalleryStore()"
      pattern: "useGalleryStore"
    - from: "src/components/gallery/GalleryGrid.tsx"
      to: "src/hooks/useInfiniteScroll.ts"
      via: "useInfiniteScroll()"
      pattern: "useInfiniteScroll"
    - from: "src/App.tsx"
      to: "src/pages/GalleryPage.tsx"
      via: "Route path='/gallery'"
      pattern: "/gallery"
---

<objective>
갤러리 페이지 UI — 카드 그리드, 정렬 탭, 무한 스크롤, 스켈레톤

Purpose: 타 사용자의 공개 네이밍 프로젝트를 탐색할 수 있는 갤러리 피드를 구축한다. per D-01, D-02, D-10, D-11.
Output: GalleryPage, GalleryGrid, GalleryCard, GallerySortTabs, GallerySkeletonCard + App.tsx 라우트 등록
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-social-gallery-collaboration/09-CONTEXT.md
@.planning/phases/09-social-gallery-collaboration/09-UI-SPEC.md
@.planning/phases/09-social-gallery-collaboration/09-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 인터페이스 — executor가 import할 것 -->

From src/types/gallery.ts:
```typescript
export type GallerySortBy = 'latest' | 'popular';
export interface GallerySession {
  id: string; title: string; industry_id: string | null;
  input_data: any; created_at: string; user_id: string;
  profiles: { full_name: string | null } | null;
  naming_results: { brand_name: string; reasoning: string }[];
  like_count: number; is_bookmarked?: boolean;
}
```

From src/store/useGalleryStore.ts:
```typescript
interface GalleryState {
  sessions: GallerySession[]; page: number; sortBy: GallerySortBy;
  isLoading: boolean; hasMore: boolean; error: string | null;
  likeCounts: Record<string, number>;
  fetchNextPage: () => Promise<void>;
  setSortBy: (sortBy: GallerySortBy) => void;
  reset: () => void;
  updateLikeCount: (sessionId: string, delta: number) => void;
}
```

From src/hooks/useInfiniteScroll.ts:
```typescript
export function useInfiniteScroll(
  onLoadMore: () => void, hasMore: boolean, isLoading: boolean
): (node: HTMLElement | null) => void;
```

From src/App.tsx (current):
```typescript
<Routes>
  <Route path="/" element={<ProtectedRoute><NamingPage /></ProtectedRoute>} />
  <Route path="/login" element={<LoginPage />} />
  <Route path="/signup" element={<SignupPage />} />
  <Route path="/dashboard" element={<ProtectedRoute><Dashboard /></ProtectedRoute>} />
</Routes>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GalleryCard + GallerySkeletonCard + GallerySortTabs 컴포넌트</name>
  <files>
    src/components/gallery/GalleryCard.tsx,
    src/components/gallery/GallerySkeletonCard.tsx,
    src/components/gallery/GallerySortTabs.tsx
  </files>
  <read_first>
    src/components/dashboard/ProjectCard.tsx,
    src/components/recommend/EmptyState.tsx,
    .planning/phases/09-social-gallery-collaboration/09-UI-SPEC.md,
    src/types/gallery.ts
  </read_first>
  <action>
**1. src/components/gallery/GalleryCard.tsx 생성:**

```typescript
import { Heart } from 'lucide-react';
import type { GallerySession } from '../../types/gallery';

interface GalleryCardProps {
  session: GallerySession;
  likeCount: number;
  onClick: (session: GallerySession) => void;
}
```

**주의:** sessionService는 import하지 않는다. GalleryCard는 요약형 카드로 formatDate 등을 사용하지 않으며, 날짜 표시는 모달(Plan 04 GalleryModal)에서만 처리한다.

JSX 구조 (09-UI-SPEC GalleryCard 내부 구조 참조):
```tsx
<div
  className="bg-[#1A1A1E] border border-white/5 rounded-2xl p-4 hover:border-[#B48C50]/30 transition-all cursor-pointer overflow-hidden relative"
  onClick={() => onClick(session)}
>
  {/* Industry badge */}
  <span className="inline-block bg-[#4A4440] text-[#D0CAC2] text-[12px] px-2 py-1 rounded-full mb-3">
    {session.industry_id || '미분류'}
  </span>

  {/* Brand names — 최대 3개 표시 */}
  <div className="space-y-1 mb-3">
    {session.naming_results.slice(0, 3).map((r, i) => (
      <p key={i} className="text-[#B48C50] text-[16px] font-semibold truncate">
        {r.brand_name}
      </p>
    ))}
    {session.naming_results.length === 0 && (
      <p className="text-[#A09890] text-[14px]">브랜드명 없음</p>
    )}
  </div>

  {/* Divider */}
  <div className="border-t border-[#4A4440] mt-3 pt-3" />

  {/* Footer row — Plan 04에서 LikeButton/BookmarkButton으로 교체 예정 */}
  <div className="flex justify-between items-center">
    <span className="text-[12px] text-[#A09890] truncate max-w-[60%]">
      {session.profiles?.full_name || '익명'}
    </span>
    <div className="flex items-center gap-1 text-[#A09890]">
      <Heart size={14} />
      <span className="text-[12px]">{likeCount}</span>
    </div>
  </div>

  {/* 장식용 그라데이션 — ProjectCard 패턴 */}
  <div className="absolute top-0 right-0 w-24 h-24 bg-gradient-to-br from-[#B48C50]/5 to-transparent pointer-events-none" />
</div>
```

- naming_results가 빈 배열일 수 있으므로 fallback 처리

**2. src/components/gallery/GallerySkeletonCard.tsx 생성:**

09-UI-SPEC: `bg-[#4A4440] animate-pulse rounded-xl`

```tsx
export default function GallerySkeletonCard() {
  return (
    <div className="bg-[#1A1A1E] border border-white/5 rounded-2xl p-4 animate-pulse">
      <div className="bg-[#4A4440] rounded-full w-16 h-5 mb-3" />
      <div className="space-y-2 mb-3">
        <div className="bg-[#4A4440] rounded-lg h-5 w-3/4" />
        <div className="bg-[#4A4440] rounded-lg h-5 w-1/2" />
      </div>
      <div className="border-t border-[#4A4440] mt-3 pt-3">
        <div className="flex justify-between">
          <div className="bg-[#4A4440] rounded h-4 w-20" />
          <div className="bg-[#4A4440] rounded h-4 w-10" />
        </div>
      </div>
    </div>
  );
}
```

**3. src/components/gallery/GallerySortTabs.tsx 생성:**

09-UI-SPEC 정렬 탭 계약:

```typescript
import type { GallerySortBy } from '../../types/gallery';

interface GallerySortTabsProps {
  activeSort: GallerySortBy;
  onSortChange: (sort: GallerySortBy) => void;
}
```

탭 컨테이너: `flex gap-0 border-b border-[#4A4440]`
비활성 탭: `px-4 py-2 text-[14px] text-[#A09890] cursor-pointer hover:text-[#E8E2DA] transition-colors duration-150`
활성 탭: `px-4 py-2 text-[14px] text-[#B48C50] border-b-2 border-[#B48C50] cursor-default`

탭 라벨: `{ key: 'latest', label: '최신순' }`, `{ key: 'popular', label: '인기순' }` (09-UI-SPEC Copywriting Contract).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/gallery/GalleryCard.tsx contains `export default function GalleryCard`
    - src/components/gallery/GalleryCard.tsx contains `bg-[#1A1A1E]`
    - src/components/gallery/GalleryCard.tsx contains `hover:border-[#B48C50]/30`
    - src/components/gallery/GalleryCard.tsx contains `text-[#B48C50]` (brand name color)
    - src/components/gallery/GalleryCard.tsx contains `naming_results.slice(0, 3)`
    - src/components/gallery/GalleryCard.tsx contains `border-[#4A4440]`
    - src/components/gallery/GalleryCard.tsx does NOT contain `import { sessionService }`
    - src/components/gallery/GallerySkeletonCard.tsx contains `animate-pulse`
    - src/components/gallery/GallerySkeletonCard.tsx contains `bg-[#4A4440]`
    - src/components/gallery/GallerySortTabs.tsx contains `최신순`
    - src/components/gallery/GallerySortTabs.tsx contains `인기순`
    - src/components/gallery/GallerySortTabs.tsx contains `border-b-2 border-[#B48C50]`
    - All files under 500 lines
  </acceptance_criteria>
  <done>GalleryCard, GallerySkeletonCard, GallerySortTabs 3개 컴포넌트 생성, sessionService 미사용, 09-UI-SPEC 색상/타이포/스페이싱 계약 준수</done>
</task>

<task type="auto">
  <name>Task 2: GalleryGrid + GalleryPage + App.tsx 라우트 등록</name>
  <files>
    src/components/gallery/GalleryGrid.tsx,
    src/pages/GalleryPage.tsx,
    src/App.tsx
  </files>
  <read_first>
    src/App.tsx,
    src/pages/Dashboard.tsx,
    src/components/gallery/GalleryCard.tsx,
    src/components/gallery/GallerySortTabs.tsx,
    src/components/gallery/GallerySkeletonCard.tsx,
    src/store/useGalleryStore.ts,
    src/hooks/useInfiniteScroll.ts,
    .planning/phases/09-social-gallery-collaboration/09-UI-SPEC.md
  </read_first>
  <action>
**1. src/components/gallery/GalleryGrid.tsx 생성:**

```typescript
import type { GallerySession } from '../../types/gallery';
import GalleryCard from './GalleryCard';
import GallerySkeletonCard from './GallerySkeletonCard';
import { useInfiniteScroll } from '../../hooks/useInfiniteScroll';

interface GalleryGridProps {
  sessions: GallerySession[];
  likeCounts: Record<string, number>;
  isLoading: boolean;
  hasMore: boolean;
  onLoadMore: () => void;
  onCardClick: (session: GallerySession) => void;
}
```

JSX:
- 그리드: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 lg:gap-6` (09-UI-SPEC: gap 16px mobile, 24px desktop)
- sessions.map으로 GalleryCard 렌더. `likeCount={likeCounts[session.id] ?? 0}` 전달.
- isLoading일 때: `Array.from({ length: 3 }).map((_, i) => <GallerySkeletonCard key={i} />)` 추가
- sentinel div: `<div ref={sentinelRef} className="h-1" />` — useInfiniteScroll 반환값 연결
- `!hasMore && sessions.length > 0`일 때: `<p className="text-[14px] text-[#A09890] text-center py-8">모든 프로젝트를 불러왔습니다</p>` (09-UI-SPEC End of feed)

useInfiniteScroll 호출:
```typescript
const sentinelRef = useInfiniteScroll(onLoadMore, hasMore, isLoading);
```

**2. src/pages/GalleryPage.tsx 생성:**

```typescript
import { useEffect, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { ArrowLeft } from 'lucide-react';
import { useGalleryStore } from '../store/useGalleryStore';
import { useSocialStore } from '../store/useSocialStore';
import { useAuthStore } from '../store/useAuthStore';
import GalleryGrid from '../components/gallery/GalleryGrid';
import GallerySortTabs from '../components/gallery/GallerySortTabs';
import type { GallerySession } from '../types/gallery';
```

컴포넌트 로직:
- `useEffect` mount: `useGalleryStore.getState().reset()` 후 `fetchNextPage()` 호출.
- `useEffect` mount: user가 있으면 `useSocialStore.getState().initSocialState(user.id)` 호출.
- `useState<GallerySession | null>(null)` — 모달용 선택된 세션 (Plan 04에서 모달 구현)
- cleanup: `useGalleryStore.getState().reset()` (페이지 떠날 때 리셋).

레이아웃 (Dashboard.tsx 패턴 참조):
```tsx
<div className="min-h-screen bg-[#0F0F11] text-white">
  {/* Header — Dashboard와 동일 패턴 */}
  <header className="border-b border-white/5 bg-[#141417]/80 backdrop-blur-md sticky top-0 z-10">
    <div className="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between">
      <div className="flex items-center gap-3">
        <button onClick={() => navigate('/dashboard')} className="p-2 text-gray-400 hover:text-white transition-colors">
          <ArrowLeft size={20} />
        </button>
        <h1 className="text-[20px] font-semibold">브랜드 갤러리</h1>
      </div>
    </div>
  </header>

  <main className="max-w-7xl mx-auto px-4 py-8">
    <GallerySortTabs activeSort={sortBy} onSortChange={setSortBy} />
    <div className="mt-6">
      {error ? (
        <p className="text-[14px] text-red-400 text-center py-8">
          갤러리를 불러오지 못했습니다. 새로고침 후 다시 시도해 주세요.
        </p>
      ) : sessions.length === 0 && !isLoading ? (
        <div className="flex flex-col items-center justify-center py-20 text-center">
          <p className="text-[#A09890] mb-2">아직 공개된 프로젝트가 없습니다</p>
          <p className="text-[#B48C50] text-sm">첫 번째 발행자가 되어보세요. 내 프로젝트에서 '갤러리에 발행' 버튼을 눌러보세요.</p>
        </div>
      ) : (
        <GalleryGrid
          sessions={sessions}
          likeCounts={likeCounts}
          isLoading={isLoading}
          hasMore={hasMore}
          onLoadMore={fetchNextPage}
          onCardClick={(s) => setSelectedSession(s)}
        />
      )}
    </div>

    {/* GalleryModal은 Plan 04에서 구현 — selectedSession 상태만 준비 */}
    {/* {selectedSession && <GalleryModal session={selectedSession} onClose={() => setSelectedSession(null)} />} */}
  </main>
</div>
```

**3. src/App.tsx — /gallery 라우트 추가:**

import 추가:
```typescript
import GalleryPage from './pages/GalleryPage';
```

Routes 내부에 추가 (ProtectedRoute로 감싸기):
```tsx
<Route
  path="/gallery"
  element={
    <ProtectedRoute>
      <GalleryPage />
    </ProtectedRoute>
  }
/>
```

기존 라우트는 변경하지 않음. `/gallery` 라우트만 추가.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/gallery/GalleryGrid.tsx contains `export default function GalleryGrid`
    - src/components/gallery/GalleryGrid.tsx contains `useInfiniteScroll`
    - src/components/gallery/GalleryGrid.tsx contains `sentinelRef`
    - src/components/gallery/GalleryGrid.tsx contains `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
    - src/components/gallery/GalleryGrid.tsx contains `모든 프로젝트를 불러왔습니다`
    - src/pages/GalleryPage.tsx contains `브랜드 갤러리`
    - src/pages/GalleryPage.tsx contains `useGalleryStore`
    - src/pages/GalleryPage.tsx contains `fetchNextPage`
    - src/pages/GalleryPage.tsx contains `아직 공개된 프로젝트가 없습니다`
    - src/pages/GalleryPage.tsx contains `갤러리를 불러오지 못했습니다`
    - src/App.tsx contains `/gallery`
    - src/App.tsx contains `GalleryPage`
    - All files under 500 lines
  </acceptance_criteria>
  <done>GalleryGrid 무한 스크롤 + GalleryPage 진입점 + /gallery 라우트 등록 완성, 빈 상태/에러 상태 09-UI-SPEC 복사 일치</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 전체 프로젝트 컴파일 성공
- /gallery 라우트가 ProtectedRoute로 보호됨
- GalleryGrid가 IntersectionObserver sentinel 사용
- 모든 텍스트가 09-UI-SPEC Copywriting Contract과 일치
- 모든 파일 500줄 이하
</verification>

<success_criteria>
- /gallery 접속 시 갤러리 페이지 렌더링
- 최신순/인기순 탭 전환 작동
- 무한 스크롤 sentinel이 하단에 존재
- 빈 상태 + 에러 상태 + 로딩 스켈레톤 모두 구현
</success_criteria>

<output>
After completion, create `.planning/phases/09-social-gallery-collaboration/09-03-SUMMARY.md`
</output>
