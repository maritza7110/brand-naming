---
phase: 09-social-gallery-collaboration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/09_social_gallery.sql
  - src/types/gallery.ts
  - src/services/galleryService.ts
  - src/services/socialService.ts
  - src/store/useGalleryStore.ts
  - src/store/useSocialStore.ts
  - src/hooks/useInfiniteScroll.ts
autonomous: true
requirements: [SOCIAL-PUBLISH, SOCIAL-GALLERY, SOCIAL-LIKE, SOCIAL-BOOKMARK]

must_haves:
  truths:
    - "갤러리 타입 시스템이 GallerySession, LikeStatus, BookmarkStatus를 export한다"
    - "galleryService가 공개 세션 페이지네이션 조회 + 좋아요 카운트 배치 조회를 제공한다"
    - "socialService가 좋아요/북마크 토글 + 상태 조회를 제공한다"
    - "useGalleryStore가 페이지, 정렬, 세션 목록, hasMore, isLoading 상태를 관리한다"
    - "useSocialStore가 likedIds/bookmarkedIds Set + 낙관적 업데이트 토글을 제공한다"
    - "useInfiniteScroll 훅이 IntersectionObserver 기반 무한 스크롤 콜백을 반환한다"
  artifacts:
    - path: "src/types/gallery.ts"
      provides: "갤러리 도메인 타입 정의"
      exports: ["GallerySession", "GallerySortBy"]
    - path: "src/services/galleryService.ts"
      provides: "공개 세션 조회 서비스"
      exports: ["galleryService"]
    - path: "src/services/socialService.ts"
      provides: "좋아요/북마크 CRUD 서비스"
      exports: ["socialService"]
    - path: "src/store/useGalleryStore.ts"
      provides: "갤러리 피드 상태 관리"
      exports: ["useGalleryStore"]
    - path: "src/store/useSocialStore.ts"
      provides: "소셜 인터랙션 상태 관리"
      exports: ["useSocialStore"]
    - path: "src/hooks/useInfiniteScroll.ts"
      provides: "무한 스크롤 커스텀 훅"
      exports: ["useInfiniteScroll"]
  key_links:
    - from: "src/services/galleryService.ts"
      to: "src/services/supabase.ts"
      via: "import { supabase }"
      pattern: "supabase\\.from\\('sessions'\\)"
    - from: "src/store/useGalleryStore.ts"
      to: "src/services/galleryService.ts"
      via: "galleryService.fetchPage"
      pattern: "galleryService\\.fetchPage"
    - from: "src/store/useSocialStore.ts"
      to: "src/services/socialService.ts"
      via: "socialService.toggleLike"
      pattern: "socialService\\.toggle"
---

<objective>
Phase 9 데이터 레이어 구축 — 갤러리 타입, 서비스, 스토어, 커스텀 훅

Purpose: 갤러리 UI 컴포넌트(Plan 03, 04)가 의존하는 데이터 계약과 서비스 인터페이스를 먼저 확립한다.
Output: DB 마이그레이션 SQL, 타입 정의, 서비스 2개, Zustand 스토어 2개, IntersectionObserver 훅 1개
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-social-gallery-collaboration/09-CONTEXT.md
@.planning/phases/09-social-gallery-collaboration/09-RESEARCH.md

<interfaces>
<!-- 기존 코드베이스 패턴 — executor가 참조해야 할 인터페이스 -->

From src/services/supabase.ts:
```typescript
import { createClient } from '@supabase/supabase-js';
export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
```

From src/services/sessionService.ts:
```typescript
export interface SessionData {
  id: string;
  user_id: string;
  title: string;
  industry_id: string | null;
  input_data: any;
  status: string;
  is_public: boolean;
  created_at: string;
  updated_at: string;
}
```

From src/store/useAuthStore.ts:
```typescript
interface AuthState {
  user: User | null;
  session: Session | null;
  isLoading: boolean;
  setAuth: (user: User | null, session: Session | null) => void;
  signOut: () => Promise<void>;
}
export const useAuthStore = create<AuthState>(...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 타입 정의 + DB 마이그레이션 SQL + 서비스 레이어</name>
  <files>
    src/types/gallery.ts,
    supabase/migrations/09_social_gallery.sql,
    src/services/galleryService.ts,
    src/services/socialService.ts
  </files>
  <read_first>
    src/services/sessionService.ts,
    src/services/supabase.ts,
    src/types/form.ts,
    .planning/phases/09-social-gallery-collaboration/09-RESEARCH.md
  </read_first>
  <action>
**1. src/types/gallery.ts 생성:**

```typescript
export type GallerySortBy = 'latest' | 'popular';

export interface GallerySession {
  id: string;
  title: string;
  industry_id: string | null;
  input_data: any;
  created_at: string;
  user_id: string;
  profiles: { full_name: string | null } | null;
  naming_results: { brand_name: string; reasoning: string }[];
  like_count: number;
  is_bookmarked?: boolean;
}

export interface LikeStatus {
  sessionId: string;
  isLiked: boolean;
  count: number;
}

export interface BookmarkStatus {
  sessionId: string;
  isBookmarked: boolean;
}
```

**2. supabase/migrations/09_social_gallery.sql 생성:**

09-RESEARCH.md의 DB 스키마 설계 섹션을 그대로 복사. likes 테이블 (id, user_id, session_id, created_at, UNIQUE(user_id, session_id)), bookmarks 테이블 (동일 구조). RLS 정책: likes SELECT 전체 공개, INSERT/DELETE authenticated 본인만. bookmarks SELECT/INSERT/DELETE 본인만. 인덱스: likes_session_id_idx, likes_user_id_idx, bookmarks_user_id_idx, bookmarks_session_id_idx. 09-RESEARCH.md의 SQL을 정확히 사용할 것.

**3. src/services/galleryService.ts 생성:**

- `import { supabase } from './supabase'` (기존 패턴)
- `import type { GallerySession, GallerySortBy } from '../types/gallery'`
- `PAGE_SIZE = 12` 상수
- `galleryService` 객체 export:
  - `fetchPage(page: number, sortBy: GallerySortBy): Promise<GallerySession[]>` — `supabase.from('sessions').select('id, title, industry_id, input_data, created_at, user_id, profiles:user_id (full_name), naming_results (brand_name, reasoning)').eq('is_public', true).order(sortBy === 'popular' ? 'updated_at' : 'created_at', { ascending: false }).range(page * PAGE_SIZE, page * PAGE_SIZE + PAGE_SIZE - 1)`. 결과에 `like_count: 0` 기본값 추가.
  - `fetchLikeCounts(sessionIds: string[]): Promise<Record<string, number>>` — sessionIds가 비어있으면 빈 객체 반환. 각 sessionId별로 `supabase.from('likes').select('*', { count: 'exact', head: true }).eq('session_id', id)` 호출. `Promise.all`로 배치 처리. `{ [sessionId]: count }` 형태 반환.

**주의:** `publishSession`은 galleryService에 포함하지 않는다. 발행/철회는 sessionService.publishSession(Plan 02)에서 단독 관리하여 중복 구현을 방지한다.

**4. src/services/socialService.ts 생성:**

- `import { supabase } from './supabase'`
- `socialService` 객체 export:
  - `getMyLikedIds(userId: string): Promise<Set<string>>` — `supabase.from('likes').select('session_id').eq('user_id', userId)`. 결과를 `new Set(data.map(d => d.session_id))` 반환.
  - `getMyBookmarkedIds(userId: string): Promise<Set<string>>` — 동일 패턴 bookmarks 테이블.
  - `toggleLike(sessionId: string, userId: string, isLiked: boolean): Promise<void>` — isLiked면 `supabase.from('likes').delete().eq('session_id', sessionId).eq('user_id', userId)`, 아니면 `supabase.from('likes').insert({ session_id: sessionId, user_id: userId })`.
  - `toggleBookmark(sessionId: string, userId: string, isBookmarked: boolean): Promise<void>` — 동일 패턴 bookmarks 테이블.
  - `getMyBookmarks(userId: string): Promise` — `supabase.from('bookmarks').select('session_id, sessions:session_id (id, title, industry_id, is_public, created_at, profiles:user_id (full_name))').eq('user_id', userId).order('created_at', { ascending: false })`. per D-09.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/types/gallery.ts contains `export type GallerySortBy`
    - src/types/gallery.ts contains `export interface GallerySession`
    - src/types/gallery.ts contains `export interface LikeStatus`
    - src/types/gallery.ts contains `export interface BookmarkStatus`
    - supabase/migrations/09_social_gallery.sql contains `CREATE TABLE IF NOT EXISTS public.likes`
    - supabase/migrations/09_social_gallery.sql contains `CREATE TABLE IF NOT EXISTS public.bookmarks`
    - supabase/migrations/09_social_gallery.sql contains `UNIQUE(user_id, session_id)`
    - supabase/migrations/09_social_gallery.sql contains `ENABLE ROW LEVEL SECURITY`
    - src/services/galleryService.ts contains `export const galleryService`
    - src/services/galleryService.ts contains `fetchPage`
    - src/services/galleryService.ts contains `fetchLikeCounts`
    - src/services/galleryService.ts does NOT contain `publishSession`
    - src/services/galleryService.ts contains `.eq('is_public', true)`
    - src/services/galleryService.ts contains `.range(`
    - src/services/socialService.ts contains `export const socialService`
    - src/services/socialService.ts contains `toggleLike`
    - src/services/socialService.ts contains `toggleBookmark`
    - src/services/socialService.ts contains `getMyBookmarks`
    - npx tsc --noEmit exits with 0 errors in these files
  </acceptance_criteria>
  <done>4개 파일 생성, TypeScript 컴파일 에러 없음, publishSession은 galleryService에 미포함 (sessionService에서 관리)</done>
</task>

<task type="auto">
  <name>Task 2: Zustand 스토어 2개 + useInfiniteScroll 훅</name>
  <files>
    src/store/useGalleryStore.ts,
    src/store/useSocialStore.ts,
    src/hooks/useInfiniteScroll.ts
  </files>
  <read_first>
    src/store/useFormStore.ts,
    src/store/useAuthStore.ts,
    src/services/galleryService.ts,
    src/services/socialService.ts,
    src/types/gallery.ts
  </read_first>
  <action>
**1. src/store/useGalleryStore.ts 생성:**

Zustand `create` 패턴 (useFormStore와 동일, persist 없이):

```typescript
import { create } from 'zustand';
import type { GallerySession, GallerySortBy } from '../types/gallery';
import { galleryService } from '../services/galleryService';

interface GalleryState {
  sessions: GallerySession[];
  page: number;
  sortBy: GallerySortBy;
  isLoading: boolean;
  hasMore: boolean;
  error: string | null;
  likeCounts: Record<string, number>;
  
  fetchNextPage: () => Promise<void>;
  setSortBy: (sortBy: GallerySortBy) => void;
  reset: () => void;
  updateLikeCount: (sessionId: string, delta: number) => void;
}
```

- `fetchNextPage`: `isLoading`이면 early return. `isLoading = true` 설정. `galleryService.fetchPage(page, sortBy)` 호출. 결과가 12개 미만이면 `hasMore = false`. 결과의 sessionIds로 `galleryService.fetchLikeCounts` 배치 호출. sessions에 append, likeCounts 머지, page + 1. error 시 `error` 설정 + isLoading false.
- `setSortBy`: `sortBy` 변경 + `sessions = []`, `page = 0`, `hasMore = true`, `error = null` 리셋 (Pitfall 2 방지). 리셋 후 `fetchNextPage()` 호출.
- `reset`: 모든 상태 초기값으로 리셋.
- `updateLikeCount`: `likeCounts[sessionId]`에 delta 적용 (낙관적 업데이트 연동).

**2. src/store/useSocialStore.ts 생성:**

```typescript
import { create } from 'zustand';
import { socialService } from '../services/socialService';
import { useGalleryStore } from './useGalleryStore';

interface SocialState {
  likedIds: Set<string>;
  bookmarkedIds: Set<string>;
  isInitialized: boolean;
  
  initSocialState: (userId: string) => Promise<void>;
  toggleLike: (sessionId: string, userId: string) => Promise<void>;
  toggleBookmark: (sessionId: string, userId: string) => Promise<void>;
}
```

- `initSocialState`: `socialService.getMyLikedIds(userId)`와 `socialService.getMyBookmarkedIds(userId)`를 `Promise.all`로 병렬 호출. `likedIds`, `bookmarkedIds` 설정, `isInitialized = true`.
- `toggleLike`: 낙관적 업데이트 패턴 (09-RESEARCH.md Pattern 3). `wasLiked = likedIds.has(sessionId)`. 즉시 Set 토글 + `useGalleryStore.getState().updateLikeCount(sessionId, wasLiked ? -1 : 1)`. try-catch로 `socialService.toggleLike` 호출. 실패 시 Set 롤백 + `updateLikeCount` 반대 delta.
- `toggleBookmark`: 동일 낙관적 패턴. `wasBookmarked = bookmarkedIds.has(sessionId)`. Set 토글. `socialService.toggleBookmark` 호출. 실패 시 롤백.

**주의:** Zustand `Set<string>` 상태는 `new Set(prev)` 복사 후 add/delete해서 새 참조 생성 필요 (React 리렌더링 트리거).

**3. src/hooks/useInfiniteScroll.ts 생성:**

09-RESEARCH.md Pattern 2 그대로 구현:

```typescript
import { useCallback, useRef } from 'react';

export function useInfiniteScroll(
  onLoadMore: () => void,
  hasMore: boolean,
  isLoading: boolean
) {
  const observerRef = useRef<IntersectionObserver | null>(null);

  const sentinelRef = useCallback((node: HTMLElement | null) => {
    if (isLoading) return;
    if (observerRef.current) observerRef.current.disconnect();

    observerRef.current = new IntersectionObserver((entries) => {
      if (entries[0].isIntersecting && hasMore) {
        onLoadMore();
      }
    });

    if (node) observerRef.current.observe(node);
  }, [isLoading, hasMore, onLoadMore]);

  return sentinelRef;
}
```

- 반환값은 `sentinelRef` — GalleryGrid에서 sentinel div의 ref로 사용.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/store/useGalleryStore.ts contains `export const useGalleryStore = create`
    - src/store/useGalleryStore.ts contains `fetchNextPage`
    - src/store/useGalleryStore.ts contains `setSortBy`
    - src/store/useGalleryStore.ts contains `updateLikeCount`
    - src/store/useGalleryStore.ts contains `hasMore`
    - src/store/useSocialStore.ts contains `export const useSocialStore = create`
    - src/store/useSocialStore.ts contains `likedIds`
    - src/store/useSocialStore.ts contains `bookmarkedIds`
    - src/store/useSocialStore.ts contains `toggleLike`
    - src/store/useSocialStore.ts contains `toggleBookmark`
    - src/store/useSocialStore.ts contains `new Set`
    - src/hooks/useInfiniteScroll.ts contains `export function useInfiniteScroll`
    - src/hooks/useInfiniteScroll.ts contains `IntersectionObserver`
    - src/hooks/useInfiniteScroll.ts contains `sentinelRef`
    - npx tsc --noEmit exits with 0 errors in these files
  </acceptance_criteria>
  <done>Zustand 스토어 2개 + useInfiniteScroll 훅 생성, 낙관적 업데이트 패턴 적용, TypeScript 컴파일 에러 없음</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 전체 프로젝트 컴파일 성공
- 모든 신규 파일이 500줄 이하
- galleryService.fetchPage가 `.eq('is_public', true)` + `.order()` + `.range()` 패턴 사용
- galleryService에 publishSession이 없음 (sessionService에서 단독 관리)
- socialService 토글 메서드가 INSERT/DELETE 패턴 사용
- useSocialStore.toggleLike가 낙관적 업데이트 + 롤백 패턴 구현
</verification>

<success_criteria>
- 6개 신규 파일 생성 + 1개 SQL 파일
- TypeScript 컴파일 에러 없음
- 모든 서비스/스토어 메서드가 export되어 Plan 02, 03, 04에서 import 가능
</success_criteria>

<output>
After completion, create `.planning/phases/09-social-gallery-collaboration/09-01-SUMMARY.md`
</output>
