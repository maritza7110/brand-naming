---
phase: 09-social-gallery-collaboration
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - src/components/gallery/LikeButton.tsx
  - src/components/gallery/BookmarkButton.tsx
  - src/components/gallery/GalleryModal.tsx
  - src/pages/GalleryPage.tsx
  - src/components/gallery/GalleryCard.tsx
autonomous: true
requirements: [SOCIAL-LIKE, SOCIAL-BOOKMARK, SOCIAL-GALLERY]

must_haves:
  truths:
    - "좋아요 하트 아이콘을 클릭하면 채워짐/비워짐이 즉시 토글된다"
    - "북마크 아이콘을 클릭하면 채워짐/비워짐이 즉시 토글된다"
    - "갤러리 카드 클릭 시 모달이 열리고 전체 브랜드명 + 입력 요약이 표시된다"
    - "모달에서 ESC 키 또는 배경 클릭으로 닫을 수 있다"
    - "좋아요/북마크 API 실패 시 로컬 상태가 롤백된다"
  artifacts:
    - path: "src/components/gallery/LikeButton.tsx"
      provides: "하트 토글 버튼"
      contains: "Heart"
    - path: "src/components/gallery/BookmarkButton.tsx"
      provides: "북마크 토글 버튼"
      contains: "Bookmark"
    - path: "src/components/gallery/GalleryModal.tsx"
      provides: "상세 모달 (입력 데이터 + 브랜드명 + 피드백)"
      contains: "GalleryModal"
  key_links:
    - from: "src/components/gallery/LikeButton.tsx"
      to: "src/store/useSocialStore.ts"
      via: "useSocialStore.toggleLike"
      pattern: "toggleLike"
    - from: "src/components/gallery/GalleryModal.tsx"
      to: "src/types/gallery.ts"
      via: "GallerySession type"
      pattern: "GallerySession"
    - from: "src/pages/GalleryPage.tsx"
      to: "src/components/gallery/GalleryModal.tsx"
      via: "selectedSession state"
      pattern: "GalleryModal"
---

<objective>
좋아요/북마크 인터랙션 + 갤러리 상세 모달

Purpose: 갤러리 카드의 인터랙션 레이어를 완성한다. 좋아요/북마크 토글(per D-07, D-08, D-09)과 카드 클릭 시 상세 모달(per D-03)을 구현한다.
Output: LikeButton, BookmarkButton, GalleryModal + GalleryPage/GalleryCard에 모달 연결
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-social-gallery-collaboration/09-CONTEXT.md
@.planning/phases/09-social-gallery-collaboration/09-UI-SPEC.md
@.planning/phases/09-social-gallery-collaboration/09-01-SUMMARY.md
@.planning/phases/09-social-gallery-collaboration/09-03-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 스토어 인터페이스 -->

From src/store/useSocialStore.ts:
```typescript
interface SocialState {
  likedIds: Set<string>;
  bookmarkedIds: Set<string>;
  isInitialized: boolean;
  initSocialState: (userId: string) => Promise<void>;
  toggleLike: (sessionId: string, userId: string) => Promise<void>;
  toggleBookmark: (sessionId: string, userId: string) => Promise<void>;
}
```

From src/store/useGalleryStore.ts:
```typescript
interface GalleryState {
  likeCounts: Record<string, number>;
  updateLikeCount: (sessionId: string, delta: number) => void;
}
```

From src/types/gallery.ts:
```typescript
export interface GallerySession {
  id: string; title: string; industry_id: string | null;
  input_data: any; created_at: string; user_id: string;
  profiles: { full_name: string | null } | null;
  naming_results: { brand_name: string; reasoning: string }[];
  like_count: number;
}
```

<!-- Plan 03에서 생성된 컴포넌트 — depends_on: 09-03이므로 파일이 반드시 존재 -->

From src/pages/GalleryPage.tsx:
```typescript
// selectedSession useState가 이미 존재
// GalleryModal import + 렌더링 주석 처리 상태
```

From src/components/gallery/GalleryCard.tsx:
```typescript
// Heart 아이콘 + likeCount 정적 표시 (footer 영역)
// LikeButton/BookmarkButton으로 교체 예정
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LikeButton + BookmarkButton 컴포넌트</name>
  <files>
    src/components/gallery/LikeButton.tsx,
    src/components/gallery/BookmarkButton.tsx
  </files>
  <read_first>
    src/store/useSocialStore.ts,
    src/store/useAuthStore.ts,
    .planning/phases/09-social-gallery-collaboration/09-UI-SPEC.md
  </read_first>
  <action>
**1. src/components/gallery/LikeButton.tsx 생성:**

```typescript
import { Heart } from 'lucide-react';
import { useSocialStore } from '../../store/useSocialStore';
import { useAuthStore } from '../../store/useAuthStore';

interface LikeButtonProps {
  sessionId: string;
  count: number;
  showCount?: boolean;  // 카드에서는 true, 모달에서도 true
}
```

JSX:
```tsx
export default function LikeButton({ sessionId, count, showCount = true }: LikeButtonProps) {
  const { likedIds, toggleLike } = useSocialStore();
  const user = useAuthStore((s) => s.user);
  const isLiked = likedIds.has(sessionId);

  const handleClick = (e: React.MouseEvent) => {
    e.stopPropagation();  // 카드 클릭 이벤트 전파 방지
    if (!user) return;
    toggleLike(sessionId, user.id);
  };

  return (
    <button
      onClick={handleClick}
      className="flex items-center gap-1 min-h-[44px] min-w-[44px] justify-center transition-all"
      aria-label="좋아요"
    >
      <Heart
        size={16}
        className={isLiked ? 'fill-[#B48C50] text-[#B48C50]' : 'text-[#A09890]'}
        style={isLiked ? { transform: 'scale(1.1)', transition: 'transform 150ms' } : { transition: 'transform 150ms' }}
      />
      {showCount && (
        <span className="text-[12px] text-[#A09890]">{count}</span>
      )}
    </button>
  );
}
```

09-UI-SPEC 준수:
- Unfilled: `text-[#A09890]`, size 16
- Filled: `fill-[#B48C50] text-[#B48C50]`
- Touch target: min-height 44px, min-width 44px
- Toggle animation: scale 100->110->100, 150ms
- `aria-label="좋아요"`
- `e.stopPropagation()` — 카드 내부에서 클릭 시 모달 열림 방지

**2. src/components/gallery/BookmarkButton.tsx 생성:**

LikeButton과 동일 패턴:

```typescript
import { Bookmark } from 'lucide-react';
import { useSocialStore } from '../../store/useSocialStore';
import { useAuthStore } from '../../store/useAuthStore';

interface BookmarkButtonProps {
  sessionId: string;
}
```

JSX:
```tsx
export default function BookmarkButton({ sessionId }: BookmarkButtonProps) {
  const { bookmarkedIds, toggleBookmark } = useSocialStore();
  const user = useAuthStore((s) => s.user);
  const isBookmarked = bookmarkedIds.has(sessionId);

  const handleClick = (e: React.MouseEvent) => {
    e.stopPropagation();
    if (!user) return;
    toggleBookmark(sessionId, user.id);
  };

  return (
    <button
      onClick={handleClick}
      className="flex items-center min-h-[44px] min-w-[44px] justify-center transition-all"
      aria-label="북마크"
    >
      <Bookmark
        size={16}
        className={isBookmarked ? 'fill-[#B48C50] text-[#B48C50]' : 'text-[#A09890]'}
        style={isBookmarked ? { transform: 'scale(1.1)', transition: 'transform 150ms' } : { transition: 'transform 150ms' }}
      />
    </button>
  );
}
```

09-UI-SPEC 준수:
- Unfilled: `Bookmark` icon, `text-[#A09890]`, size 16
- Filled: `fill-[#B48C50] text-[#B48C50]`
- Touch target: min-height 44px, min-width 44px
- `aria-label="북마크"`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/gallery/LikeButton.tsx contains `export default function LikeButton`
    - src/components/gallery/LikeButton.tsx contains `aria-label="좋아요"`
    - src/components/gallery/LikeButton.tsx contains `fill-[#B48C50] text-[#B48C50]`
    - src/components/gallery/LikeButton.tsx contains `min-h-[44px]`
    - src/components/gallery/LikeButton.tsx contains `stopPropagation`
    - src/components/gallery/LikeButton.tsx contains `Heart`
    - src/components/gallery/BookmarkButton.tsx contains `export default function BookmarkButton`
    - src/components/gallery/BookmarkButton.tsx contains `aria-label="북마크"`
    - src/components/gallery/BookmarkButton.tsx contains `fill-[#B48C50] text-[#B48C50]`
    - src/components/gallery/BookmarkButton.tsx contains `min-h-[44px]`
    - src/components/gallery/BookmarkButton.tsx contains `Bookmark`
    - Both files under 500 lines
  </acceptance_criteria>
  <done>LikeButton, BookmarkButton 완성. 44px 터치 타겟, 낙관적 토글, aria-label, stopPropagation 모두 적용</done>
</task>

<task type="auto">
  <name>Task 2: GalleryModal + GalleryPage/GalleryCard에 모달 및 버튼 연결</name>
  <files>
    src/components/gallery/GalleryModal.tsx,
    src/pages/GalleryPage.tsx,
    src/components/gallery/GalleryCard.tsx
  </files>
  <read_first>
    src/pages/GalleryPage.tsx,
    src/components/gallery/GalleryCard.tsx,
    src/components/gallery/LikeButton.tsx,
    src/components/gallery/BookmarkButton.tsx,
    src/store/useGalleryStore.ts,
    src/services/sessionService.ts,
    .planning/phases/09-social-gallery-collaboration/09-UI-SPEC.md
  </read_first>
  <action>
**1. src/components/gallery/GalleryModal.tsx 생성:**

```typescript
import { useEffect } from 'react';
import { X } from 'lucide-react';
import type { GallerySession } from '../../types/gallery';
import { sessionService } from '../../services/sessionService';
import { useGalleryStore } from '../../store/useGalleryStore';
import LikeButton from './LikeButton';
import BookmarkButton from './BookmarkButton';

interface GalleryModalProps {
  session: GallerySession;
  onClose: () => void;
}
```

JSX 구조 (09-UI-SPEC GalleryModal 내부 구조):
```tsx
export default function GalleryModal({ session, onClose }: GalleryModalProps) {
  const likeCounts = useGalleryStore((s) => s.likeCounts);

  // ESC 키 닫기
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose();
    };
    document.addEventListener('keydown', handleKeyDown);
    return () => document.removeEventListener('keydown', handleKeyDown);
  }, [onClose]);

  // 스크롤 방지
  useEffect(() => {
    document.body.style.overflow = 'hidden';
    return () => { document.body.style.overflow = ''; };
  }, []);

  // input_data에서 주요 필드 추출 (옵셔널 체이닝 — Pitfall 5)
  const inputData = session.input_data ?? {};
  const analysisFields = [
    { label: '경쟁사', value: inputData?.competitors },
    { label: '시장 트렌드', value: inputData?.marketTrend },
    { label: 'USP', value: inputData?.usp },
  ].filter(f => f.value);

  const identityFields = [
    { label: '브랜드 퍼스낼리티', value: inputData?.brandPersonality },
    { label: '비전', value: inputData?.longTermGoal || inputData?.ceoVision },
    { label: '타겟 고객', value: inputData?.targetCustomer || inputData?.customerDefinition },
  ].filter(f => f.value);

  return (
    {/* Backdrop */}
    <div className="fixed inset-0 bg-black/60 z-50 flex items-center justify-center p-4" onClick={onClose}>
      {/* Modal container */}
      <div
        className="bg-[#1A1A1E] border border-[#4A4440] rounded-2xl max-w-[720px] w-full max-h-[80vh] overflow-y-auto animate-fadeIn p-8"
        onClick={(e) => e.stopPropagation()}
      >
        {/* Header */}
        <div className="flex justify-between items-start mb-6">
          <div>
            <h2 className="text-[20px] font-semibold text-[#E8E2DA]">{session.title}</h2>
            {session.industry_id && (
              <span className="inline-block bg-[#4A4440] text-[#D0CAC2] text-[12px] px-2 py-1 rounded-full mt-2">
                {session.industry_id}
              </span>
            )}
          </div>
          <button onClick={onClose} className="text-[#A09890] hover:text-[#E8E2DA] transition-colors p-1" aria-label="닫기">
            <X size={20} />
          </button>
        </div>

        {/* 입력 요약 섹션 */}
        {(analysisFields.length > 0 || identityFields.length > 0) && (
          <div className="mb-6">
            <h3 className="text-[14px] text-[#A09890] mb-3">입력 요약</h3>
            <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
              {[...analysisFields, ...identityFields].map((field, i) => (
                <div key={i} className="bg-[#2C2825] rounded-xl p-3">
                  <p className="text-[12px] text-[#A09890] mb-1">{field.label}</p>
                  <p className="text-[14px] text-[#D0CAC2]">{field.value}</p>
                </div>
              ))}
            </div>
          </div>
        )}

        {/* 브랜드명 목록 — 전체 표시 */}
        <div className="mb-6">
          <h3 className="text-[14px] text-[#A09890] mb-3">추천 브랜드명</h3>
          <div className="space-y-3">
            {session.naming_results.map((result, i) => (
              <div key={i} className="bg-[#2C2825] rounded-xl p-4">
                <p className="text-[16px] font-semibold text-[#B48C50] mb-1">{result.brand_name}</p>
                {result.reasoning && (
                  <p className="text-[14px] text-[#D0CAC2] leading-relaxed">{result.reasoning}</p>
                )}
              </div>
            ))}
            {session.naming_results.length === 0 && (
              <p className="text-[14px] text-[#A09890]">추천된 브랜드명이 없습니다</p>
            )}
          </div>
        </div>

        {/* 피드백 영역 — LikeButton + BookmarkButton */}
        <div className="flex items-center gap-2 border-t border-[#4A4440] pt-4">
          <LikeButton sessionId={session.id} count={likeCounts[session.id] ?? 0} />
          <BookmarkButton sessionId={session.id} />
          <span className="text-[12px] text-[#A09890] ml-auto">
            {session.profiles?.full_name || '익명'} · {sessionService.formatDate(session.created_at)}
          </span>
        </div>
      </div>
    </div>
  );
}
```

**중요:** `animate-fadeIn`은 이미 `src/index.css`에 정의되어 있음 (Phase 08에서 추가). 추가 CSS 불필요.
**중요:** `sessionService.formatDate`는 모달에서만 사용한다 (카드에서는 사용하지 않음).

**2. src/pages/GalleryPage.tsx 수정 — 모달 연결:**

Plan 03에서 생성된 GalleryPage.tsx를 읽고 (depends_on: 09-03이므로 파일이 반드시 존재), 주석 처리된 모달 코드를 활성화:
- `import GalleryModal from '../components/gallery/GalleryModal'` 추가
- 기존 주석 `{/* {selectedSession && <GalleryModal ...} */}` 제거하고 실제 코드로 교체:
```tsx
{selectedSession && (
  <GalleryModal
    session={selectedSession}
    onClose={() => setSelectedSession(null)}
  />
)}
```

**3. src/components/gallery/GalleryCard.tsx 수정 — LikeButton, BookmarkButton 연결:**

Plan 03에서 생성된 GalleryCard.tsx를 읽고 (depends_on: 09-03이므로 파일이 반드시 존재), footer 영역을 수정:
- 기존 정적 `<Heart size={14} />` + count 표시를 `<LikeButton>` + `<BookmarkButton>`으로 교체
- `import LikeButton from './LikeButton'`
- `import BookmarkButton from './BookmarkButton'`
- 기존 `import { Heart } from 'lucide-react'` 제거 (LikeButton이 내부적으로 Heart 사용)
- Footer Actions 영역:
```tsx
<div className="flex items-center gap-0">
  <LikeButton sessionId={session.id} count={likeCount} />
  <BookmarkButton sessionId={session.id} />
</div>
```

기존 정적 Heart 아이콘과 카운트 span 제거.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/gallery/GalleryModal.tsx contains `export default function GalleryModal`
    - src/components/gallery/GalleryModal.tsx contains `fixed inset-0 bg-black/60`
    - src/components/gallery/GalleryModal.tsx contains `max-w-[720px]`
    - src/components/gallery/GalleryModal.tsx contains `Escape`
    - src/components/gallery/GalleryModal.tsx contains `animate-fadeIn`
    - src/components/gallery/GalleryModal.tsx contains `aria-label="닫기"`
    - src/components/gallery/GalleryModal.tsx contains `LikeButton`
    - src/components/gallery/GalleryModal.tsx contains `BookmarkButton`
    - src/components/gallery/GalleryModal.tsx contains `입력 요약`
    - src/components/gallery/GalleryModal.tsx contains `추천 브랜드명`
    - src/components/gallery/GalleryModal.tsx contains `stopPropagation`
    - src/components/gallery/GalleryModal.tsx contains `sessionService.formatDate`
    - src/pages/GalleryPage.tsx contains `GalleryModal`
    - src/pages/GalleryPage.tsx contains `selectedSession`
    - src/components/gallery/GalleryCard.tsx contains `LikeButton`
    - src/components/gallery/GalleryCard.tsx contains `BookmarkButton`
    - src/components/gallery/GalleryCard.tsx does NOT contain `import { Heart }`
    - All files under 500 lines
  </acceptance_criteria>
  <done>GalleryModal 상세 보기 완성 (ESC/배경 클릭 닫기, 입력 요약, 브랜드명 전체, 피드백 버튼), GalleryPage에 모달 연결, GalleryCard에 인터랙티브 버튼 연결</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` 전체 프로젝트 컴파일 성공
- GalleryModal이 ESC 키 + 배경 클릭으로 닫힘
- LikeButton/BookmarkButton이 낙관적 업데이트 스토어와 연결됨
- 모든 aria-label 존재 (좋아요, 북마크, 닫기)
- GalleryCard에서 Heart 직접 import 제거, LikeButton으로 대체
- 모든 파일 500줄 이하
</verification>

<success_criteria>
- 갤러리 카드 클릭 시 모달 열림
- 모달에서 입력 요약 + 브랜드명 전체 + rationale 확인 가능
- 좋아요/북마크 토글이 즉시 UI 반영
- 모든 접근성 속성 존재
</success_criteria>

<output>
After completion, create `.planning/phases/09-social-gallery-collaboration/09-04-SUMMARY.md`
</output>
