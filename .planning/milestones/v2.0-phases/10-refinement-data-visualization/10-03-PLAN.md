---
phase: 10-refinement-data-visualization
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/gallery/CommentSection.tsx
  - src/components/gallery/GalleryModal.tsx
autonomous: true
requirements:
  - COMMENT-UI

must_haves:
  truths:
    - "GalleryModal 하단에 댓글 섹션이 표시된다"
    - "댓글 목록에 작성자명 + 작성일 + 본문이 표시된다"
    - "인증 사용자가 댓글을 작성할 수 있다"
    - "본인 댓글에만 삭제 버튼이 표시된다"
    - "미인증 사용자에게는 로그인 안내 문구가 표시된다"
    - "댓글 삭제는 confirm 없이 즉시 실행된다"
  artifacts:
    - path: "src/components/gallery/CommentSection.tsx"
      provides: "댓글 목록 + 입력 폼 컴포넌트"
      min_lines: 80
    - path: "src/components/gallery/GalleryModal.tsx"
      provides: "하단에 CommentSection 마운트"
      contains: "CommentSection"
  key_links:
    - from: "src/components/gallery/CommentSection.tsx"
      to: "src/store/useSocialStore.ts"
      via: "fetchComments/addComment/deleteComment 호출"
      pattern: "useSocialStore.*fetchComments"
    - from: "src/components/gallery/GalleryModal.tsx"
      to: "src/components/gallery/CommentSection.tsx"
      via: "import + JSX 렌더링"
      pattern: "CommentSection"
---

<objective>
GalleryModal 하단에 CommentSection(댓글 목록 + 입력 폼)을 생성하고 마운트한다.

Purpose: 갤러리 모달에서 사용자가 댓글을 작성/삭제하고 다른 사용자의 피드백을 볼 수 있게 한다 (D-06~D-08).
Output: CommentSection.tsx 신규 생성, GalleryModal.tsx 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-refinement-data-visualization/10-CONTEXT.md
@.planning/phases/10-refinement-data-visualization/10-UI-SPEC.md
@.planning/phases/10-refinement-data-visualization/10-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 타입/스토어 시그니처 -->

From src/types/gallery.ts (Plan 01 확장):
```typescript
export interface CommentData {
  id: string;
  session_id: string;
  user_id: string;
  content: string;
  created_at: string;
  profiles: { full_name: string | null } | null;
}
```

From src/store/useSocialStore.ts (Plan 01 확장):
```typescript
// 추가된 상태/액션
comments: CommentData[];
isCommentsLoading: boolean;
fetchComments: (sessionId: string) => Promise<void>;
addComment: (sessionId: string, userId: string, content: string, userName: string) => Promise<void>;
deleteComment: (commentId: string) => Promise<void>;
```

From src/store/useAuthStore.ts:
```typescript
// user?.id, user?.email — 인증 상태
```

From src/components/gallery/GalleryModal.tsx:
```typescript
interface GalleryModalProps {
  session: GallerySession;
  onClose: () => void;
}
// 현재 122줄, max-h-[80vh] overflow-y-auto 모달 컨테이너
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CommentSection 컴포넌트 생성</name>
  <files>src/components/gallery/CommentSection.tsx</files>
  <read_first>
    - src/store/useSocialStore.ts (Plan 01에서 확장된 comments/fetchComments/addComment/deleteComment)
    - src/store/useAuthStore.ts (user 객체 구조 — id, email)
    - src/components/gallery/GalleryModal.tsx (스타일 패턴 — 색상, 간격, 텍스트 사이즈)
    - src/components/gallery/LikeButton.tsx (낙관적 업데이트 UI 패턴 참조)
    - .planning/phases/10-refinement-data-visualization/10-UI-SPEC.md (CommentSection 레이아웃 스펙)
  </read_first>
  <action>
**src/components/gallery/CommentSection.tsx 생성:**

```typescript
import { useEffect, useState } from 'react';
import { Trash2 } from 'lucide-react';
import { formatDistanceToNow } from 'date-fns';
import { ko } from 'date-fns/locale';
import { useSocialStore } from '../../store/useSocialStore';
import { useAuthStore } from '../../store/useAuthStore';
```

Props: `{ sessionId: string }`

컴포넌트 내부:
1. useSocialStore에서 구독: `comments`, `isCommentsLoading`, `fetchComments`, `addComment`, `deleteComment`
2. useAuthStore에서 구독: `user`
3. 로컬 상태: `const [content, setContent] = useState('')`
4. useEffect: 마운트 시 `fetchComments(sessionId)` 호출 (per RESEARCH.md — 단순 mount fetch 허용)

**댓글 목록:**
- 컨테이너: `space-y-3` (gap-3)
- 댓글 없을 때: "아직 댓글이 없습니다" — `text-[14px] text-[#A09890] text-center py-4`
- 댓글 있을 때 헤더: `댓글 {comments.length}개` — `text-[14px] font-semibold text-[#A09890] mb-3`

**각 CommentItem (댓글 목록 내 반복):**
- 외부: `flex justify-between items-start`
- 좌측:
  - 작성자명: `text-[14px] font-semibold text-[#D0CAC2]` — `comment.profiles?.full_name || '익명'`
  - 작성일: `text-[12px] text-[#5A5550]` — `formatDistanceToNow(new Date(comment.created_at), { addSuffix: true, locale: ko })` (예: "3분 전")
  - 본문: `text-[14px] text-[#E8E2DA] whitespace-pre-wrap mt-1` — `comment.content`
- 우측 (본인 댓글만): Trash2 아이콘(lucide, size=14). `text-[#5A5550] hover:text-[#EF4444] transition-colors cursor-pointer`. onClick: `deleteComment(comment.id)` — confirm 없이 즉시 실행 (per D-08, UI-SPEC destructive action 규칙).

**댓글 입력 폼:**
- 조건: `user` 존재 시만 textarea + 버튼 표시
- 미인증 시: "댓글을 작성하려면 로그인하세요" — `text-[14px] text-[#A09890] text-center py-4`
- textarea: `min-h-[80px] w-full bg-[#1A1A1E] border border-[#4A4440] rounded-lg p-3 text-[14px] text-[#E8E2DA] placeholder-[#5A5550] focus:border-[#B48C50] focus:outline-none resize-none` — maxLength={500}, placeholder="댓글을 입력하세요..."
- 제출 버튼: "댓글 작성" — `text-[14px] font-semibold text-[#B48C50] hover:text-[#C5A06B] transition-colors cursor-pointer` — 우측 정렬 (`flex justify-end mt-2`)
- 제출 핸들러:
  ```typescript
  const handleSubmit = () => {
    if (!content.trim() || !user) return;
    addComment(sessionId, user.id, content.trim(), user.email ?? '익명');
    setContent('');
  };
  ```
- Enter 키 제출은 하지 않음 (textarea이므로 줄바꿈 허용). 버튼 클릭으로만 제출.

**로딩 상태:** `isCommentsLoading` 시 `text-[14px] text-[#A09890] text-center py-4` "댓글을 불러오는 중..."
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/gallery/CommentSection.tsx exists
    - File contains `fetchComments`
    - File contains `addComment`
    - File contains `deleteComment`
    - File contains `Trash2`
    - File contains `formatDistanceToNow`
    - File contains `댓글을 작성하려면 로그인하세요`
    - File contains `댓글 작성` (제출 버튼 텍스트)
    - File contains `아직 댓글이 없습니다`
    - File contains `maxLength={500}` or `maxLength: 500`
    - npx tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>CommentSection이 댓글 목록(작성자명+작성일+본문+삭제 버튼) + 입력 폼(textarea+제출 버튼) + 미인증 안내를 렌더링하고, 낙관적 업데이트로 CRUD가 동작한다</done>
</task>

<task type="auto">
  <name>Task 2: GalleryModal에 CommentSection 마운트</name>
  <files>src/components/gallery/GalleryModal.tsx</files>
  <read_first>
    - src/components/gallery/GalleryModal.tsx (현재 전체 코드 — 122줄, max-h-[80vh], 피드백 영역 위치)
    - src/components/gallery/CommentSection.tsx (Task 1에서 생성 — props 인터페이스)
    - .planning/phases/10-refinement-data-visualization/10-UI-SPEC.md (GalleryModal 높이 조정: max-h-[90vh])
  </read_first>
  <action>
**src/components/gallery/GalleryModal.tsx 수정:**

1. import 추가:
```typescript
import CommentSection from './CommentSection';
```

2. 모달 컨테이너 `max-h-[80vh]`를 `max-h-[90vh]`로 변경 (per UI-SPEC — 댓글 섹션 추가로 모달 높이 증가):
```
max-h-[80vh] → max-h-[90vh]
```

3. 피드백 영역 (`<div className="flex items-center gap-2 border-t border-[#4A4440] pt-4">`) 다음에 CommentSection 마운트:

기존 피드백 영역 닫는 `</div>` 뒤에 추가:
```tsx
{/* 댓글 섹션 (per D-06) */}
<hr className="border-[#4A4440] my-4" />
<CommentSection sessionId={session.id} />
```

4. 파일이 500줄을 초과하지 않는지 확인 (현재 122줄 + hr + CommentSection import/마운트 = ~125줄, 안전)

변경하지 않는 것:
- 기존 ESC 키 닫기 useEffect
- 기존 body overflow hidden useEffect
- 기존 backdrop onClick + stopPropagation 패턴
- 기존 입력 요약/브랜드명 목록/피드백 영역
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/gallery/GalleryModal.tsx contains `import CommentSection`
    - src/components/gallery/GalleryModal.tsx contains `<CommentSection`
    - src/components/gallery/GalleryModal.tsx contains `sessionId={session.id}`
    - src/components/gallery/GalleryModal.tsx contains `max-h-[90vh]`
    - src/components/gallery/GalleryModal.tsx does NOT contain `max-h-[80vh]`
    - npx tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>GalleryModal 하단에 hr 구분선 + CommentSection이 마운트되고, 모달 높이가 90vh로 확장되어 댓글 섹션을 수용한다</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript 컴파일 에러 없음
2. `npm run dev`로 갤러리 모달 열기 — 하단에 댓글 섹션 표시 확인
3. 댓글 입력 후 "댓글 작성" 클릭 — 낙관적으로 목록에 추가 확인
4. 본인 댓글에만 Trash2 아이콘 표시 확인
5. 미로그인 상태에서 "댓글을 작성하려면 로그인하세요" 표시 확인
6. 모달 스크롤이 자연스럽게 동작 (Pitfall 2 — 별도 내부 스크롤 없음)
</verification>

<success_criteria>
- CommentSection이 댓글 목록(작성자명+상대 시간+본문) + 삭제 버튼(본인만) + 입력 폼(textarea+제출) + 미인증 안내를 렌더링
- GalleryModal 하단에 hr 구분선 + CommentSection이 마운트됨
- 모달 max-h가 90vh로 확장됨
- 댓글 삭제는 confirm 없이 즉시 실행
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/10-refinement-data-visualization/10-03-SUMMARY.md`
</output>
