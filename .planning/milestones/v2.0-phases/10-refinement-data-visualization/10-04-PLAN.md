---
phase: 10-refinement-data-visualization
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/dashboard/StatsSection.tsx
  - src/pages/Dashboard.tsx
  - package.json
autonomous: true
requirements:
  - STATS-UI

must_haves:
  truths:
    - "대시보드 하단에 통계 섹션이 프로젝트 3개 이상일 때만 표시된다"
    - "인기 키워드 바 차트가 표시된다"
    - "업종 분포 도넛 차트가 표시된다"
    - "네이밍 스타일 바 차트가 표시된다"
    - "차트가 프로젝트 테마 색상(#B48C50 계열)으로 렌더링된다"
  artifacts:
    - path: "src/components/dashboard/StatsSection.tsx"
      provides: "3개 Recharts 차트 (키워드 BarChart, 업종 PieChart, 스타일 BarChart)"
      min_lines: 80
    - path: "src/pages/Dashboard.tsx"
      provides: "프로젝트 3개 이상 조건부 StatsSection 렌더링"
      contains: "StatsSection"
  key_links:
    - from: "src/components/dashboard/StatsSection.tsx"
      to: "recharts"
      via: "BarChart, PieChart, ResponsiveContainer import"
      pattern: "from 'recharts'"
    - from: "src/pages/Dashboard.tsx"
      to: "src/components/dashboard/StatsSection.tsx"
      via: "조건부 렌더링"
      pattern: "sessions\\.length >= 3"
---

<objective>
대시보드에 StatsSection(인기 키워드 바 차트 + 업종 분포 도넛 차트 + 네이밍 스타일 바 차트)을 생성하고, recharts를 설치하며, Dashboard에 조건부 마운트한다.

Purpose: 사용자가 자신의 네이밍 프로젝트 데이터를 시각적으로 분석할 수 있게 한다 (D-09~D-11).
Output: recharts npm 패키지 설치, StatsSection.tsx 신규 생성, Dashboard.tsx 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-refinement-data-visualization/10-CONTEXT.md
@.planning/phases/10-refinement-data-visualization/10-RESEARCH.md
@.planning/phases/10-refinement-data-visualization/10-UI-SPEC.md
@.planning/phases/10-refinement-data-visualization/10-01-SUMMARY.md

<interfaces>
<!-- 대시보드 기존 구조 -->

From src/pages/Dashboard.tsx:
```typescript
// sessions: SessionData[] — loadSessions에서 sessionService.getSessions()로 로드
// activeTab: 'projects' | 'bookmarks'
// 북마크 탭 콘텐츠 끝나는 지점 (라인 183) 아래에 StatsSection 마운트
```

From src/services/sessionService.ts:
```typescript
export interface SessionData {
  id: string;
  user_id: string;
  title: string;
  industry_id: string | null;
  input_data: any;
  status: string;
  is_public: boolean;
  created_at: string;
  updated_at: string;
}
```

From RESEARCH.md Pattern 3 — Recharts 차트 패턴:
```typescript
// Chart color palette
const CHART_COLORS = ['#B48C50', '#8B6E3A', '#C5A06B', '#6B5030', '#D4B485'];

// Tooltip 공통 스타일
{ backgroundColor: '#1A1A1E', border: '1px solid #4A4440', borderRadius: '8px', color: '#E8E2DA' }
```

From RESEARCH.md Pattern 5 — 통계 데이터 집계:
```typescript
// sessions 배열에서 클라이언트 집계
// 1. industry_id.split('/')[0] → 대분류 빈도
// 2. input_data.storeBasic.productKeywords → 키워드 빈도
// 3. input_data.expression.namingStyle → 네이밍 스타일 빈도
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: recharts 설치 + StatsSection 컴포넌트 생성</name>
  <files>package.json, src/components/dashboard/StatsSection.tsx</files>
  <read_first>
    - package.json (현재 의존성 — recharts 미설치 확인)
    - src/services/sessionService.ts (SessionData 인터페이스 — input_data 구조)
    - src/pages/Dashboard.tsx (sessions 배열이 어떻게 로드되는지, 탭 구조)
    - .planning/phases/10-refinement-data-visualization/10-UI-SPEC.md (StatsSection 레이아웃, 차트 스타일, 반응형)
    - .planning/phases/10-refinement-data-visualization/10-RESEARCH.md (Pattern 3 Recharts 코드, Pattern 5 통계 집계)
  </read_first>
  <action>
**1. recharts 설치:**
```bash
npm install recharts
```

**2. src/components/dashboard/ 디렉토리 확인 (없으면 생성):**
기존에 `src/components/dashboard/ProjectList.tsx`가 있으므로 디렉토리는 존재함.

**3. src/components/dashboard/StatsSection.tsx 생성:**

```typescript
import {
  BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer,
  PieChart, Pie, Cell,
} from 'recharts';
import type { SessionData } from '../../services/sessionService';
```

Props: `{ sessions: SessionData[] }`

**상수:**
```typescript
const CHART_COLORS = ['#B48C50', '#8B6E3A', '#C5A06B', '#6B5030', '#D4B485'];

const TOOLTIP_STYLE = {
  backgroundColor: '#1A1A1E',
  border: '1px solid #4A4440',
  borderRadius: '8px',
  color: '#E8E2DA',
};
```

**데이터 집계 함수 (컴포넌트 외부 또는 useMemo):**

```typescript
function computeStats(sessions: SessionData[]) {
  // 1. 업종 분포: industry_id → 대분류 추출 → 빈도
  const industryCount: Record<string, number> = {};
  sessions.forEach(s => {
    if (s.industry_id) {
      const major = s.industry_id.split('/')[0];
      industryCount[major] = (industryCount[major] ?? 0) + 1;
    }
  });
  const industryData = Object.entries(industryCount)
    .map(([name, value]) => ({ name, value }))
    .sort((a, b) => b.value - a.value)
    .slice(0, 6); // 최대 6개 표시

  // 2. 키워드 빈도: input_data에서 키워드 관련 필드 집계
  const keywordCount: Record<string, number> = {};
  sessions.forEach(s => {
    const keywords: string[] = [];
    // productKeywords (쉼표 구분 문자열)
    const pk = s.input_data?.storeBasic?.productKeywords || s.input_data?.productKeywords;
    if (typeof pk === 'string' && pk.trim()) {
      keywords.push(...pk.split(',').map((k: string) => k.trim()).filter(Boolean));
    }
    // 업종명도 키워드로 카운트
    if (s.industry_id) {
      const parts = s.industry_id.split('/');
      if (parts.length >= 2) keywords.push(parts[1]); // 중분류
    }
    keywords.forEach(kw => {
      keywordCount[kw] = (keywordCount[kw] ?? 0) + 1;
    });
  });
  const keywordData = Object.entries(keywordCount)
    .map(([name, count]) => ({ name, count }))
    .sort((a, b) => b.count - a.count)
    .slice(0, 8); // TOP 8 키워드

  // 3. 네이밍 스타일 빈도: input_data.expression.namingStyle 배열
  const styleCount: Record<string, number> = {};
  sessions.forEach(s => {
    const styles = s.input_data?.expression?.namingStyle;
    if (Array.isArray(styles)) {
      styles.forEach((st: string) => {
        styleCount[st] = (styleCount[st] ?? 0) + 1;
      });
    }
  });
  const styleData = Object.entries(styleCount)
    .map(([name, count]) => ({ name, count }))
    .sort((a, b) => b.count - a.count);

  return { industryData, keywordData, styleData };
}
```

**컴포넌트 렌더링:**

- useMemo로 computeStats 호출: `const { industryData, keywordData, styleData } = useMemo(() => computeStats(sessions), [sessions]);`

- 섹션 헤더: "나의 네이밍 통계" — `text-[20px] font-semibold text-[#E8E2DA] mt-8 mb-6` (per UI-SPEC)

- 차트 그리드: `grid grid-cols-1 sm:grid-cols-3 gap-4` (per UI-SPEC 반응형: mobile=1열, tablet=2열은 sm:grid-cols-2지만 UI-SPEC은 sm:grid-cols-3이므로 스펙 준수)

**차트 카드 공통 스타일:** `bg-[#1A1A1E] rounded-xl p-4 border border-white/5`

**차트 1: 인기 키워드 (수평 BarChart, per UI-SPEC):**
- 카드 타이틀: "인기 키워드" — `text-[14px] font-semibold text-[#A09890] mb-3`
- `keywordData.length === 0` 시: "아직 데이터가 없습니다" — `text-[14px] text-[#5A5550] text-center py-8`
- 차트:
  ```tsx
  <ResponsiveContainer width="100%" height={180}>
    <BarChart data={keywordData} layout="vertical">
      <XAxis type="number" hide />
      <YAxis type="category" dataKey="name" tick={{ fill: '#A09890', fontSize: 12 }} width={80} />
      <Tooltip contentStyle={TOOLTIP_STYLE} />
      <Bar dataKey="count" fill="#B48C50" radius={[0, 4, 4, 0]} />
    </BarChart>
  </ResponsiveContainer>
  ```

**차트 2: 업종 분포 (도넛 PieChart, per UI-SPEC):**
- 카드 타이틀: "업종 분포" — 동일 스타일
- `industryData.length === 0` 시: 빈 상태
- 차트:
  ```tsx
  <ResponsiveContainer width="100%" height={220}>
    <PieChart>
      <Pie
        data={industryData}
        cx="50%"
        cy="50%"
        innerRadius={55}
        outerRadius={90}
        dataKey="value"
        label={({ name, percent }) => `${name} ${(percent * 100).toFixed(0)}%`}
        labelLine={false}
      >
        {industryData.map((_, index) => (
          <Cell key={index} fill={CHART_COLORS[index % CHART_COLORS.length]} />
        ))}
      </Pie>
      <Tooltip contentStyle={TOOLTIP_STYLE} />
    </PieChart>
  </ResponsiveContainer>
  ```
  - 도넛 라벨: name + percent (예: "음식 40%")
  - 라벨 텍스트 스타일: `fontSize: 12, fill: '#A09890'`

**차트 3: 네이밍 스타일 (수직 BarChart, per UI-SPEC):**
- 카드 타이틀: "네이밍 스타일" — 동일 스타일
- `styleData.length === 0` 시: 빈 상태
- 차트:
  ```tsx
  <ResponsiveContainer width="100%" height={180}>
    <BarChart data={styleData}>
      <XAxis dataKey="name" tick={{ fill: '#A09890', fontSize: 11 }} interval={0} angle={-20} textAnchor="end" height={50} />
      <YAxis tick={{ fill: '#A09890', fontSize: 12 }} />
      <Tooltip contentStyle={TOOLTIP_STYLE} />
      <Bar dataKey="count" fill="#C5A06B" radius={[4, 4, 0, 0]} />
    </BarChart>
  </ResponsiveContainer>
  ```
  - 스타일 차트는 Tertiary 색상 `#C5A06B` 사용하여 키워드 차트(`#B48C50`)와 시각적 구분
  - XAxis 라벨이 긴 한글이므로 angle=-20, textAnchor="end" 적용

**파일 500줄 이하 확인:** computeStats 함수 ~40줄 + 렌더링 ~120줄 = ~160줄 예상, 안전.
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"recharts"`
    - src/components/dashboard/StatsSection.tsx exists
    - File contains `from 'recharts'`
    - File contains `BarChart`
    - File contains `PieChart`
    - File contains `ResponsiveContainer`
    - File contains `CHART_COLORS`
    - File contains `나의 네이밍 통계` (섹션 헤더 — 이 텍스트는 StatsSection 내부가 아닌 Dashboard에 있을 수 있으므로, 둘 중 하나에 존재해야 함)
    - File contains `인기 키워드`
    - File contains `업종 분포`
    - File contains `네이밍 스타일`
    - File contains `computeStats` or `industryData` (집계 로직)
    - npx tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>recharts 설치 완료, StatsSection이 3개 차트(키워드 수평 바, 업종 도넛, 스타일 수직 바)를 프로젝트 테마 색상으로 렌더링하고, 빈 데이터 상태를 처리한다</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard에 StatsSection 조건부 마운트</name>
  <files>src/pages/Dashboard.tsx</files>
  <read_first>
    - src/pages/Dashboard.tsx (현재 전체 코드 — 187줄, sessions 상태, 탭 콘텐츠 영역)
    - src/components/dashboard/StatsSection.tsx (Task 1에서 생성 — props: { sessions: SessionData[] })
  </read_first>
  <action>
**src/pages/Dashboard.tsx 수정:**

1. import 추가:
```typescript
import StatsSection from '../components/dashboard/StatsSection';
```

2. 탭 콘텐츠 블록(`{activeTab === 'projects' ? ... : ...}`) 닫는 괄호 뒤에 StatsSection 조건부 렌더링 추가:

```tsx
{/* 통계 섹션 — 프로젝트 3개 이상일 때만 표시 (per D-09) */}
{sessions.length >= 3 && (
  <StatsSection sessions={sessions} />
)}
```

이 위치는 `</main>` 닫기 직전이어야 한다. 탭 콘텐츠와 동일 레벨 (탭 내부가 아님 — 통계는 탭과 무관하게 항상 하단에 표시).

3. "나의 네이밍 통계" 섹션 헤더가 StatsSection 내부에 있다면 Dashboard에서는 추가하지 않음. StatsSection 내부에 없다면 여기서 추가:
```tsx
{sessions.length >= 3 && (
  <>
    <h3 className="text-[20px] font-semibold text-[#E8E2DA] mt-8 mb-6">나의 네이밍 통계</h3>
    <StatsSection sessions={sessions} />
  </>
)}
```
(Task 1의 StatsSection에 헤더가 포함되어 있으므로 중복 방지 — StatsSection 코드를 확인 후 판단)

4. 파일 500줄 이하 확인: 187줄 + import 1줄 + 조건부 렌더링 3줄 = ~191줄, 안전.

변경하지 않는 것:
- 기존 header, 탭 버튼, 프로젝트 목록, 북마크 탭 — 모두 그대로 유지
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/Dashboard.tsx contains `import StatsSection`
    - src/pages/Dashboard.tsx contains `sessions.length >= 3`
    - src/pages/Dashboard.tsx contains `<StatsSection`
    - src/pages/Dashboard.tsx contains `sessions={sessions}`
    - npx tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>Dashboard 하단에 프로젝트 3개 이상일 때만 StatsSection이 조건부 렌더링되고, sessions 배열이 props로 전달된다</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript 컴파일 에러 없음
2. `npm run dev`로 대시보드 접근 — 프로젝트 3개 이상일 때 하단에 3개 차트 표시
3. 프로젝트 3개 미만일 때 통계 섹션 미표시
4. 차트가 프로젝트 테마 색상(#B48C50 계열)으로 렌더링
5. 모바일(< 640px)에서 차트가 단열(grid-cols-1)로 표시
</verification>

<success_criteria>
- recharts가 package.json에 추가됨
- StatsSection이 3개 Recharts 차트(키워드 수평 바, 업종 도넛, 스타일 수직 바)를 렌더링
- 차트 색상이 CHART_COLORS 팔레트(#B48C50 계열) 사용
- Dashboard 하단에 sessions.length >= 3 조건부로 StatsSection 마운트
- 빈 데이터 상태("아직 데이터가 없습니다") 처리
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/10-refinement-data-visualization/10-04-SUMMARY.md`
</output>
