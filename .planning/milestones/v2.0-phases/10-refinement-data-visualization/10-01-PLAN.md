---
phase: 10-refinement-data-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/gallery.ts
  - src/services/galleryService.ts
  - src/services/socialService.ts
  - src/store/useGalleryStore.ts
  - src/store/useSocialStore.ts
  - supabase/migrations/20260406_comments.sql
autonomous: true
requirements:
  - FILTER-DATA
  - COMMENT-DATA
  - LEADERBOARD-DATA

must_haves:
  truths:
    - "갤러리 필터 상태(industry, namingStyle, keyword)가 Zustand 스토어에서 관리된다"
    - "필터 변경 시 sessions/page/hasMore가 리셋되고 fetchNextPage가 재실행된다"
    - "galleryService.fetchPage가 필터 파라미터를 Supabase 쿼리에 적용한다"
    - "socialService에 댓글 CRUD(getComments, addComment, deleteComment)가 존재한다"
    - "useSocialStore에 댓글 상태(comments, fetchComments, addComment, deleteComment)가 존재한다"
    - "galleryService에 리더보드 쿼리(fetchLeaderboard)가 존재한다"
    - "useGalleryStore에 리더보드 상태(leaderboard, leaderboardPeriod)가 존재한다"
  artifacts:
    - path: "src/types/gallery.ts"
      provides: "GalleryFilters, CommentData, LeaderboardEntry 타입"
      contains: "GalleryFilters"
    - path: "src/services/galleryService.ts"
      provides: "필터 쿼리 + 리더보드 쿼리"
      exports: ["galleryService"]
    - path: "src/services/socialService.ts"
      provides: "댓글 CRUD"
      exports: ["socialService"]
    - path: "src/store/useGalleryStore.ts"
      provides: "필터 상태 + 리더보드 상태"
      exports: ["useGalleryStore"]
    - path: "src/store/useSocialStore.ts"
      provides: "댓글 상태"
      exports: ["useSocialStore"]
    - path: "supabase/migrations/20260406_comments.sql"
      provides: "comments 테이블 DDL + RLS"
      contains: "CREATE TABLE comments"
  key_links:
    - from: "src/store/useGalleryStore.ts"
      to: "src/services/galleryService.ts"
      via: "fetchNextPage에서 filters 전달"
      pattern: "galleryService\\.fetchPage.*filters"
    - from: "src/store/useSocialStore.ts"
      to: "src/services/socialService.ts"
      via: "fetchComments/addComment/deleteComment 호출"
      pattern: "socialService\\.(getComments|addComment|deleteComment)"
---

<objective>
Phase 10 데이터 레이어 구축 — 갤러리 필터/리더보드/댓글 시스템의 타입, 서비스, 스토어, DB 마이그레이션을 생성한다.

Purpose: 후속 UI 플랜(02: 필터+리더보드, 03: 댓글, 04: 통계)이 모두 이 데이터 레이어에 의존한다. 타입과 서비스를 먼저 확립하여 UI 플랜들이 Wave 2에서 병렬 실행 가능하도록 한다.
Output: 확장된 types/gallery.ts, galleryService, socialService, useGalleryStore, useSocialStore, comments 테이블 마이그레이션 SQL
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-refinement-data-visualization/10-CONTEXT.md
@.planning/phases/10-refinement-data-visualization/10-RESEARCH.md
@.planning/phases/10-refinement-data-visualization/10-UI-SPEC.md

<interfaces>
<!-- 기존 타입/서비스 시그니처 — executor가 직접 확인할 필요 없이 사용 -->

From src/types/gallery.ts:
```typescript
export type GallerySortBy = 'latest' | 'popular';
export interface GallerySession {
  id: string;
  title: string;
  industry_id: string | null;
  input_data: any;
  created_at: string;
  user_id: string;
  profiles: { full_name: string | null } | null;
  naming_results: { brand_name: string; reasoning: string }[];
  like_count: number;
  is_bookmarked?: boolean;
}
```

From src/services/galleryService.ts:
```typescript
export const galleryService = {
  fetchPage: async (page: number, sortBy: GallerySortBy): Promise<GallerySession[]>,
  fetchLikeCounts: async (sessionIds: string[]): Promise<Record<string, number>>,
};
```

From src/store/useGalleryStore.ts:
```typescript
interface GalleryState {
  sessions: GallerySession[];
  page: number;
  sortBy: GallerySortBy;
  isLoading: boolean;
  hasMore: boolean;
  error: string | null;
  likeCounts: Record<string, number>;
  fetchNextPage: () => Promise<void>;
  setSortBy: (sortBy: GallerySortBy) => void;
  reset: () => void;
  updateLikeCount: (sessionId: string, delta: number) => void;
}
```

From src/services/socialService.ts:
```typescript
export const socialService = {
  getMyLikedIds, getMyBookmarkedIds, toggleLike, toggleBookmark, getMyBookmarks,
};
```

From src/store/useSocialStore.ts:
```typescript
interface SocialState {
  likedIds: Set<string>;
  bookmarkedIds: Set<string>;
  isInitialized: boolean;
  initSocialState, toggleLike, toggleBookmark,
}
```

From src/data/industryData.ts:
```typescript
export interface IndustryCategory { major: string; medium: string; minor: string; }
export const INDUSTRY_DATA: IndustryCategory[];
// 대분류 추출: [...new Set(INDUSTRY_DATA.map(d => d.major))]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 타입 정의 + DB 마이그레이션 SQL + 서비스 확장</name>
  <files>src/types/gallery.ts, supabase/migrations/20260406_comments.sql, src/services/galleryService.ts, src/services/socialService.ts</files>
  <read_first>
    - src/types/gallery.ts (현재 타입 구조 확인)
    - src/services/galleryService.ts (현재 fetchPage/fetchLikeCounts 시그니처)
    - src/services/socialService.ts (현재 toggleLike/toggleBookmark/getMyBookmarks 패턴)
    - src/services/supabase.ts (supabase 임포트 경로)
    - src/data/industryData.ts (INDUSTRY_DATA 구조 — major 필드 확인)
  </read_first>
  <action>
**1. src/types/gallery.ts 확장:**

기존 타입은 그대로 유지하고 아래 타입을 파일 하단에 추가:

```typescript
export interface GalleryFilters {
  industry: string | null;     // 대분류 (음식, 소매, 생활서비스 등)
  namingStyle: string | null;  // 네이밍 스타일 (합성어, 추상어 등)
  keyword: string;             // 키워드 검색 (브랜드명/title 텍스트 검색)
}

export type LeaderboardPeriod = 'week' | 'all';

export interface LeaderboardEntry {
  session: GallerySession;
  likeCount: number;
}

export interface CommentData {
  id: string;
  session_id: string;
  user_id: string;
  content: string;
  created_at: string;
  profiles: { full_name: string | null } | null;
}
```

**2. supabase/migrations/20260406_comments.sql 생성:**

`supabase/migrations/` 디렉토리가 없으면 생성. 파일 내용:

```sql
-- Phase 10: Comments table for gallery feedback
CREATE TABLE IF NOT EXISTS comments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id UUID NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  content TEXT NOT NULL CHECK (char_length(content) <= 500),
  created_at TIMESTAMPTZ DEFAULT now()
);

ALTER TABLE comments ENABLE ROW LEVEL SECURITY;

-- 공개 세션 댓글은 모두 읽기 가능
CREATE POLICY "anyone can read comments on public sessions"
ON comments FOR SELECT
USING (
  EXISTS (SELECT 1 FROM sessions WHERE id = session_id AND is_public = true)
);

-- 인증 사용자만 댓글 작성
CREATE POLICY "authenticated users can insert comments"
ON comments FOR INSERT
WITH CHECK (auth.uid() = user_id);

-- 본인 댓글만 삭제
CREATE POLICY "users can delete own comments"
ON comments FOR DELETE
USING (auth.uid() = user_id);

-- likes 테이블에 created_at이 없을 경우 추가 (리더보드 주간 필터용)
DO $$
BEGIN
  IF NOT EXISTS (
    SELECT 1 FROM information_schema.columns
    WHERE table_name = 'likes' AND column_name = 'created_at'
  ) THEN
    ALTER TABLE likes ADD COLUMN created_at TIMESTAMPTZ DEFAULT now();
  END IF;
END $$;
```

**3. src/services/galleryService.ts 확장:**

- `fetchPage` 시그니처를 `(page: number, sortBy: GallerySortBy, filters?: GalleryFilters)` 로 변경. 기존 2-인자 호출과 하위 호환을 위해 `filters`는 옵셔널.
- filters 적용 로직 추가 (체이닝):
  - `filters.industry` 있으면: `.ilike('industry_id', \`${filters.industry}%\`)` (대분류 prefix 매칭, per D-02)
  - `filters.keyword` 있으면: `.ilike('title', \`%${filters.keyword}%\`)` (per D-04)
  - `filters.namingStyle`은 sessions 테이블에 직접 필터 불가 — 클라이언트 후처리 또는 input_data JSON 검색. 현실적으로 `.ilike('input_data', \`%${filters.namingStyle}%\`)`는 JSONB에 작동하지 않으므로, fetchPage 반환 후 store에서 클라이언트 필터링 처리. 서비스에서는 industry + keyword만 서버 쿼리.
- `fetchLeaderboard` 메서드 추가:
  ```typescript
  fetchLeaderboard: async (period: LeaderboardPeriod, limit = 5): Promise<LeaderboardEntry[]> => {
    // 1. likes 테이블에서 session_id별 count 조회
    //    period === 'week'이면 .gte('created_at', 7일전ISO) 필터 추가
    // 2. 클라이언트에서 session_id별 count 집계 (reduce)
    // 3. count 내림차순 정렬 후 상위 limit개 session_id 추출
    // 4. 해당 session_id들의 session 정보 fetchPage와 동일한 select로 조회
    // 5. LeaderboardEntry[] 반환
  }
  ```
  구체적 구현: likes 전체 조회 후 클라이언트 집계 (100인 앱이라 데이터 소규모).

import 추가: `import type { GalleryFilters, LeaderboardPeriod, LeaderboardEntry } from '../types/gallery';`

**4. src/services/socialService.ts 확장:**

기존 메서드는 그대로 유지. 아래 3개 메서드를 socialService 객체에 추가:

```typescript
getComments: async (sessionId: string): Promise<CommentData[]> => {
  const { data, error } = await supabase
    .from('comments')
    .select('id, content, created_at, user_id, profiles:user_id(full_name)')
    .eq('session_id', sessionId)
    .order('created_at', { ascending: true });
  if (error) throw error;
  return (data ?? []) as CommentData[];
},

addComment: async (sessionId: string, userId: string, content: string): Promise<void> => {
  const { error } = await supabase
    .from('comments')
    .insert({ session_id: sessionId, user_id: userId, content });
  if (error) throw error;
},

deleteComment: async (commentId: string): Promise<void> => {
  const { error } = await supabase
    .from('comments')
    .delete()
    .eq('id', commentId);
  if (error) throw error;
},
```

import 추가: `import type { CommentData } from '../types/gallery';`
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/types/gallery.ts contains `export interface GalleryFilters`
    - src/types/gallery.ts contains `export type LeaderboardPeriod = 'week' | 'all'`
    - src/types/gallery.ts contains `export interface LeaderboardEntry`
    - src/types/gallery.ts contains `export interface CommentData`
    - src/services/galleryService.ts contains `filters?: GalleryFilters`
    - src/services/galleryService.ts contains `.ilike('industry_id'`
    - src/services/galleryService.ts contains `fetchLeaderboard`
    - src/services/socialService.ts contains `getComments`
    - src/services/socialService.ts contains `addComment`
    - src/services/socialService.ts contains `deleteComment`
    - supabase/migrations/20260406_comments.sql contains `CREATE TABLE`
    - supabase/migrations/20260406_comments.sql contains `ROW LEVEL SECURITY`
    - npx tsc --noEmit exits with 0 (no type errors)
  </acceptance_criteria>
  <done>GalleryFilters/CommentData/LeaderboardEntry 타입 정의 완료, galleryService에 필터 쿼리+리더보드 쿼리 추가, socialService에 댓글 CRUD 추가, comments 테이블 마이그레이션 SQL 생성, TypeScript 컴파일 에러 없음</done>
</task>

<task type="auto">
  <name>Task 2: Zustand 스토어 확장 (필터 상태 + 리더보드 상태 + 댓글 상태)</name>
  <files>src/store/useGalleryStore.ts, src/store/useSocialStore.ts</files>
  <read_first>
    - src/store/useGalleryStore.ts (현재 GalleryState 인터페이스, setSortBy 리셋 패턴)
    - src/store/useSocialStore.ts (현재 SocialState 인터페이스, toggleLike 낙관적 업데이트 패턴)
    - src/services/galleryService.ts (Task 1에서 확장된 fetchPage/fetchLeaderboard 시그니처)
    - src/services/socialService.ts (Task 1에서 추가된 getComments/addComment/deleteComment)
    - src/types/gallery.ts (Task 1에서 추가된 GalleryFilters, LeaderboardEntry, CommentData)
  </read_first>
  <action>
**1. src/store/useGalleryStore.ts 확장:**

GalleryState 인터페이스에 추가:
```typescript
filters: GalleryFilters;
leaderboard: LeaderboardEntry[];
leaderboardPeriod: LeaderboardPeriod;
isLeaderboardLoading: boolean;

setFilters: (filters: Partial<GalleryFilters>) => void;
resetFilters: () => void;
fetchLeaderboard: (period?: LeaderboardPeriod) => Promise<void>;
setLeaderboardPeriod: (period: LeaderboardPeriod) => void;
```

initialState에 추가:
```typescript
filters: { industry: null, namingStyle: null, keyword: '' },
leaderboard: [],
leaderboardPeriod: 'week' as LeaderboardPeriod,
isLeaderboardLoading: false,
```

fetchNextPage 수정 — filters를 galleryService.fetchPage에 전달:
```typescript
const { isLoading, page, sortBy, filters } = get();
// ...
const newSessions = await galleryService.fetchPage(page, sortBy, filters);
```

namingStyle 클라이언트 필터링 추가 (fetchNextPage 내, 서버 결과 받은 후):
```typescript
// namingStyle 클라이언트 필터 (input_data.expression.namingStyle 배열에서 매칭)
let filtered = newSessions;
if (filters.namingStyle) {
  filtered = newSessions.filter(s => {
    const styles = s.input_data?.expression?.namingStyle;
    return Array.isArray(styles) && styles.includes(filters.namingStyle);
  });
}
```

setFilters 액션 — Pitfall 1 방지 (반드시 리셋 후 refetch):
```typescript
setFilters: (partial: Partial<GalleryFilters>) => {
  set(state => ({
    filters: { ...state.filters, ...partial },
    sessions: [],
    page: 0,
    hasMore: true,
    error: null,
  }));
  get().fetchNextPage();
},
```

resetFilters 액션:
```typescript
resetFilters: () => {
  set({
    filters: { industry: null, namingStyle: null, keyword: '' },
    sessions: [],
    page: 0,
    hasMore: true,
    error: null,
  });
  get().fetchNextPage();
},
```

fetchLeaderboard 액션:
```typescript
fetchLeaderboard: async (period?: LeaderboardPeriod) => {
  const p = period ?? get().leaderboardPeriod;
  set({ isLeaderboardLoading: true });
  try {
    const entries = await galleryService.fetchLeaderboard(p, 5);
    set({ leaderboard: entries, isLeaderboardLoading: false });
  } catch {
    set({ isLeaderboardLoading: false });
  }
},
```

setLeaderboardPeriod 액션:
```typescript
setLeaderboardPeriod: (period: LeaderboardPeriod) => {
  set({ leaderboardPeriod: period });
  get().fetchLeaderboard(period);
},
```

import 추가: `import type { GalleryFilters, LeaderboardPeriod, LeaderboardEntry } from '../types/gallery';`

**2. src/store/useSocialStore.ts 확장:**

SocialState 인터페이스에 추가:
```typescript
comments: CommentData[];
isCommentsLoading: boolean;

fetchComments: (sessionId: string) => Promise<void>;
addComment: (sessionId: string, userId: string, content: string, userName: string) => Promise<void>;
deleteComment: (commentId: string) => Promise<void>;
```

fetchComments 액션:
```typescript
fetchComments: async (sessionId: string) => {
  set({ isCommentsLoading: true });
  try {
    const comments = await socialService.getComments(sessionId);
    set({ comments, isCommentsLoading: false });
  } catch {
    set({ isCommentsLoading: false });
  }
},
```

addComment 액션 (낙관적 업데이트 — toggleLike 패턴과 동일):
```typescript
addComment: async (sessionId: string, userId: string, content: string, userName: string) => {
  const tempId = crypto.randomUUID();
  const tempComment: CommentData = {
    id: tempId,
    session_id: sessionId,
    user_id: userId,
    content,
    created_at: new Date().toISOString(),
    profiles: { full_name: userName },
  };
  set(state => ({ comments: [...state.comments, tempComment] }));
  try {
    await socialService.addComment(sessionId, userId, content);
    // re-fetch로 실제 ID 동기화
    const comments = await socialService.getComments(sessionId);
    set({ comments });
  } catch {
    // 롤백
    set(state => ({ comments: state.comments.filter(c => c.id !== tempId) }));
  }
},
```

deleteComment 액션 (낙관적 업데이트):
```typescript
deleteComment: async (commentId: string) => {
  const prev = get().comments;
  set(state => ({ comments: state.comments.filter(c => c.id !== commentId) }));
  try {
    await socialService.deleteComment(commentId);
  } catch {
    set({ comments: prev });
  }
},
```

import 추가: `import type { CommentData } from '../types/gallery';`
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/store/useGalleryStore.ts contains `filters: GalleryFilters`
    - src/store/useGalleryStore.ts contains `setFilters`
    - src/store/useGalleryStore.ts contains `resetFilters`
    - src/store/useGalleryStore.ts contains `fetchLeaderboard`
    - src/store/useGalleryStore.ts contains `setLeaderboardPeriod`
    - src/store/useGalleryStore.ts contains `leaderboard: LeaderboardEntry[]`
    - src/store/useGalleryStore.ts contains `galleryService.fetchPage(page, sortBy, filters)`
    - src/store/useSocialStore.ts contains `comments: CommentData[]`
    - src/store/useSocialStore.ts contains `fetchComments`
    - src/store/useSocialStore.ts contains `addComment`
    - src/store/useSocialStore.ts contains `deleteComment`
    - npx tsc --noEmit exits with 0 (no type errors)
  </acceptance_criteria>
  <done>useGalleryStore에 필터/리더보드 상태+액션 추가 완료, useSocialStore에 댓글 상태+낙관적 업데이트 액션 추가 완료, fetchNextPage가 filters를 galleryService.fetchPage에 전달, 필터 변경 시 sessions/page/hasMore 리셋 패턴 적용, TypeScript 컴파일 에러 없음</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — 전체 TypeScript 컴파일 에러 없음
2. `grep -n "GalleryFilters\|CommentData\|LeaderboardEntry\|LeaderboardPeriod" src/types/gallery.ts` — 4개 타입 모두 존재
3. `grep -n "fetchLeaderboard\|ilike" src/services/galleryService.ts` — 리더보드 쿼리 + 필터 쿼리 존재
4. `grep -n "getComments\|addComment\|deleteComment" src/services/socialService.ts` — 댓글 CRUD 3개 존재
5. `grep -n "setFilters\|fetchLeaderboard\|leaderboard" src/store/useGalleryStore.ts` — 필터+리더보드 상태 존재
6. `grep -n "fetchComments\|addComment\|deleteComment\|comments:" src/store/useSocialStore.ts` — 댓글 상태 존재
</verification>

<success_criteria>
- GalleryFilters, CommentData, LeaderboardEntry, LeaderboardPeriod 타입이 gallery.ts에 정의됨
- galleryService.fetchPage가 filters 옵셔널 파라미터를 받고 industry ilike + keyword ilike 쿼리 적용
- galleryService.fetchLeaderboard가 period별 TOP 5 세션을 좋아요 수로 반환
- socialService에 댓글 getComments/addComment/deleteComment 3개 메서드 추가
- useGalleryStore에 filters 상태 + setFilters/resetFilters + leaderboard 상태 + fetchLeaderboard/setLeaderboardPeriod 추가
- useSocialStore에 comments 상태 + fetchComments/addComment/deleteComment 낙관적 업데이트 추가
- comments 테이블 마이그레이션 SQL이 RLS 포함하여 생성됨
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/10-refinement-data-visualization/10-01-SUMMARY.md`
</output>
