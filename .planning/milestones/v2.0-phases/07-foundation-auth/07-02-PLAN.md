---
phase: 07-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [src/store/useAuthStore.ts, src/components/auth/LoginForm.tsx, src/components/auth/SignupForm.tsx, src/components/auth/ProtectedRoute.tsx, src/pages/LoginPage.tsx, src/pages/SignupPage.tsx, src/App.tsx]
autonomous: true
requirements: [REQ-01, REQ-04]
---

<objective>
Zustand 기반의 인증 상태 관리 시스템과 로그인/회원가입 UI를 구현합니다.
인증 여부에 따라 페이지 접근을 제한하는 보안 로직을 적용합니다.

Purpose: 사용자 인증 체계 구축 및 데이터 접근 권한 관리
Output: Auth Store, 로그인/회원가입 컴포넌트, 페이지 접근 가드
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation-auth/07-RESEARCH.md
@.planning/phases/07-foundation-auth/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth Store (Zustand) 및 인증 리스너 구현</name>
  <files>src/store/useAuthStore.ts, src/App.tsx</files>
  <action>
    - `useAuthStore`를 생성하여 `user`, `session`, `isLoading` 상태를 관리합니다.
    - `src/App.tsx` 또는 별도의 Provider에서 `supabase.auth.onAuthStateChange` 리스너를 설정하여 인증 상태를 스토어와 동기화합니다.
    - 로그인, 로그아웃, 회원가입 액션을 스토어에 추가합니다.
  </action>
  <verify>
    <automated>grep "useAuthStore" src/store/useAuthStore.ts && grep "onAuthStateChange" src/App.tsx</automated>
  </verify>
  <done>전역 인증 상태 관리 인프라 완료</done>
</task>

<task type="auto">
  <name>Task 2: 로그인 및 회원가입 UI 구현 (한글)</name>
  <files>src/components/auth/LoginForm.tsx, src/components/auth/SignupForm.tsx, src/pages/LoginPage.tsx, src/pages/SignupPage.tsx</files>
  <action>
    - Tailwind CSS를 사용하여 깔끔하고 직관적인 로그인/회원가입 폼을 제작합니다.
    - 모든 라벨 및 메시지는 한국어로 작성합니다.
    - 입력값 유효성 검사 및 에러 메시지 처리를 포함합니다. (REQ-01)
  </action>
  <verify>
    <automated>ls src/pages/LoginPage.tsx && grep "로그인" src/pages/LoginPage.tsx</automated>
  </verify>
  <done>사용자 인증 UI 개발 완료</done>
</task>

<task type="auto">
  <name>Task 3: Protected Route 및 접근 제어 구현</name>
  <files>src/components/auth/ProtectedRoute.tsx, src/App.tsx</files>
  <action>
    - 인증되지 않은 사용자가 대시보드 접근 시 로그인 페이지로 리다이렉트하는 `ProtectedRoute` 컴포넌트를 구현합니다.
    - `src/App.tsx`의 라우트 설정에 `ProtectedRoute`를 적용합니다.
  </action>
  <verify>
    <automated>grep "Navigate" src/components/auth/ProtectedRoute.tsx</automated>
  </verify>
  <done>페이지 수준의 인증 보안 가드 설정 완료</done>
</task>

</tasks>

<verification>
- 회원가입 후 자동 로그인이 되는지 확인.
- 로그아웃 후 보호된 경로(/dashboard 등) 접근 시 로그인 페이지로 튕기는지 확인.
- Zustand 스토어에 유저 정보가 정상적으로 담기는지 확인.
</verification>

<success_criteria>
- 이메일/비밀번호 기반의 인증 프로세스 작동.
- 인증 상태 변화에 따른 전역 상태 및 UI 업데이트.
- 권한 없는 사용자의 접근 차단.
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-auth/07-02-SUMMARY.md`
</output>
