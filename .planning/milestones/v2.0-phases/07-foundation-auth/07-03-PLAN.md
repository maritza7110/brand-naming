---
phase: 07-foundation-auth
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [src/pages/Dashboard.tsx, src/components/dashboard/ProjectList.tsx, src/components/dashboard/ProjectCard.tsx, src/services/sessionService.ts]
autonomous: true
requirements: [REQ-03, REQ-04]
---

<objective>
사용자의 이전 프로젝트 목록을 조회하고 관리할 수 있는 개인 대시보드를 구현합니다.
Supabase와의 실시간 데이터 연동 및 검색/필터링 기능을 제공합니다.

Purpose: 사용자의 프로젝트 관리 편의성 증대 및 데이터 영속성 활용
Output: 대시보드 UI, 프로젝트 목록 컴포넌트, 세션 관리 서비스
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation-auth/07-RESEARCH.md
@.planning/phases/07-foundation-auth/07-01-SUMMARY.md
@.planning/phases/07-foundation-auth/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 대시보드 레이아웃 및 프로젝트 목록 UI</name>
  <files>src/pages/Dashboard.tsx, src/components/dashboard/ProjectList.tsx, src/components/dashboard/ProjectCard.tsx</files>
  <action>
    - 전체 프로젝트 목록을 보여주는 그리드 레이아웃의 대시보드 페이지를 제작합니다.
    - `ProjectCard` 컴포넌트를 사용하여 각 세션의 제목, 산업분류, 생성일 등의 정보를 표시합니다.
    - `EmptyState` 컴포넌트를 적용하여 프로젝트가 없을 때의 UI를 처리합니다.
  </action>
  <verify>
    <automated>ls src/pages/Dashboard.tsx && grep "ProjectList" src/pages/Dashboard.tsx</automated>
  </verify>
  <done>대시보드 UI 기초 구성 완료</done>
</task>

<task type="auto">
  <name>Task 2: 세션 CRUD 데이터 서비스 구현</name>
  <files>src/services/sessionService.ts</files>
  <action>
    - Supabase를 호출하여 세션(프로젝트) 목록을 가져오고, 삭제하고, 수정하는 함수를 작성합니다.
    - `naming_results`를 포함하여 세션 상세 정보를 조회하는 로직을 포함합니다.
    - `date-fns`를 사용하여 날짜 데이터를 포맷팅합니다.
  </action>
  <verify>
    <automated>grep "supabase.from('sessions')" src/services/sessionService.ts</automated>
  </verify>
  <done>Supabase 기반 세션 관리 로직 완성</done>
</task>

<task type="auto">
  <name>Task 3: 대시보드 기능 통합 및 검색/필터링</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
    - 페이지 로드 시 `sessionService`를 통해 유저의 프로젝트 목록을 로드합니다.
    - 검색창(Search Input)을 통해 프로젝트 제목으로 필터링하는 클라이언트 사이드 로직을 구현합니다.
    - 새 프로젝트 생성 버튼을 클릭하면 메인 네이밍 페이지(`/`)로 이동하게 합니다.
  </action>
  <verify>
    <automated>grep "filter" src/pages/Dashboard.tsx</automated>
  </verify>
  <done>인터랙티브 대시보드 기능 구현 완료</done>
</task>

</tasks>

<verification>
- 대시보드 진입 시 본인의 프로젝트 목록만 정상적으로 표시되는지 확인 (RLS 검증).
- 프로젝트 제목 검색 시 실시간으로 목록이 필터링되는지 확인.
- 프로젝트 삭제 기능이 정상적으로 DB에 반영되는지 확인.
</verification>

<success_criteria>
- 개인 대시보드를 통한 과거 네이밍 기록 탐색 가능.
- 제목 기반 검색 및 프로젝트 삭제 기능 정상 작동.
- RLS 정책에 의해 타 사용자의 프로젝트가 노출되지 않음.
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-auth/07-03-SUMMARY.md`
</output>
