---
phase: 07-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/services/supabase.ts, supabase/migrations/07_init_schema.sql, src/main.tsx, src/App.tsx]
autonomous: true
requirements: [REQ-01, REQ-02, REQ-04]
user_setup:
  - service: supabase
    why: "인증 및 데이터베이스 인프라"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API"
    dashboard_config:
      - task: "프로젝트 생성 및 URL/Key 확보"
        location: "Supabase Dashboard"

must_haves:
  truths:
    - "Supabase 클라이언트가 환경 변수를 로드하여 초기화됨"
    - "데이터베이스에 profiles, sessions, naming_results, tags 테이블이 존재함"
    - "RLS 정책이 적용되어 인증된 유저만 자신의 데이터에 접근 가능함"
  artifacts:
    - path: "src/services/supabase.ts"
      provides: "Supabase client instance"
    - path: "supabase/migrations/07_init_schema.sql"
      provides: "Database schema and RLS policies"
  key_links:
    - from: "src/services/supabase.ts"
      to: "VITE_SUPABASE_URL"
      via: "import.meta.env"
      pattern: "import\\.meta\\.env\\.VITE_SUPABASE_URL"
---

<objective>
Supabase 인프라를 구축하고 기본 DB 스키마와 RLS 보안 정책을 설정합니다.
Vite 환경에서의 라이브러리 연동 및 라우팅 구조를 준비합니다.

Purpose: 영속성 있는 데이터 저장소 확보 및 보안 기반 마련
Output: Supabase 클라이언트, SQL 스키마 파일, 라우팅 기초 구조
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation-auth/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 의존성 설치 및 Supabase 클라이언트 초기화</name>
  <files>package.json, src/services/supabase.ts, .env.example</files>
  <action>
    - `@supabase/supabase-js`, `react-router-dom`, `date-fns` 패키지를 설치합니다.
    - `src/services/supabase.ts` 파일을 생성하여 `VITE_SUPABASE_URL` 및 `VITE_SUPABASE_ANON_KEY`를 사용하는 클라이언트를 export합니다.
    - `.env.example` 파일을 생성하여 필요한 환경 변수 목록을 기록합니다.
  </action>
  <verify>
    <automated>npm list @supabase/supabase-js react-router-dom date-fns && ls src/services/supabase.ts</automated>
  </verify>
  <done>클라이언트 라이브러리 연동 및 설정 완료</done>
</task>

<task type="auto">
  <name>Task 2: DB 스키마 및 RLS 정책 SQL 작성</name>
  <files>supabase/migrations/07_init_schema.sql</files>
  <action>
    - `profiles`, `sessions`, `naming_results`, `tags`, `session_tags` 테이블을 생성하는 SQL을 작성합니다.
    - `profiles` 자동 생성을 위한 `handle_new_user()` 함수 및 `auth.users` 트리거를 포함합니다.
    - 모든 테이블에 RLS를 활성화하고, `auth.uid() = user_id` 기반의 보안 정책을 설정합니다. (REQ-04)
    - SQL 내에 한글 주석을 포함하여 구조를 설명합니다.
  </action>
  <verify>
    <automated>ls supabase/migrations/07_init_schema.sql && grep "ENABLE ROW LEVEL SECURITY" supabase/migrations/07_init_schema.sql</automated>
  </verify>
  <done>DB 스키마 정의 및 보안 정책 수립 완료</done>
</task>

<task type="auto">
  <name>Task 3: 라우팅 기초 구조 설정 (React Router v7)</name>
  <files>src/main.tsx, src/App.tsx</files>
  <action>
    - `BrowserRouter`를 `src/main.tsx`에 설정합니다.
    - `src/App.tsx`를 `Routes`, `Route` 구조로 개편하여 향후 추가될 대시보드 및 로그인 페이지를 수용할 수 있게 합니다.
    - 기존 메인 네이밍 UI를 `/` 경로에 배치합니다.
  </action>
  <verify>
    <automated>grep "BrowserRouter" src/main.tsx && grep "Routes" src/App.tsx</automated>
  </verify>
  <done>어플리케이션 라우팅 기반 마련</done>
</task>

</tasks>

<verification>
- `supabase/migrations/07_init_schema.sql` 내용을 Supabase SQL Editor에 복사하여 실행했을 때 오류가 없는지 확인 (Manual).
- 환경 변수 설정 후 `src/services/supabase.ts`가 정상적으로 로드되는지 확인.
</verification>

<success_criteria>
- Supabase 라이브러리 연동 및 클라이언트 초기화 코드 완료.
- 5개의 핵심 테이블 및 RLS 정책을 담은 SQL 마이그레이션 파일 생성.
- React Router를 통한 페이지 전환 준비 완료.
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-auth/07-01-SUMMARY.md`
</output>
