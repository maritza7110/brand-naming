---
phase: 11-ux
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - src/components/wizard/AnalysisTab.tsx
  - src/components/wizard/IdentityTab.tsx
  - src/components/sections/CompetitorSection.tsx
  - src/components/sections/USPSection.tsx
  - src/components/sections/PersonaSection.tsx
autonomous: true
requirements:
  - UX-TAB-CONTENT-RESHUFFLE
must_haves:
  truths:
    - "분석탭에 MarketTrendSection이 USPSection 아래에 렌더된다"
    - "분석탭의 경쟁사/USP 섹션에 '시장 현황 관점' 배지가 표시된다"
    - "정체성탭에서 PersonaSection과 MarketTrendSection이 제거되어 BrandVision + BrandPersonality만 남는다"
    - "정체성탭 키워드 추출에서 persona/marketTrend 관련 로직이 제거된다"
    - "분석탭 키워드 추출에 시장트렌드 항목이 추가된다"
    - "정체성탭에서 페르소나 관련 필드가 표시되지 않고 BrandVision + BrandPersonality만 렌더된다"
  artifacts:
    - path: "src/components/wizard/AnalysisTab.tsx"
      provides: "분석탭 with MarketTrend + 관점 배지"
      contains: "MarketTrendSection"
    - path: "src/components/wizard/IdentityTab.tsx"
      provides: "정체성탭 (BrandVision + BrandPersonality only)"
      min_lines: 30
    - path: "src/components/sections/CompetitorSection.tsx"
      provides: "경쟁사 섹션 with 관점 배지"
      contains: "시장 현황 관점"
    - path: "src/components/sections/USPSection.tsx"
      provides: "USP 섹션 with 관점 배지"
      contains: "시장 현황 관점"
  key_links:
    - from: "src/components/wizard/AnalysisTab.tsx"
      to: "src/components/sections/MarketTrendSection.tsx"
      via: "import MarketTrendSection"
      pattern: "import.*MarketTrendSection"
    - from: "src/components/wizard/AnalysisTab.tsx"
      to: "src/store/useFormStore.ts"
      via: "identity.marketTrend selector"
      pattern: "identity\\.marketTrend"
---

<objective>
탭 간 컨텐츠를 재배치한다: MarketTrendSection을 정체성→분석탭으로 이동, 정체성탭에서 PersonaSection/MarketTrendSection 제거, 분석탭 경쟁사/USP에 "시장 현황 관점" 배지 추가.

Purpose: D-04(시장트렌드 이동)와 D-08(관점 차이 명시)을 구현하여 탭 간 역할 분리 완성
Output: 분석탭과 정체성탭의 최종 컨텐츠 구성
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ux/11-CONTEXT.md
@.planning/phases/11-ux/11-UI-SPEC.md
@.planning/phases/11-ux/11-01-SUMMARY.md
@.planning/phases/11-ux/11-02-SUMMARY.md

<interfaces>
<!-- AnalysisTab.tsx current structure (77 lines) -->
```typescript
// Render order: StoreBasicSection → CompetitorSection → USPSection → KWSlider → RecommendButton
// Keywords: storeBasic fields + analysis.competitors + analysis.usp
// hasInput: storeBasic.industry.major || storeBasic text fields || analysis fields
```

<!-- IdentityTab.tsx current structure (90 lines) -->
```typescript
// Render order: BrandVisionSection → PersonaSection → MarketTrendSection → BrandPersonalitySection
// Keywords: brandVision fields + persona 16 fields + identity.marketTrend + identity.brandPersonality
// hasInput: brandVision || persona || identity.marketTrend || identity.brandPersonality
// PERSONA_LABELS constant (16 entries) — will be removed
```

<!-- MarketTrendSection.tsx (21 lines, no changes to this file) -->
```typescript
// Uses: useFormStore(s => s.identity.marketTrend) + updateIdentityText('marketTrend', v)
// Style: rounded-2xl bg-[#E8E4DE] p-5 lg:p-7 border border-[#C5BFB7]
```

<!-- CompetitorSection.tsx (21 lines) -->
```typescript
export function CompetitorSection() {
  const competitors = useFormStore((s) => s.analysis.competitors);
  const updateAnalysis = useFormStore((s) => s.updateAnalysis);
  return (
    <section className="rounded-2xl bg-[#E8E4DE] p-5 lg:p-7 border border-[#C5BFB7]">
      <SectionHeader title="경쟁사 분석" />
      <TextArea label="경쟁사 분석" value={competitors}
        onChange={(v) => updateAnalysis('competitors', v)}
        placeholder="주요 경쟁 브랜드명과 그 특징을 적어주세요 (예: '스타벅스 - 프리미엄 이미지, 이디야 - 가성비')"
        rows={3} />
    </section>
  );
}
```

<!-- USPSection.tsx (21 lines) -->
```typescript
export function USPSection() {
  const usp = useFormStore((s) => s.analysis.usp);
  const updateAnalysis = useFormStore((s) => s.updateAnalysis);
  return (
    <section className="rounded-2xl bg-[#E8E4DE] p-5 lg:p-7 border border-[#C5BFB7]">
      <SectionHeader title="USP (차별화 요소)" />
      <TextArea label="USP (차별화 요소)" value={usp}
        onChange={(v) => updateAnalysis('usp', v)}
        placeholder="다른 브랜드와 구별되는 핵심 차별점을 적어주세요 (예: '로컬 원두 직접 로스팅, 반려동물 동반 특화')"
        rows={2} />
    </section>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 분석탭에 MarketTrendSection 추가 + 경쟁사/USP 관점 배지</name>
  <files>
    src/components/wizard/AnalysisTab.tsx,
    src/components/sections/CompetitorSection.tsx,
    src/components/sections/USPSection.tsx
  </files>
  <read_first>
    src/components/wizard/AnalysisTab.tsx,
    src/components/sections/CompetitorSection.tsx,
    src/components/sections/USPSection.tsx,
    src/components/sections/MarketTrendSection.tsx,
    src/store/useFormStore.ts
  </read_first>
  <action>
**1. AnalysisTab.tsx 수정 (per D-04):**

import 추가:
```typescript
import { MarketTrendSection } from '../sections/MarketTrendSection';
```

identity.marketTrend에 접근하기 위한 store selector 추가:
```typescript
const identity = useFormStore((s) => s.identity);
```

키워드 추출에 시장트렌드 추가 (analysis.usp 키워드 추출 뒤에):
```typescript
if (identity.marketTrend.trim() !== '') {
  keywords.push({ label: '시장트렌드', weight: keywordWeights['시장트렌드'] ?? 3 });
}
```

hasInput 조건에 추가:
```typescript
const hasInput =
  storeBasic.industry.major !== '' ||
  Object.entries(storeBasic)
    .filter(([k]) => k !== 'industry')
    .some(([, v]) => typeof v === 'string' && v.trim() !== '') ||
  analysis.competitors.trim() !== '' ||
  analysis.usp.trim() !== '' ||
  identity.marketTrend.trim() !== '';
```

렌더링 순서 변경 — USPSection 아래에 MarketTrendSection 추가:
```tsx
<StoreBasicSection />
<CompetitorSection />
<USPSection />
<MarketTrendSection />
{/* KeywordWeightSlider + RecommendButton 기존 위치 유지 */}
```

**2. CompetitorSection.tsx 수정 (per D-08):**

SectionHeader 아래, TextArea 위에 관점 배지 + 헬퍼 텍스트 추가:
```tsx
<section className="rounded-2xl bg-[#E8E4DE] p-5 lg:p-7 border border-[#C5BFB7]">
  <SectionHeader title="경쟁사 분석" />
  <div className="flex items-center gap-2 mb-3">
    <span className="inline-block px-2 py-0.5 rounded-full text-[14px] text-[#6B6560] bg-[#DDD7CF]">
      시장 현황 관점
    </span>
  </div>
  <p className="text-[14px] text-[#6B6560] mb-4">
    경쟁사가 누구이고, 시장에서 어떤 포지션을 점유하고 있는지 적어주세요. 외부에서 바라본 사실 위주로.
  </p>
  <TextArea
    label="경쟁사 분석"
    value={competitors}
    onChange={(v) => updateAnalysis('competitors', v)}
    placeholder="예: 스타벅스, 블루보틀 — 프리미엄 원두와 공간 경험으로 선점"
    rows={3}
  />
</section>
```

placeholder를 UI-SPEC에 정의된 값으로 변경:
- 기존: `주요 경쟁 브랜드명과 그 특징을 적어주세요 (예: '스타벅스 - 프리미엄 이미지, 이디야 - 가성비')`
- 변경: `예: 스타벅스, 블루보틀 — 프리미엄 원두와 공간 경험으로 선점`

**3. USPSection.tsx 수정 (per D-08):**

동일 패턴으로 관점 배지 + 헬퍼 텍스트 추가:
```tsx
<section className="rounded-2xl bg-[#E8E4DE] p-5 lg:p-7 border border-[#C5BFB7]">
  <SectionHeader title="USP (차별화 요소)" />
  <div className="flex items-center gap-2 mb-3">
    <span className="inline-block px-2 py-0.5 rounded-full text-[14px] text-[#6B6560] bg-[#DDD7CF]">
      시장 현황 관점
    </span>
  </div>
  <p className="text-[14px] text-[#6B6560] mb-4">
    지금 시장에서 경쟁사 대비 눈에 띄는 차별점은 무엇인가요?
  </p>
  <TextArea
    label="USP (차별화 요소)"
    value={usp}
    onChange={(v) => updateAnalysis('usp', v)}
    placeholder="예: 동네 단 하나뿐인 스페셜티 로스터리, 30분 내 픽업"
    rows={2}
  />
</section>
```

placeholder를 UI-SPEC에 정의된 값으로 변경:
- 기존: `다른 브랜드와 구별되는 핵심 차별점을 적어주세요 (예: '로컬 원두 직접 로스팅, 반려동물 동반 특화')`
- 변경: `예: 동네 단 하나뿐인 스페셜티 로스터리, 30분 내 픽업`
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/AnalysisTab.tsx contains `import { MarketTrendSection }`
    - src/components/wizard/AnalysisTab.tsx contains `identity.marketTrend`
    - src/components/wizard/AnalysisTab.tsx contains `시장트렌드` in keyword extraction
    - src/components/wizard/AnalysisTab.tsx renders MarketTrendSection after USPSection (MarketTrendSection appears after USPSection in JSX)
    - src/components/sections/CompetitorSection.tsx contains `시장 현황 관점`
    - src/components/sections/CompetitorSection.tsx contains `경쟁사가 누구이고`
    - src/components/sections/CompetitorSection.tsx contains `bg-[#DDD7CF]`
    - src/components/sections/USPSection.tsx contains `시장 현황 관점`
    - src/components/sections/USPSection.tsx contains `경쟁사 대비 눈에 띄는`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>분석탭에 MarketTrendSection이 추가되고, 경쟁사/USP 섹션에 "시장 현황 관점" 배지와 헬퍼 텍스트가 표시된다</done>
</task>

<task type="auto">
  <name>Task 2: 정체성탭 슬림화 + PersonaSection 삭제</name>
  <files>
    src/components/wizard/IdentityTab.tsx,
    src/components/sections/PersonaSection.tsx
  </files>
  <read_first>
    src/components/wizard/IdentityTab.tsx,
    src/components/sections/PersonaSection.tsx
  </read_first>
  <action>
**1. IdentityTab.tsx 대폭 수정:**

제거할 것:
- `import { PersonaSection }` 제거
- `import { MarketTrendSection }` 제거
- `PERSONA_LABELS` 상수 전체 제거 (16개 엔트리)
- `const persona = useFormStore((s) => s.persona);` 제거
- `const identity = useFormStore((s) => s.identity);` 에서 marketTrend 관련 키워드 추출 제거
- persona 키워드 추출 `Object.entries(persona).forEach(...)` 블록 제거
- marketTrend 키워드 추출 `if (identity.marketTrend.trim() !== '')` 제거
- hasInput에서 `Object.values(persona).some(...)` 제거
- hasInput에서 `identity.marketTrend.trim() !== ''` 제거
- 렌더에서 `<PersonaSection />` 제거
- 렌더에서 `<MarketTrendSection />` 제거

남길 것:
- BrandVisionSection import + 렌더
- BrandPersonalitySection import + 렌더
- BRAND_VISION_LABELS 상수 (3개)
- brandVision 키워드 추출 로직
- brandPersonality 키워드 추출 로직
- hasInput: brandVision || brandPersonality

변경 후 IdentityTab 전체 구조:
```typescript
import { useFormStore } from '../../store/useFormStore';
import { BrandVisionSection } from '../sections/BrandVisionSection';
import { BrandPersonalitySection } from '../sections/BrandPersonalitySection';
import { KeywordWeightSlider } from '../ui/KeywordWeightSlider';
import { RecommendButton } from '../ui/RecommendButton';
import { useRecommend } from '../../hooks/useRecommend';

const BRAND_VISION_LABELS: Record<string, string> = {
  ceoVision: 'CEO비전',
  longTermGoal: '장기목표',
  personalStory: '개인스토리',
};

export function IdentityTab() {
  const brandVision = useFormStore((s) => s.brandVision);
  const identity = useFormStore((s) => s.identity);
  const keywordWeights = useFormStore((s) => s.keywordWeights);
  const setKeywordWeight = useFormStore((s) => s.setKeywordWeight);
  const { recommend, isLoading } = useRecommend();

  const keywords: { label: string; weight: number }[] = [];

  Object.entries(brandVision).forEach(([key, value]) => {
    if (typeof value === 'string' && value.trim() !== '') {
      const label = BRAND_VISION_LABELS[key] ?? key;
      keywords.push({ label, weight: keywordWeights[label] ?? 3 });
    }
  });

  if (identity.brandPersonality.length > 0) {
    keywords.push({ label: '브랜드퍼스널리티', weight: keywordWeights['브랜드퍼스널리티'] ?? 3 });
  }

  const hasInput =
    Object.values(brandVision).some((v) => v.trim() !== '') ||
    identity.brandPersonality.length > 0;

  return (
    <div className="space-y-5">
      <BrandVisionSection />
      <BrandPersonalitySection />
      {keywords.length > 0 && (
        <div className="rounded-2xl bg-[#363230] p-5 lg:p-7 border border-[#4A4440]">
          <KeywordWeightSlider keywords={keywords} onChange={setKeywordWeight} />
        </div>
      )}
      <div className="flex justify-end">
        <RecommendButton onClick={recommend} loading={isLoading} disabled={!hasInput} />
      </div>
    </div>
  );
}
```

예상 라인 수: ~45줄 (현재 90줄에서 절반 축소)

**2. PersonaSection.tsx 삭제:**

**IMPORTANT: 삭제 전 반드시 사용자에게 확인을 받는다.** 프로젝트 규칙(destructive-commands.md)에 따라, `rm` 명령 실행 전에 "PersonaSection.tsx를 삭제해도 될까요?" 라고 사용자에게 물어본 후, 명시적 승인을 받은 뒤에만 삭제를 진행한다.

삭제 전 codebase에서 PersonaSection import가 없는지 확인:
```bash
grep -r "PersonaSection" src/ --include="*.tsx" --include="*.ts"
```
IdentityTab.tsx에서 제거 후 결과가 PersonaSection.tsx 자체만 나오면 안전하게 삭제 가능.

`src/components/sections/PersonaSection.tsx` 파일을 삭제한다. 이 파일은 더 이상 어디서도 import되지 않는다 (IdentityTab에서 제거 완료).
  </action>
  <verify>
    <automated>cd C:/BRAND && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/IdentityTab.tsx does NOT contain `PersonaSection`
    - src/components/wizard/IdentityTab.tsx does NOT contain `MarketTrendSection`
    - src/components/wizard/IdentityTab.tsx does NOT contain `PERSONA_LABELS`
    - src/components/wizard/IdentityTab.tsx contains `BrandVisionSection` and `BrandPersonalitySection`
    - src/components/wizard/IdentityTab.tsx does NOT contain `persona` store selector
    - src/components/sections/PersonaSection.tsx does NOT exist (deleted)
    - TypeScript compilation passes with no errors
    - grep -r "PersonaSection" src/ returns no results
  </acceptance_criteria>
  <done>정체성탭이 BrandVision + BrandPersonality만 렌더하고, PersonaSection.tsx가 삭제되었으며, 키워드 추출에서 persona/marketTrend 로직이 제거됨</done>
</task>

</tasks>

<verification>
```bash
cd C:/BRAND && npx tsc --noEmit
cd C:/BRAND && npx vite build --mode development 2>&1 | tail -5
cd C:/BRAND && grep -r "PersonaSection" src/ --include="*.tsx" --include="*.ts" | grep -v "persona/"
```
- TypeScript 에러 없음
- Vite 빌드 성공
- PersonaSection 참조가 코드베이스에서 완전 제거됨
</verification>

<success_criteria>
- 분석탭 렌더 순서: StoreBasic → Competitor → USP → MarketTrend → KWSlider → Button
- 경쟁사/USP 섹션에 "시장 현황 관점" 배지 + 헬퍼 텍스트 표시
- 분석탭 키워드 추출에 시장트렌드 포함
- 정체성탭: BrandVision + BrandPersonality만 남음
- PersonaSection.tsx 삭제됨
- 기존 기능(각 탭 추천 버튼, 키워드 슬라이더) 동작 유지
</success_criteria>

<output>
After completion, create `.planning/phases/11-ux/11-03-SUMMARY.md`
</output>
